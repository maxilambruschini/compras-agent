---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/storage.py
  - backend/app/services/extraction.py
  - backend/app/routers/extraction.py
  - backend/app/config.py
  - backend/app/main.py
  - backend/tests/test_storage.py
  - backend/tests/test_extraction.py
  - backend/pyproject.toml
autonomous: true
requirements: [EXT-06, EXT-07, VAL-04, VAL-05]
tags: [extraction, storage, scaffold, mvp-skeleton]

must_haves:
  truths:
    - "Developer can POST an image to /extraction/test when DEBUG=true and receive an ExtractionResult JSON"
    - "LocalStorageBackend.save() writes bytes to {STORAGE_PATH}/{uuid}/{filename} and returns the relative path"
    - "LocalStorageBackend.save() rejects/sanitizes path-traversal filenames via os.path.basename() (T-02-01)"
    - "ExtractionService.extract() returns an ExtractionResult containing invoice, confidence_score, status, image_path"
    - "Confidence score equals non_null({tipo_comprobante, numero_documento, proveedor, fecha}) / 4 (D-01)"
    - "status == 'auto_saved' when score >= settings.confidence_threshold, else 'pending_review' (D-03)"
    - "Test suite covering storage + extraction service + debug router runs green via `pytest -m 'not integration'`"
    - "Settings exposes storage_path with default '/data/invoices' (D-08)"
    - "/extraction/test is NOT registered when settings.debug is False (D-05)"
  artifacts:
    - path: "backend/app/services/storage.py"
      provides: "StorageBackend Protocol + LocalStorageBackend with path-traversal sanitization"
      contains: "class StorageBackend(Protocol)"
    - path: "backend/app/services/extraction.py"
      provides: "ExtractionService class + ExtractionResult DTO (skeleton using injected OpenAI mock-friendly client)"
      contains: "class ExtractionService"
    - path: "backend/app/routers/extraction.py"
      provides: "POST /test endpoint accepting UploadFile, returns ExtractionResult JSON"
      contains: "@router.post(\"/test\")"
    - path: "backend/tests/test_storage.py"
      provides: "Unit tests for LocalStorageBackend.save + path traversal sanitization"
    - path: "backend/tests/test_extraction.py"
      provides: "Unit test stubs for confidence score, status assignment, storage wiring, refusal handling, debug-router gating"
    - path: "backend/app/config.py"
      provides: "Adds storage_path: str = '/data/invoices' to Settings"
      contains: "storage_path"
    - path: "backend/app/main.py"
      provides: "Conditional extraction router registration when settings.debug"
      contains: "if settings.debug"
    - path: "backend/pyproject.toml"
      provides: "Registers 'integration' pytest marker"
      contains: "integration"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/routers/extraction.py"
      via: "conditional include_router inside create_app() guarded by settings.debug"
      pattern: "if settings.debug"
    - from: "backend/app/routers/extraction.py"
      to: "backend/app/services/extraction.py"
      via: "FastAPI dependency that constructs ExtractionService with AsyncOpenAI + LocalStorageBackend + Settings"
      pattern: "ExtractionService"
    - from: "backend/app/services/extraction.py"
      to: "backend/app/services/storage.py"
      via: "StorageBackend.save() called inside ExtractionService.extract() with '{uuid}/{basename(filename)}'"
      pattern: "storage.save"
    - from: "backend/app/services/storage.py"
      to: "backend/app/config.py"
      via: "LocalStorageBackend root constructed from settings.storage_path"
      pattern: "storage_path"
---

<objective>
Deliver the thinnest end-to-end vertical slice of the extraction pipeline: a developer can `curl -F file=@invoice.jpg http://localhost:8000/extraction/test` (with DEBUG=true) and receive an `ExtractionResult` JSON containing a valid `ExtractedInvoice`, a confidence score, a status, and the storage path of the saved image — with the GPT-4o call mockable. This plan establishes the StorageBackend Protocol + LocalStorageBackend, the ExtractionService class with confidence/status logic and storage wiring, the debug-gated FastAPI router, the `storage_path` Setting, the `integration` pytest marker, and the full mocked-test scaffold for Phase 2.

Purpose: Lock the public surface (interfaces, return types, file paths, routing, settings) before Plan 02 wires the real GPT-4o call and before Plan 03 builds the calibration script. Tests created here exercise every requirement (EXT-06, EXT-07, VAL-04, VAL-05) using mocked `AsyncOpenAI.chat.completions.parse` so the suite is fast and deterministic.

Output: New services/storage.py, services/extraction.py, routers/extraction.py; modified config.py, main.py, pyproject.toml; new test_storage.py, test_extraction.py — all green under `pytest -m 'not integration'`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-extraction-pipeline/02-CONTEXT.md
@.planning/phases/02-extraction-pipeline/02-RESEARCH.md
@.planning/phases/02-extraction-pipeline/02-PATTERNS.md
@.planning/phases/02-extraction-pipeline/02-VALIDATION.md

@backend/app/models/extraction.py
@backend/app/config.py
@backend/app/main.py
@backend/app/routers/health.py
@backend/app/db/session.py
@backend/tests/conftest.py
@backend/tests/test_health.py
@backend/pyproject.toml

<interfaces>
<!-- Locked contracts produced by this plan and consumed by Plan 02/03 and Phase 3 -->

Phase-1 model (DO NOT modify) — backend/app/models/extraction.py:
- class TipoComprobante(str, Enum): FACTURA_A | FACTURA_B | FACTURA_C | REMITO | LISTA_INFORMAL | UNKNOWN
- class LineItem(BaseModel): all Optional fields, model_config = ConfigDict(use_enum_values=True)
- class ExtractedInvoice(BaseModel): top-level fields tipo_comprobante, numero_documento, proveedor, fecha, cuit_proveedor, cae, fecha_vencimiento_cae, line_items, model_config = ConfigDict(use_enum_values=True)

StorageBackend Protocol (this plan creates):
- @runtime_checkable class StorageBackend(Protocol):
-     def save(self, data: bytes, filename: str) -> str: ...   # returns relative path

LocalStorageBackend (this plan creates):
- __init__(self, root: str) -> None
- save(self, data: bytes, filename: str) -> str — sanitizes filename via os.path.basename(), os.makedirs(parent, exist_ok=True), writes bytes, returns the input filename (relative path; root excluded per D-07).

ExtractionResult Pydantic DTO (this plan creates):
- class ExtractionResult(BaseModel):
-     invoice: ExtractedInvoice
-     confidence_score: float           # 0.0..1.0
-     status: str                       # "auto_saved" | "pending_review"
-     image_path: str                   # relative path returned by StorageBackend.save

ExtractionService (this plan creates as functioning skeleton — Plan 02 enriches the prompt + error semantics):
- __init__(self, openai_client: AsyncOpenAI, storage: StorageBackend, settings: Settings) -> None
- async extract(self, image_bytes: bytes, filename: str) -> ExtractionResult
-   1. invoice_uuid = str(uuid.uuid4())
-   2. safe_filename = os.path.basename(filename)
-   3. relative_path = self._storage.save(image_bytes, f"{invoice_uuid}/{safe_filename}")
-   4. (parsed, refusal) = await self._call_gpt4o(image_bytes)
-   5. if refusal is not None: raise ExtractionRefusalError(refusal)
-   6. if parsed is None: raise ExtractionFailedError("parsed is None")
-   7. score = compute_confidence(parsed) ; status = assign_status(score, settings.confidence_threshold)
-   8. return ExtractionResult(invoice=parsed, confidence_score=score, status=status, image_path=relative_path)

Custom exception classes (this plan creates):
- class ExtractionError(Exception): pass
- class ExtractionRefusalError(ExtractionError): pass
- class ExtractionFailedError(ExtractionError): pass

Router contract (this plan creates):
- APIRouter at backend/app/routers/extraction.py with router = APIRouter()
- POST /test  — accepts UploadFile (form field name: "file"); returns ExtractionResult JSON
- Service constructed inside the route via Depends() that injects AsyncOpenAI, LocalStorageBackend, Settings.
- Registered ONLY when settings.debug is True via `app.include_router(extraction_router, prefix="/extraction", tags=["extraction"])` in create_app().

Settings field (this plan adds):
- storage_path: str = "/data/invoices"

pytest marker registration (this plan adds in backend/pyproject.toml):
- markers = ["integration: marks tests as live API integration tests (deselect with '-m not integration')"]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffolding — fixtures, markers, failing stubs</name>
  <files>backend/tests/test_storage.py, backend/tests/test_extraction.py, backend/pyproject.toml</files>
  <read_first>
    backend/tests/conftest.py (existing env_setup + db_session fixtures — inherited automatically)
    backend/tests/test_health.py (ASGI client + dependency_overrides pattern — copy verbatim style)
    backend/tests/test_extraction_models.py (pure Pydantic test style)
    backend/app/models/extraction.py (ExtractedInvoice + TipoComprobante — used as parsed value in mocks)
    backend/pyproject.toml (current pytest config — add markers key)
    .planning/phases/02-extraction-pipeline/02-RESEARCH.md (Pattern 5 mock construction; Pattern 7 conditional router; Test Framework section)
    .planning/phases/02-extraction-pipeline/02-PATTERNS.md (Mock OpenAI parse() pattern verbatim; Integration test marker pattern)
  </read_first>
  <behavior>
    - test_storage.py::test_save_writes_bytes_and_returns_relative_path  — given root=tmp_path, save(b"hello", "abc-uuid/photo.jpg") creates "{tmp_path}/abc-uuid/photo.jpg" with contents b"hello" and returns "abc-uuid/photo.jpg".
    - test_storage.py::test_save_creates_parent_directories — directory "{tmp_path}/abc-uuid/" is auto-created.
    - test_storage.py::test_save_sanitizes_path_traversal — save(b"x", "../../etc/passwd") writes ONLY to "{tmp_path}/passwd" (basename stripping). The string "etc" must NOT appear in any path component below tmp_path. (T-02-01)
    - test_storage.py::test_save_with_uuid_prefix_filename — save(b"x", "uuid-xxx/../../bad") writes to "{tmp_path}/bad" (basename of full filename).
    - test_extraction.py::test_compute_confidence_all_four_critical_fields_present — ExtractedInvoice with tipo=FACTURA_A, numero="X", proveedor="Y", fecha="2026-01-01" → 1.0.
    - test_extraction.py::test_compute_confidence_two_of_four_present — only proveedor + fecha set → 0.5.
    - test_extraction.py::test_compute_confidence_all_none → 0.0.
    - test_extraction.py::test_assign_status_auto_saved_at_threshold — score=0.85, threshold=0.85 → "auto_saved".
    - test_extraction.py::test_assign_status_pending_review_below_threshold — score=0.5 → "pending_review".
    - test_extraction.py::test_extract_calls_storage_with_uuid_and_basename — mock storage; assert storage.save called with bytes + "<uuid>/photo.jpg" pattern (regex r"^[0-9a-f-]{36}/photo\.jpg$").
    - test_extraction.py::test_extract_returns_extraction_result_with_image_path — image_path on result equals the mocked storage.save return.
    - test_extraction.py::test_extract_raises_refusal_error_when_message_refusal_set — mock parse() returns completion with msg.refusal="reason" and msg.parsed=None → service raises ExtractionRefusalError.
    - test_extraction.py::test_extract_raises_extraction_failed_when_parsed_is_none_and_no_refusal — msg.parsed=None and msg.refusal=None → ExtractionFailedError.
    - test_extraction.py::test_extract_logs_error_with_filename_on_failure — using caplog or capsys + structlog testing, assert event "extraction.failed" emitted with filename binding (VAL-05).
    - test_extraction.py::test_post_extraction_test_returns_extraction_result_when_debug_true — ASGI client; monkeypatch settings.debug=True; POST multipart file → 200 with JSON keys {invoice, confidence_score, status, image_path}.
    - test_extraction.py::test_post_extraction_test_404_when_debug_false — when settings.debug=False, the router is not registered → 404.
  </behavior>
  <action>
    Create backend/tests/test_storage.py:
    - Import pytest, tempfile, pathlib.
    - Import LocalStorageBackend from app.services.storage (import will fail until Task 2; that is expected RED).
    - Write the four storage tests above using tmp_path fixture; assert returned relative path string matches input and file contents on disk match.
    - The path-traversal test asserts: list(pathlib.Path(tmp_path).rglob("*passwd*"))  contains exactly one file whose parent == tmp_path (not a nested "etc/" dir).

    Create backend/tests/test_extraction.py:
    - Imports: pytest, pytest_asyncio, httpx, structlog, from unittest.mock import MagicMock, AsyncMock; from openai.types.chat.parsed_chat_completion import ParsedChatCompletion, ParsedChoice, ParsedChatCompletionMessage; from app.models.extraction import ExtractedInvoice, TipoComprobante; from app.services.extraction import ExtractionService, ExtractionResult, ExtractionRefusalError, ExtractionFailedError, compute_confidence, assign_status; from app.services.storage import StorageBackend.
    - Define helper make_mock_completion(parsed_invoice, refusal=None) using the verbatim Pattern 5 mock from 02-PATTERNS.md.
    - Define pytest_asyncio fixture extraction_service that constructs ExtractionService with MagicMock openai client whose chat.completions.parse is AsyncMock, MagicMock(spec=StorageBackend) storage with storage.save returning "fake-uuid/photo.jpg", and a real Settings() (env vars are already set by conftest env_setup).
    - Define pytest_asyncio fixture debug_client that overrides get_settings to return a Settings with debug=True, then calls app.main.create_app() to obtain a debug-enabled app, wraps in httpx.AsyncClient(transport=ASGITransport(app=app)) — pattern from test_health.py lines 15-28.
    - Define pytest_asyncio fixture nodebug_client that does the inverse (settings.debug=False), confirms /extraction/test → 404.
    - Implement all behavior tests listed above. Mark async tests with @pytest.mark.asyncio (asyncio_mode = "auto" in pyproject.toml is already set but explicit marker is allowed).
    - Test for caplog: bind structlog to standard logging via structlog.testing.capture_logs context manager; assert any({"event": "extraction.failed", "filename": "photo.jpg"}.items() <= entry.items() for entry in cap_logs).

    Modify backend/pyproject.toml [tool.pytest.ini_options]: add key `markers = ["integration: marks tests as live API integration tests (deselect with '-m not integration')"]` — keep existing asyncio_mode and testpaths.

    DO NOT create app.services.storage or app.services.extraction yet — they are Tasks 2 & 3. This task expects test_storage and test_extraction collection to fail with ImportError, which is the explicit Wave-0 RED step.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_storage.py tests/test_extraction.py --collect-only 2>&1 | grep -E "(ImportError|errors during collection|ModuleNotFoundError)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_storage.py exists with at least 4 test functions matching names listed in <behavior>.
    - File backend/tests/test_extraction.py exists with at least 12 test functions matching names listed in <behavior>.
    - backend/pyproject.toml contains the literal substring `integration: marks tests as live API integration tests`.
    - `cd backend && python -m pytest --collect-only tests/test_storage.py tests/test_extraction.py 2>&1` reports collection errors referencing missing module `app.services.storage` or `app.services.extraction` (RED state confirmed).
    - `cd backend && python -m pytest -m "not integration" --collect-only 2>&1 | grep -c "integration"` returns 0 (marker recognized; no warning about unknown marker).
    - No existing test from Phase 1 (test_config.py, test_db.py, test_health.py, test_extraction_models.py) has been modified.
  </acceptance_criteria>
  <done>RED state established: test files committed, both fail with ImportError on missing services modules; pytest marker `integration` registered without warnings; Phase 1 tests untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StorageBackend Protocol + LocalStorageBackend with path-traversal sanitization</name>
  <files>backend/app/services/storage.py, backend/app/config.py</files>
  <read_first>
    backend/app/config.py (current Settings — add one field; preserve lru_cache and env_file behavior)
    backend/app/db/session.py (provider/dependency style for service-layer code)
    .planning/phases/02-extraction-pipeline/02-PATTERNS.md (storage.py section — Protocol + LocalStorageBackend skeleton)
    .planning/phases/02-extraction-pipeline/02-RESEARCH.md (Pattern 2 Protocol; Security Domain path-traversal mitigation block)
    backend/tests/test_storage.py (the tests this task must satisfy — written in Task 1)
  </read_first>
  <action>
    Create backend/app/services/storage.py with:
    - Module docstring describing the StorageBackend Protocol and LocalStorageBackend implementation, citing D-07/D-08/D-09 and the path-traversal mitigation per T-02-01.
    - Imports: from __future__ import annotations; import os; from typing import Protocol, runtime_checkable.
    - @runtime_checkable class StorageBackend(Protocol) with one method `save(self, data: bytes, filename: str) -> str` (ellipsis body and one-line docstring "Save data to storage. Returns relative path.").
    - class LocalStorageBackend with constructor `__init__(self, root: str) -> None` storing self._root, and method `save(self, data: bytes, filename: str) -> str` that:
        1. Splits filename into directory_part, basename via os.path.split.
        2. Sanitizes the basename portion: `safe_basename = os.path.basename(filename)` (strips traversal segments).
        3. If filename contains a directory segment (e.g. "{uuid}/photo.jpg"), reconstruct as `relative_path = os.path.join(os.path.basename(directory_part), safe_basename)` so the {uuid} prefix is preserved but any "../" inside it is stripped. If directory_part is empty, relative_path = safe_basename.
        4. full_path = os.path.join(self._root, relative_path).
        5. Verify `os.path.commonpath([os.path.realpath(full_path), os.path.realpath(self._root)]) == os.path.realpath(self._root)` and raise ValueError("path escapes storage root") if not.
        6. os.makedirs(os.path.dirname(full_path), exist_ok=True) — only when dirname is non-empty.
        7. open(full_path, "wb").write(data) inside `with` block.
        8. return relative_path.

    Modify backend/app/config.py: add `storage_path: str = "/data/invoices"` to Settings, immediately after the `confidence_threshold: float = 0.85` line. Preserve all other lines verbatim (model_config, required fields, debug, log_level, get_settings lru_cache).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_storage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File backend/app/services/storage.py exists; `grep -c "class StorageBackend" backend/app/services/storage.py` returns 1.
    - `grep -c "class LocalStorageBackend" backend/app/services/storage.py` returns 1.
    - `grep -c "@runtime_checkable" backend/app/services/storage.py` returns 1.
    - `grep -c "os.path.basename" backend/app/services/storage.py` returns at least 1 (mitigation present).
    - `grep -c "storage_path" backend/app/config.py` returns at least 1.
    - `cd backend && python -m pytest tests/test_storage.py -x -q` exits 0 with all 4 storage tests passing.
    - `cd backend && python -m pytest tests/test_config.py -x -q` exits 0 (regression: Phase 1 config tests still pass after the new field is added).
    - Behavior: invoking `LocalStorageBackend("/tmp/sb").save(b"x", "../../etc/passwd")` writes a file at `/tmp/sb/passwd` only (no `/tmp/sb/etc/` or `/tmp/etc/` created).
  </acceptance_criteria>
  <done>Storage tests GREEN. LocalStorageBackend writes bytes safely; Settings exposes storage_path with default '/data/invoices'.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ExtractionService skeleton + ExtractionResult DTO + debug-gated router</name>
  <files>backend/app/services/extraction.py, backend/app/routers/extraction.py, backend/app/main.py</files>
  <read_first>
    backend/app/routers/health.py (router + Depends pattern — copy import + endpoint style)
    backend/app/main.py (current create_app() factory — insertion point for conditional include_router)
    backend/app/config.py (Settings + get_settings — DI source)
    backend/app/models/extraction.py (ExtractedInvoice contract — DO NOT modify)
    backend/app/services/storage.py (StorageBackend + LocalStorageBackend — produced in Task 2)
    backend/tests/test_extraction.py (the test surface this task must satisfy — written in Task 1)
    .planning/phases/02-extraction-pipeline/02-PATTERNS.md (extraction.py service section: ExtractionService skeleton, compute_confidence, assign_status; routers/extraction.py section)
    .planning/phases/02-extraction-pipeline/02-RESEARCH.md (Pattern 1 GPT-4o call shape; Pattern 4 constructor injection; Pattern 5 mock shape this code must satisfy)
  </read_first>
  <action>
    Create backend/app/services/extraction.py with:
    - Module docstring "ExtractionService — GPT-4o vision extraction skeleton (real prompt + error semantics enriched in Plan 02). Citations: D-01 confidence formula; D-03 status thresholds; D-04 interface; T-02-02 OpenAI key not logged."
    - Imports: from __future__ import annotations; import base64; import os; import uuid; from typing import Optional, Tuple; import structlog; from openai import AsyncOpenAI; from pydantic import BaseModel; from app.config import Settings; from app.models.extraction import ExtractedInvoice; from app.services.storage import StorageBackend.
    - Module-level: log = structlog.get_logger() — only used at module/import scope where allowed.
    - class ExtractionError(Exception): pass; class ExtractionRefusalError(ExtractionError): pass; class ExtractionFailedError(ExtractionError): pass.
    - class ExtractionResult(BaseModel): invoice: ExtractedInvoice; confidence_score: float; status: str; image_path: str.
    - def compute_confidence(invoice: ExtractedInvoice) -> float: critical = [invoice.tipo_comprobante, invoice.numero_documento, invoice.proveedor, invoice.fecha]; return sum(1.0 for f in critical if f is not None) / 4.0.
    - def assign_status(score: float, threshold: float) -> str: return "auto_saved" if score >= threshold else "pending_review".
    - class ExtractionService:
        __init__(self, openai_client: AsyncOpenAI, storage: StorageBackend, settings: Settings) -> None storing the three deps and self._log = structlog.get_logger().
        SYSTEM_PROMPT class attribute: minimal placeholder string "You are an invoice extraction system. Extract the invoice fields exactly as visible. Return null for any field that is not present or is ambiguous." (Plan 02 may iterate; the placeholder is intentional per D-10.)
        async def _call_gpt4o(self, image_bytes: bytes) -> Tuple[Optional[ExtractedInvoice], Optional[str]]: implements the verbatim Pattern 1 call against self._client with model="gpt-4o", response_format=ExtractedInvoice, image_url content type `f"data:image/jpeg;base64,{b64}"`. Returns (msg.parsed, msg.refusal).
        async def extract(self, image_bytes: bytes, filename: str) -> ExtractionResult:
          - log = self._log.bind(filename=os.path.basename(filename)) — bind at call time only.
          - log.info("extraction.start")
          - try:
              invoice_uuid = str(uuid.uuid4())
              safe_basename = os.path.basename(filename)
              relative_path = self._storage.save(image_bytes, f"{invoice_uuid}/{safe_basename}")
              parsed, refusal = await self._call_gpt4o(image_bytes)
              if refusal is not None: log.error("extraction.failed", error="refusal", refusal=refusal); raise ExtractionRefusalError(refusal)
              if parsed is None: log.error("extraction.failed", error="parsed_none"); raise ExtractionFailedError("GPT-4o returned no parsed content and no refusal")
              score = compute_confidence(parsed)
              status = assign_status(score, self._settings.confidence_threshold)
              log.info("extraction.complete", confidence=score, status=status, image_path=relative_path)
              return ExtractionResult(invoice=parsed, confidence_score=score, status=status, image_path=relative_path)
            except ExtractionError: raise
            except Exception as exc:
              log.error("extraction.failed", error=str(exc))   # NEVER include settings.openai_api_key (T-02-02)
              raise ExtractionFailedError(str(exc)) from exc

    Create backend/app/routers/extraction.py with:
    - Module docstring referencing D-05 (debug-only) and D-06 (no DB write).
    - Imports: from fastapi import APIRouter, Depends, UploadFile, File; from openai import AsyncOpenAI; from app.config import Settings, get_settings; from app.services.extraction import ExtractionService, ExtractionResult; from app.services.storage import LocalStorageBackend.
    - router = APIRouter().
    - def get_extraction_service(settings: Settings = Depends(get_settings)) -> ExtractionService:
        openai_client = AsyncOpenAI(api_key=settings.openai_api_key)
        storage = LocalStorageBackend(root=settings.storage_path)
        return ExtractionService(openai_client=openai_client, storage=storage, settings=settings)
    - @router.post("/test", response_model=ExtractionResult) async def extraction_test(file: UploadFile = File(...), service: ExtractionService = Depends(get_extraction_service)) -> ExtractionResult: data = await file.read(); return await service.extract(image_bytes=data, filename=file.filename or "upload.bin").

    Modify backend/app/main.py: inside create_app(), after the existing `app.include_router(health_router)` line, insert:
        if settings.debug:
            from app.routers.extraction import router as extraction_router
            app.include_router(extraction_router, prefix="/extraction", tags=["extraction"])
    Preserve all other lines (lifespan, log import, factory signature, `app = create_app()` at bottom).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_extraction.py tests/test_storage.py tests/test_health.py tests/test_config.py tests/test_db.py tests/test_extraction_models.py -x -q -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class ExtractionService" backend/app/services/extraction.py` returns 1.
    - `grep -c "class ExtractionResult" backend/app/services/extraction.py` returns 1.
    - `grep -c "compute_confidence" backend/app/services/extraction.py` returns at least 2 (definition + invocation).
    - `grep -c "assign_status" backend/app/services/extraction.py` returns at least 2.
    - `grep -c "ExtractionRefusalError\|ExtractionFailedError" backend/app/services/extraction.py` returns at least 4.
    - `grep -v '^#' backend/app/services/extraction.py | grep -c "openai_api_key"` returns 0 (key never referenced in service code; mitigates T-02-02).
    - `grep -c "if settings.debug" backend/app/main.py` returns at least 1.
    - `grep -c "@router.post(\"/test\"" backend/app/routers/extraction.py` returns 1.
    - `cd backend && python -m pytest -m "not integration" -x -q` exits 0 with all Phase 1 tests + all Phase 2 mocked tests green.
    - Behavior: hitting `/extraction/test` with a multipart file upload and DEBUG=true returns a JSON object whose keys are exactly {"invoice","confidence_score","status","image_path"} (verified by ASGI client test).
    - Behavior: with DEBUG=false the same POST returns HTTP 404.
  </acceptance_criteria>
  <done>ExtractionService + ExtractionResult + debug router land green. End-to-end mocked slice: POST /extraction/test → ExtractionService.extract → StorageBackend.save → mocked GPT-4o → ExtractionResult JSON. All Phase 1 tests still pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → POST /extraction/test | Untrusted multipart filename + image bytes cross into the service layer. Endpoint is debug-gated but still treated as untrusted input. |
| ExtractionService → LocalStorageBackend.save | Filename is forwarded from external input; must be path-safe before touching disk. |
| ExtractionService → OpenAI API | Outbound call carrying the API key — key MUST stay in settings/AsyncOpenAI client, never in logs or HTTP responses. |
| Settings → app code | settings.openai_api_key, whatsapp_token, whatsapp_verify_token — secrets that must never be emitted by structlog. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | LocalStorageBackend.save (storage.py) | mitigate | Sanitize via `os.path.basename()` on both directory_part and basename portions of the relative path. Verify `os.path.commonpath([realpath(full_path), realpath(root)]) == realpath(root)` and raise ValueError otherwise. Unit test `test_save_sanitizes_path_traversal` exercises `../../etc/passwd` and `uuid/../../bad`. |
| T-02-02 | Information Disclosure | ExtractionService logging (extraction.py) | mitigate | structlog `.bind(filename=...)` never includes `settings.openai_api_key`. Service never imports/passes the raw key; AsyncOpenAI client constructed in router DI receives the key once and the service holds only the client. Acceptance grep asserts `openai_api_key` never appears in services/extraction.py. |
| T-02-03 | Elevation of Privilege | POST /extraction/test exposed in production | mitigate | Router conditionally included in `create_app()` ONLY when `settings.debug` is True (D-05). Production .env keeps DEBUG=false. Test `test_post_extraction_test_404_when_debug_false` asserts the route is absent. |
| T-02-04 | Denial of Service | Unbounded UploadFile size on /extraction/test | accept | Debug-only endpoint; Phase 3 will add size limits at the WhatsApp ingress layer. Acceptable risk for dev-only surface. |
| T-02-05 | Tampering | Prompt-injection text embedded in invoice image | mitigate | OpenAI Structured Outputs constrains the response to ExtractedInvoice JSON Schema; the model cannot return free-form fields. Refusals surface via `message.refusal` and raise ExtractionRefusalError (caught by service; not echoed to client). |

</threat_model>

<verification>
- `cd backend && python -m pytest -m "not integration" -x -q` → 0 failures, all Phase 1 + Phase 2 mocked tests green.
- `cd backend && grep -v '^#' app/services/extraction.py | grep -c "openai_api_key"` → 0.
- `cd backend && python -c "from app.services.storage import StorageBackend, LocalStorageBackend; from app.services.extraction import ExtractionService, ExtractionResult, ExtractionRefusalError, ExtractionFailedError, compute_confidence, assign_status; print('ok')"` → "ok".
- `cd backend && python -c "from app.config import get_settings; s = get_settings(); assert hasattr(s, 'storage_path') and s.storage_path == '/data/invoices'; print('ok')"` → "ok".
- `cd backend && DEBUG=true python -c "from app.main import create_app; app = create_app(); paths = [r.path for r in app.routes]; assert '/extraction/test' in paths, paths; print('ok')"` → "ok".
- `cd backend && DEBUG=false python -c "from app.main import create_app; app = create_app(); paths = [r.path for r in app.routes]; assert '/extraction/test' not in paths, paths; print('ok')"` → "ok".
</verification>

<success_criteria>
1. End-to-end mocked vertical slice works: POST /extraction/test with multipart file (DEBUG=true) → 200 → JSON body keys {invoice, confidence_score, status, image_path}.
2. Confidence score formula matches D-01 exactly (3 unit tests cover edge cases).
3. Status assignment matches D-03 thresholds (2 unit tests cover boundary and below).
4. Path traversal in filename cannot escape storage root (T-02-01 mitigation test green).
5. OpenAI API key is never referenced in services/extraction.py (T-02-02 grep gate green).
6. /extraction/test is not registered when DEBUG=false (T-02-03 mitigation test green).
7. All Phase 1 tests still pass; no regressions in test_config, test_db, test_health, test_extraction_models.
</success_criteria>

<output>
After completion, create `.planning/phases/02-extraction-pipeline/02-01-SUMMARY.md` with: artifacts list, RED→GREEN log per task, test command output, and any deviations from this PLAN.
</output>
