---
phase: 01-data-conversation-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/models.py
  - backend/alembic/versions/c3d4e5f6a7b8_add_gastos_tables.py
  - backend/app/config.py
  - backend/app/main.py
  - backend/tests/conftest.py
  - backend/tests/test_gastos_models.py
autonomous: true
requirements: [CONV-01]
must_haves:
  truths:
    - "A conversations row persists per sender_phone and survives process restarts (DB-backed state)"
    - "A gastos table exists to hold committed expense records (fecha, concepto, monto, optional ticket path)"
    - "A caja_cierres table exists for Phase 2 cash-closing writes"
    - "AGENT_MODE defaults to gastos and CONVERSATION_TIMEOUT_HOURS defaults to 4"
    - "The invoice/whatsapp webhook router is registered only when AGENT_MODE == invoice"
  artifacts:
    - path: "backend/app/db/models.py"
      provides: "Gasto, Conversation, CajaCierre ORM models"
      contains: "class Conversation"
    - path: "backend/alembic/versions/c3d4e5f6a7b8_add_gastos_tables.py"
      provides: "Single migration creating all three tables, down_revision b1c2d3e4f5a6"
      contains: "b1c2d3e4f5a6"
    - path: "backend/app/config.py"
      provides: "agent_mode + conversation_timeout_hours settings"
      contains: "agent_mode"
  key_links:
    - from: "backend/app/main.py"
      to: "settings.agent_mode"
      via: "conditional router registration"
      pattern: "agent_mode"
---

<objective>
Build the data foundation for the Gastos Bot: three ORM models (Gasto, Conversation, CajaCierre), a single Alembic migration creating all three tables, the two new config settings (AGENT_MODE, CONVERSATION_TIMEOUT_HOURS), the conditional webhook registration seam in main.py (D-09), and the conftest env wiring all later tests depend on.

Purpose: Every other Phase 1 plan (slot extraction, gasto service, orchestrator) needs these tables, models, and settings to exist. This is the shared foundation — it runs alone in Wave 1 so Waves 2-3 can run in parallel against a stable schema.

Output: Extended models.py, new migration file, extended config.py + main.py, extended conftest.py, and a models smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-conversation-core/01-CONTEXT.md
@.planning/phases/01-data-conversation-core/01-RESEARCH.md
@.planning/phases/01-data-conversation-core/01-PATTERNS.md

<interfaces>
<!-- Conventions to mirror. Read the analog files in each task's read_first. -->
Existing models live in backend/app/db/models.py and extend the shared `Base` (DeclarativeBase) — DO NOT create a new Base, import and extend the existing one.
Dialect-agnostic types only: `sqlalchemy.Uuid` (NOT postgresql.UUID), `Numeric`, `Date`, `DateTime(timezone=True)`, `Text`, `String`, `Index`.
Timestamp convention: `server_default=func.now()`; updated_at additionally uses `onupdate=func.now()` (the D-08 timeout anchor).
Current Alembic head is `b1c2d3e4f5a6` (add_invoice_duplicate_constraint.py). Chain: 0cd640399c29 → 9f9e9cf65e1e → b1c2d3e4f5a6 (HEAD).
Settings live in backend/app/config.py as a pydantic-settings `Settings` class with `@lru_cache get_settings()`; env names case-insensitive.
create_app() in backend/app/main.py imports routers INSIDE the factory; currently registers whatsapp router unconditionally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gasto, Conversation, CajaCierre ORM models</name>
  <read_first>
    - backend/app/db/models.py (the file being extended; copy Invoice/SenderAllowlist conventions, the imports block, PK + timestamp pattern, __table_args__ index pattern)
    - .planning/phases/01-data-conversation-core/01-RESEARCH.md Pattern 4 (exact column definitions for all three models)
    - .planning/phases/01-data-conversation-core/01-PATTERNS.md section "backend/app/db/models.py" (line-referenced conventions)
  </read_first>
  <action>
    Extend backend/app/db/models.py with three classes that subclass the existing `Base` (do not redefine Base):
    - `Conversation` (`__tablename__ = "conversations"`): `sender_phone` String(30) primary_key; `state` String(30) not null default "idle"; `draft_gasto` Text nullable (JSON dump of DraftGasto); `last_message_id` String(100) nullable (CONV-02 idempotency key); `updated_at` DateTime(timezone=True) with `server_default=func.now()` AND `onupdate=func.now()` (the D-08 timeout anchor — must have onupdate). Add an Index on sender_phone via `__table_args__`.
    - `Gasto` (`__tablename__ = "gastos"`): `id` Uuid primary_key default uuid.uuid4; `fecha` Date not null; `concepto` Text not null; `monto` Numeric(14, 2) not null (ARS pesos, the salida per D-01); `ticket_image_path` Text nullable (populated in Phase 2, GASTO-04 allows None); `sender_phone` String(30) not null; `created_at` DateTime(timezone=True) server_default func.now(). Add indexes `ix_gastos_fecha` and `ix_gastos_sender_phone`.
    - `CajaCierre` (`__tablename__ = "caja_cierres"`): `id` Uuid primary_key default uuid.uuid4; `fecha` Date not null; `hora_cierre` String(5) not null ("12:00" | "17:00"); `efectivo_en_caja` Numeric(14, 2) not null; `sender_phone` String(30) not null; `created_at` DateTime(timezone=True) server_default func.now(). Add index `ix_caja_cierres_fecha`. (Created here per RESEARCH recommendation — one migration, no schema debt; reactive write is Phase 2.)
    Follow D-01 strictly: NO lugar/proveedor, NO entrada, NO category column on Gasto. Use only dialect-agnostic types so aiosqlite tests work.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.db.models import Gasto, Conversation, CajaCierre; print(Conversation.__tablename__, Gasto.__tablename__, CajaCierre.__tablename__)"</automated>
  </verify>
  <acceptance_criteria>
    - Importing Gasto, Conversation, CajaCierre from app.db.models succeeds with no error.
    - `Conversation` has columns sender_phone (PK), state, draft_gasto, last_message_id, updated_at; updated_at has onupdate=func.now().
    - `Gasto` has columns id, fecha, concepto, monto (Numeric), ticket_image_path (nullable), sender_phone, created_at; NO lugar/proveedor/entrada/category column.
    - `CajaCierre` has columns id, fecha, hora_cierre, efectivo_en_caja, sender_phone, created_at.
    - All UUID columns use `sqlalchemy.Uuid`, not postgresql.UUID.
  </acceptance_criteria>
  <done>Three new ORM classes added to models.py, importable, matching D-01 minimal field set and existing dialect-agnostic conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Add config settings + conditional router seam + conftest env</name>
  <read_first>
    - backend/app/config.py (the Settings class + lru_cache get_settings; add to the optional-with-defaults block)
    - backend/app/main.py (create_app() factory; routers imported inside the factory)
    - backend/tests/conftest.py (the session-scoped env_setup fixture that sets env + calls get_settings.cache_clear())
    - .planning/phases/01-data-conversation-core/01-RESEARCH.md Pattern 8 (config + main.py snippets) and Pitfall G (conftest cache)
  </read_first>
  <action>
    1. In backend/app/config.py `Settings`: add `agent_mode: str = "gastos"` (values "invoice" | "gastos", default "gastos" per D-09) and `conversation_timeout_hours: int = 4` (D-08). Both go in the optional-with-defaults block; no validator needed (defaults keep existing invoice deployments unaffected). Leave the `@lru_cache get_settings()` untouched.
    2. In backend/app/main.py `create_app()`: make the existing invoice/whatsapp router registration conditional — `if settings.agent_mode == "invoice":` import and include the whatsapp router. Leave an `elif settings.agent_mode == "gastos":` comment noting the gastos webhook is wired in Phase 2 (do NOT register a gastos router here — it does not exist yet). This is the demo-isolation seam (D-09).
    3. In backend/tests/conftest.py: add `mp.setenv("AGENT_MODE", "gastos")` to the existing session-scoped env_setup fixture (alongside the other setenv calls, before get_settings.cache_clear()). This ensures all later tests construct Settings with the new default. Do not add CONVERSATION_TIMEOUT_HOURS (its int default of 4 is sufficient for tests).
    Threat T-01-04: agent_mode is read once at app construction and only gates which router mounts; there is no runtime flip path. Document this in a code comment near the branch.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_config.py -q && python -c "from app.config import get_settings; get_settings.cache_clear(); s=get_settings(); assert s.agent_mode=='gastos'; assert s.conversation_timeout_hours==4; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `get_settings().agent_mode == "gastos"` and `get_settings().conversation_timeout_hours == 4` with no env vars set.
    - main.py create_app() registers the whatsapp router only inside `if settings.agent_mode == "invoice":`.
    - conftest env_setup sets AGENT_MODE=gastos before get_settings.cache_clear().
    - Existing test_config.py still passes.
  </acceptance_criteria>
  <done>Config gains agent_mode + conversation_timeout_hours; main.py gates the invoice router on agent_mode; conftest sets AGENT_MODE.</done>
</task>

<task type="auto">
  <name>Task 3: Create the gastos schema Alembic migration + models smoke test</name>
  <read_first>
    - backend/alembic/versions/0cd640399c29_initial_schema.py (op.create_table + create_index idiom, server_default=sa.text('now()'), reverse downgrade)
    - backend/alembic/versions/add_invoice_duplicate_constraint.py (header block + reversible up/down)
    - backend/app/db/models.py (the models from Task 1 — migration must match them exactly)
    - backend/tests/conftest.py (async_engine + db_session aiosqlite fixtures the smoke test reuses)
    - .planning/phases/01-data-conversation-core/01-RESEARCH.md Pattern 9 (migration conventions + RLS) and 01-PATTERNS.md "Migration head"
  </read_first>
  <action>
    Create backend/alembic/versions/c3d4e5f6a7b8_add_gastos_tables.py with `revision = 'c3d4e5f6a7b8'` and `down_revision = 'b1c2d3e4f5a6'` (the current head). `upgrade()` creates all three tables matching the Task 1 models exactly: `conversations` (sender_phone String(30) PK, state String(30) not null, draft_gasto Text, last_message_id String(100), updated_at DateTime(timezone=True) server_default sa.text('now()')), `gastos` (id Uuid PK, fecha Date not null, concepto Text not null, monto Numeric(14,2) not null, ticket_image_path Text, sender_phone String(30) not null, created_at server_default now()), `caja_cierres` (id Uuid PK, fecha Date not null, hora_cierre String(5) not null, efectivo_en_caja Numeric(14,2) not null, sender_phone String(30) not null, created_at server_default now()). Create the indexes ix_gastos_fecha, ix_gastos_sender_phone, ix_caja_cierres_fecha, and the sender_phone index on conversations. After each create_table, add `op.execute("ALTER TABLE <name> ENABLE ROW LEVEL SECURITY")` (Pitfall 3 / RLS requirement). `downgrade()` drops indexes then tables in reverse order.
    Then add backend/tests/test_gastos_models.py: a single async test using the db_session fixture that inserts one Conversation, one Gasto (fecha=date.today(), monto Decimal), and one CajaCierre row, commits, re-queries each, and asserts the round-tripped values match (proves CONV-01 persistence and that the models map cleanly onto a created schema). Use the existing conftest async_engine which creates tables via Base.metadata — confirm the new tables are created by the fixture (Base.metadata.create_all picks them up automatically since they subclass Base).
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_gastos_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Migration file declares `down_revision = 'b1c2d3e4f5a6'`.
    - `cd backend && python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; s=ScriptDirectory.from_config(Config('alembic.ini')); print([h for h in s.get_heads()])"` shows a single head (the new revision) — chain is linear, not branched.
    - test_gastos_models.py inserts and re-reads a Conversation, Gasto, and CajaCierre row; all asserted values round-trip.
    - Each upgrade() create_table is followed by an ENABLE ROW LEVEL SECURITY statement; downgrade() drops all three tables.
  </acceptance_criteria>
  <done>One migration creates all three tables (chained to b1c2d3e4f5a6, RLS enabled, reversible); a smoke test proves persistence round-trips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → app config | AGENT_MODE / CONVERSATION_TIMEOUT_HOURS read at startup; gate which agent is live |
| ORM → Postgres | New tables hold money values and conversation state; RLS must be enabled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation of Privilege | AGENT_MODE selecting live agent | mitigate | agent_mode read once at create_app(); only gates router mount; no runtime flip path; default "gastos" so misconfig fails closed to the intended demo agent |
| T-01-RLS | Information Disclosure | new tables (gastos, conversations, caja_cierres) | mitigate | ENABLE ROW LEVEL SECURITY on all three tables in the migration (Pitfall 3) |
| T-01-SC | Tampering | npm/pip installs | accept | No new packages introduced this phase (RESEARCH Package Legitimacy Audit: none); no install task exists |
</threat_model>

<verification>
- `cd backend && pytest tests/test_gastos_models.py tests/test_config.py -q` is green.
- Alembic head is single and linear (no branch).
- Models import cleanly; D-01 minimal field set respected (no lugar/proveedor/entrada/category).
</verification>

<success_criteria>
- Three ORM models exist and round-trip through the aiosqlite test DB (supports ROADMAP success criterion 1 prerequisite: persistent conversation state).
- AGENT_MODE + CONVERSATION_TIMEOUT_HOURS configured with correct defaults.
- main.py gates the invoice router on AGENT_MODE (D-09 demo isolation seam).
- Single reversible migration chained to b1c2d3e4f5a6 with RLS enabled.
</success_criteria>

<output>
Create `.planning/phases/01-data-conversation-core/01-01-SUMMARY.md` when done.
</output>
