---
phase: 01-data-conversation-core
plan: 03
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/app/services/gasto.py
  - backend/tests/test_gasto_service.py
autonomous: true
requirements: [GASTO-05]
must_haves:
  truths:
    - "GastoService.save_gasto(session, draft, sender_phone) inserts one gastos row with fecha=today, concepto, monto, optional ticket path"
    - "save_gasto strips the whatsapp: prefix from sender_phone before persisting"
    - "save_gasto does not open or commit its own session — the caller (orchestrator) owns the transaction; it flushes to populate the id"
  artifacts:
    - path: "backend/app/services/gasto.py"
      provides: "GastoService persistence (mirrors InvoiceService)"
      contains: "class GastoService"
    - path: "backend/tests/test_gasto_service.py"
      provides: "save_gasto persistence tests against aiosqlite"
      contains: "save_gasto"
  key_links:
    - from: "backend/app/services/gasto.py"
      to: "Gasto ORM model"
      via: "session.add(Gasto(...))"
      pattern: "session.add"
---

<objective>
Build the gasto persistence layer: a stateless GastoService mirroring InvoiceService that takes a session + DraftGasto and writes a committed gastos row. This is the write side of the confirmation gate (GASTO-05) — it is invoked by the orchestrator (Plan 04) only after explicit confirmation.

Purpose: Independent of the slot-extraction layer (Plan 02), so it runs in parallel in Wave 2. It depends only on the Gasto model from Plan 01.

Output: New gasto service + persistence test. TDD plan — failing test first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-conversation-core/01-CONTEXT.md
@.planning/phases/01-data-conversation-core/01-RESEARCH.md
@.planning/phases/01-data-conversation-core/01-PATTERNS.md

<interfaces>
<!-- Mirror these existing patterns. Read the analog files in each task's read_first. -->
InvoiceService in backend/app/services/invoice.py: stateless (no session held), `__init__(self) -> None` sets `self._log = structlog.get_logger()`; methods take `session: AsyncSession` as the first arg; builds the ORM object, session.add(...), and logs `<entity>.saved` with id=str(...). Strips the "whatsapp:" prefix from sender. Has a try/except IntegrityError → rollback → re-raise pattern for write races.
DraftGasto (from Plan 02, backend/app/models/conversation.py): concepto: Optional[str], monto: Optional[Decimal], ticket_image_path: Optional[str], failure_count: int.
Gasto ORM model (from Plan 01): fecha (Date), concepto (Text), monto (Numeric), ticket_image_path (Text nullable), sender_phone (String), created_at.
test_invoice_service.py: uses the db_session aiosqlite fixture + DTO factory helpers; monkeypatches session.commit to raise IntegrityError to assert the catch path (Postgres-only constructs are not exercised by SQLite).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GastoService.save_gasto (TDD)</name>
  <files>backend/app/services/gasto.py, backend/tests/test_gasto_service.py</files>
  <read_first>
    - backend/app/services/invoice.py (InvoiceService — stateless service, session-first method, build/add/log, whatsapp: prefix strip, IntegrityError handling)
    - backend/tests/test_invoice_service.py (db_session usage, DTO factory pattern, monkeypatch commit pattern)
    - backend/app/models/conversation.py (DraftGasto — built in Plan 02; if not yet present, define a minimal local DraftGasto-shaped input or import once available — Plan 02 is same wave, prefer importing)
    - .planning/phases/01-data-conversation-core/01-RESEARCH.md Pattern 7 (GastoService spec + commit-ownership note)
  </read_first>
  <behavior>
    - save_gasto(session, draft, sender_phone="whatsapp:+5491122334455") inserts exactly one row into gastos.
    - The inserted row has fecha == date.today() (D-02 — fecha auto, no backdating), concepto == draft.concepto, monto == draft.monto, ticket_image_path == draft.ticket_image_path, sender_phone == "+5491122334455" (whatsapp: prefix stripped, trimmed).
    - save_gasto returns the Gasto with a populated id (flushed) but does NOT commit — after the call, before the caller commits, the row is visible within the same session/transaction.
    - With ticket_image_path=None on the draft, the saved row has ticket_image_path None (GASTO-04 supports skipping ticket — persistence side).
  </behavior>
  <action>
    First write backend/tests/test_gasto_service.py using the db_session aiosqlite fixture and a small DraftGasto factory; encode the behavior cases above (RED). Then implement backend/app/services/gasto.py: `GastoService` with `__init__(self) -> None` setting `self._log = structlog.get_logger()` (stateless, mirrors InvoiceService). Implement `async def save_gasto(self, session: AsyncSession, draft: DraftGasto, sender_phone: str) -> Gasto`: build `Gasto(fecha=date.today(), concepto=draft.concepto, monto=draft.monto, ticket_image_path=draft.ticket_image_path, sender_phone=sender_phone.replace("whatsapp:", "").strip())`, `session.add(gasto)`, `await session.flush()` (to populate id), log `gasto.saved` with id=str(gasto.id) and monto=str(draft.monto). DO NOT call session.commit() — the orchestrator owns the transaction (RESEARCH Pattern 7 commit-ownership note). The test commits explicitly to verify persistence.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_gasto_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - One gastos row inserted with fecha=date.today(), correct concepto/monto/ticket_image_path, sender_phone with whatsapp: prefix stripped.
    - save_gasto returns a Gasto with a non-None id without having committed (no commit call inside save_gasto).
    - ticket_image_path=None on the draft yields a row with ticket_image_path None.
    - GastoService holds no session state (stateless).
  </acceptance_criteria>
  <done>GastoService.save_gasto implemented and green; mirrors InvoiceService, owns no transaction, defaults fecha to today.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DraftGasto → gastos row | A confirmed draft becomes a permanent money record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | monto written to gastos | mitigate | save_gasto is invoked only after the orchestrator's deterministic confirm gate (Plan 04); monto carried as Decimal; no implicit save path exists |
| T-03-02 | Repudiation | gasto write auditability | mitigate | structlog gasto.saved logged with id + monto for an audit trail |
| T-03-SC | Tampering | pip installs | accept | No new packages (RESEARCH Package Legitimacy Audit: none); no install task |
</threat_model>

<verification>
- `cd backend && pytest tests/test_gasto_service.py -q` green.
- save_gasto does not commit (grep gasto.py shows no session.commit in save_gasto).
</verification>

<success_criteria>
- A confirmed DraftGasto becomes a committed gastos row with fecha=today and stripped sender (GASTO-05 persistence side).
- Service is stateless and transaction-agnostic (orchestrator owns commit).
</success_criteria>

<output>
Create `.planning/phases/01-data-conversation-core/01-03-SUMMARY.md` when done.
</output>
