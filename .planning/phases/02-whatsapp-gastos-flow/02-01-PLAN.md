---
phase: 02-whatsapp-gastos-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/ticket_vision.py
  - backend/app/services/conversation.py
  - backend/tests/test_ticket_vision.py
  - backend/tests/test_conversation.py
autonomous: true
requirements: [GASTO-03]

must_haves:
  truths:
    - "After concepto is known, the bot asks for a ticket photo BEFORE asking for an amount (ticket-first, D-01)"
    - "A ticket photo's amount is read by GPT-4o vision and becomes the gasto monto (D-02)"
    - "Replying 'sin ticket' skips storage and routes to awaiting_monto where the bot asks the manager to type the amount (D-01, GASTO-04)"
    - "When vision cannot read an amount, the bot falls back to asking the manager to type it, reusing the 3-strikes re-prompt (D-01b)"
    - "An off-topic idle message that yields no gasto intent gets a fixed Spanish deflection reply and leaves state at idle (D-04)"
    - "The confirm summary shows the resolved amount so the manager can correct it freeform (D-01a)"
  artifacts:
    - path: "backend/app/services/ticket_vision.py"
      provides: "Amount-only GPT-4o vision extractor (TicketAmount Pydantic model + service)"
      contains: "response_format="
    - path: "backend/app/services/conversation.py"
      provides: "Ticket-first reworked FSM with media-carrying entry path"
      contains: "ticket_amount"
  key_links:
    - from: "backend/app/services/conversation.py"
      to: "DraftGasto.ticket_image_path / DraftGasto.monto"
      via: "handle_message media params set draft fields before CONFIRM"
      pattern: "ticket_image_path"
    - from: "backend/app/services/ticket_vision.py"
      to: "openai chat.completions.parse"
      via: "amount-only vision call"
      pattern: "chat\\.completions\\.parse"
---

<objective>
Rework the Phase 1 ConversationOrchestrator into the D-01 ticket-first flow and add the amount-only GPT-4o vision extractor (D-02). The orchestrator's entry path is extended to carry media (D-06 orchestrator side) so a stored ticket image path + a vision-extracted amount can be fed in. Off-topic idle messages get a fixed Spanish deflection (D-04).

Purpose: This is the core conversation-logic change for the phase, independently unit-testable with NO WhatsApp/router/network. The router (Plan 02) consumes the new entry signature + the vision service.
Output: `backend/app/services/ticket_vision.py` (new) + reworked `backend/app/services/conversation.py`, both unit-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-whatsapp-gastos-flow/02-CONTEXT.md
@.planning/phases/02-whatsapp-gastos-flow/02-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — do NOT re-explore. -->

DraftGasto (backend/app/models/conversation.py) — all-Optional draft, stored as JSON in conversations.draft_gasto:
  concepto: Optional[str]
  monto: Optional[Decimal]          # converted from float via Decimal(str(...))
  ticket_image_path: Optional[str]
  failure_count: int = 0

GastoSlots (backend/app/models/conversation.py) — gpt-4o-mini text slot output:
  concepto: Optional[str]; monto: Optional[float]   # JSON number sidesteps Decimal("1.500") trap

SlotExtractionService.extract(text) -> GastoSlots          (slot_extraction.py)
GastoService.save_gasto(session, draft, sender_phone) -> Gasto  (gasto.py:40 — flush only, caller commits)
WhatsAppProvider.send_message(to, text) / download_media(url) (providers/base.py — FIXED interface)

ExtractionService vision .parse() pattern to mirror (extraction.py:159-203):
  b64 = base64.b64encode(image_bytes).decode("utf-8")
  completion = await client.chat.completions.parse(
      model="gpt-4o-2024-08-06",
      messages=[{role:system, content: SYSTEM_PROMPT},
                {role:user, content:[{type:text,...},
                 {type:image_url, image_url:{url:f"data:image/jpeg;base64,{b64}"}}]}],
      response_format=<PydanticModel>)
  msg = completion.choices[0].message; return (msg.parsed, msg.refusal)   # refusal checked BEFORE parsed
  Constructor DI: (openai_client, storage, settings). NEVER log the API key.
  Exception hierarchy: ExtractionRefusalError / ExtractionFailedError (extraction.py:70-79).

Current ConvState (conversation.py:85-89): IDLE / AWAITING_MONTO / AWAITING_TICKET / CONFIRM.
Non-negotiable entry sequence (conversation.py:185-274): ensure-row → lock(FOR NO KEY UPDATE) →
  snapshot updated_at → idempotency(last_message_id) → timeout → cancel → dispatch →
  commit in session.begin() → reply OUTSIDE txn. DO NOT alter this ordering.
Pure helpers to reuse unchanged: is_confirmation, is_cancel, patch_draft, _load_draft, _save_draft
  (reassign-don't-mutate for onupdate), _confirm_summary.
</interfaces>

@backend/app/services/conversation.py
@backend/app/services/extraction.py
@backend/app/models/conversation.py
@backend/app/services/gasto.py
@backend/app/providers/base.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Amount-only GPT-4o vision extractor</name>
  <read_first>
    - backend/app/services/ticket_vision.py (target — will be created; read if it somehow exists)
    - backend/app/services/extraction.py (analog — the _call_gpt4o .parse() pattern at lines 159-203, constructor DI 148-157, exception hierarchy 70-79)
    - backend/app/models/conversation.py (Optional-everything Pydantic convention)
    - backend/tests/test_extraction.py (test pattern: MagicMock client, mocked completion.choices[0].message.parsed/refusal)
  </read_first>
  <behavior>
    - Test: a mocked client returning parsed.monto=1500.0 → service returns Decimal("1500") (converted via Decimal(str(...)), not Decimal(float)).
    - Test: a mocked client returning monto=None (unreadable) → service returns None (no exception, no hallucinated amount).
    - Test: a mocked completion with msg.refusal set → refusal is checked BEFORE parsed; returns None (treated as unreadable, NOT a crash).
    - Test: an openai exception (network/auth) → raises ExtractionFailedError; the API key never appears in the log call.
  </behavior>
  <action>
    Create `backend/app/services/ticket_vision.py`. Define a `TicketAmount(BaseModel)` with `model_config = ConfigDict(use_enum_values=True)` and a single field `monto: Optional[float] = None` (JSON number — null > hallucination, sidesteps the Decimal("1.500") trap per models/conversation.py). Define `TicketVisionService` with constructor DI `(openai_client, settings)` mirroring ExtractionService.__init__ (extraction.py:148-157) — NO storage param here (the router/orchestrator owns storage). Add an async method `extract_amount(image_bytes: bytes) -> Optional[Decimal]` that base64-encodes the bytes and calls `self._client.chat.completions.parse(model="gpt-4o-2024-08-06", ...)` with `response_format=TicketAmount` and an amount-only Spanish system prompt (module-level `SYSTEM_PROMPT` constant: instruct the model to return only the total amount paid shown on the ticket as a number, null if unreadable). Check `msg.refusal` BEFORE `msg.parsed` (Pitfall 2). Return `None` when refusal is set OR parsed is None OR parsed.monto is None; otherwise return `Decimal(str(parsed.monto))`. Wrap the openai call in try/except and raise `ExtractionFailedError` (reuse the class imported from app.services.extraction) on transport errors; NEVER log the API key (extraction.py:197-198). Bind structlog at call time. Amount-only is the hard requirement per D-02 — do NOT reuse the full ExtractedInvoice schema.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_ticket_vision.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "response_format=TicketAmount" backend/app/services/ticket_vision.py` returns a match.
    - `grep -n "Decimal(str(" backend/app/services/ticket_vision.py` returns a match (no Decimal(float)).
    - `grep -v '^#' backend/app/services/ticket_vision.py | grep -c "msg.refusal"` is >= 1, and refusal is referenced before parsed in source order.
    - `cd backend && python -m pytest tests/test_ticket_vision.py -q` passes with >= 4 tests covering: success, unreadable(None), refusal, transport error.
  </acceptance_criteria>
  <done>TicketVisionService.extract_amount returns Decimal on a readable ticket, None on unreadable/refusal, raises ExtractionFailedError on transport error; API key never logged; all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rework orchestrator to ticket-first + media entry path + deflection</name>
  <read_first>
    - backend/app/services/conversation.py (target — the FULL FSM; preserve entry sequence 185-274, rework _handle_idle 341-392, _handle_awaiting_ticket 446-469, dispatch 296-317)
    - backend/app/models/conversation.py (DraftGasto fields)
    - backend/tests/test_conversation.py (existing FSM tests — the _make_orchestrator / _make_session_factory fixtures at lines 57-77, AsyncMock collaborators)
    - .planning/phases/02-whatsapp-gastos-flow/02-PATTERNS.md (the conversation.py rework section, lines 129-182)
  </read_first>
  <behavior>
    - Test: idle message with concepto present, no monto → state advances to AWAITING_TICKET and reply asks for a ticket photo / "sin ticket" (NOT for an amount). This is the ticket-first reorder (D-01).
    - Test: idle message with concepto missing AND no recognizable gasto slots (off-topic) → fixed Spanish deflection reply returned, state stays IDLE, no draft saved (D-04).
    - Test: idle message missing only concepto but otherwise a gasto attempt → asks for concepto (state remains a concepto-collecting state), does NOT ask amount.
    - Test: at AWAITING_TICKET, text "sin ticket" → ticket_image_path stays None, state → AWAITING_MONTO, reply asks the manager to type the amount (D-01, GASTO-04).
    - Test: handle_message called WITH ticket_image_path + ticket_amount (media path) while state == AWAITING_TICKET → draft.monto = ticket_amount, draft.ticket_image_path = path, state → CONFIRM, reply is the confirm summary showing the resolved amount (D-01a, D-02).
    - Test: handle_message called WITH ticket_image_path but ticket_amount is None (vision unreadable) while AWAITING_TICKET → image path still stored on draft, state → AWAITING_MONTO, reply asks to type the amount (D-01b, reuses re-prompt).
    - Test: duplicate message_id and timeout and cancel paths from Phase 1 still pass unchanged (entry sequence preserved).
  </behavior>
  <action>
    Rework `backend/app/services/conversation.py` for D-01 ticket-first ordering and D-06 media entry, WITHOUT touching the non-negotiable entry sequence (ensure-row → lock → snapshot → idempotency → timeout → cancel → dispatch → commit → reply outside txn; conversation.py:185-274).

    1. Extend `handle_message` signature to carry media: add `ticket_image_path: Optional[str] = None` and `ticket_amount: Optional[Decimal] = None` keyword params (defaulting None so text-only and Phase-1-style calls still work). Thread these into `_dispatch(session, conv, text, ticket_image_path, ticket_amount)`.

    2. Rework `_handle_idle` (currently lines 341-392, including the confused inline comments at 360-376 — DELETE those comments): after `slots = await self._slot_service.extract(text)` and `patch_draft`, branch — if `draft.concepto is None` AND `draft.monto is None` AND the message yielded no usable slots, treat as off-topic → return the fixed Spanish deflection (D-04) and leave `conv.state = ConvState.IDLE` WITHOUT saving a draft. If `draft.concepto is None` but it looks like a gasto attempt → ask for concepto (set a concepto-collecting state; reuse AWAITING_MONTO-as-concepto path is acceptable but the reply must ask concepto). If `draft.concepto` is known → set `conv.state = ConvState.AWAITING_TICKET` and ask for the ticket photo / "sin ticket" (ticket-first — do NOT ask amount here even when monto is missing). If concepto AND monto are both present from the intent (D-01a stated amount), still go to AWAITING_TICKET so vision can re-read; the confirm summary later shows the resolved amount.

    3. Rework `_handle_awaiting_ticket` (currently lines 446-469) — make it `async def` and await it in `_dispatch`. Accept the new `ticket_image_path` / `ticket_amount` params. Branches: (a) text == "sin ticket" (case-insensitive) → `draft.ticket_image_path = None`, `conv.state = ConvState.AWAITING_MONTO`, return a reply asking the manager to type the amount (this is the ONLY remaining use of AWAITING_MONTO as default; reuses the existing `_handle_awaiting_monto` 3-strikes logic on the next turn). (b) `ticket_image_path is not None` AND `ticket_amount is not None` → set `draft.monto = ticket_amount`, `draft.ticket_image_path = ticket_image_path`, `conv.state = ConvState.CONFIRM`, return `self._confirm_summary(draft)` (D-01a/D-02). (c) `ticket_image_path is not None` AND `ticket_amount is None` (vision unreadable, D-01b) → set `draft.ticket_image_path = ticket_image_path`, `conv.state = ConvState.AWAITING_MONTO`, return a reply asking the manager to type the amount (falls into the re-prompt path). (d) plain text that is neither "sin ticket" nor an accompanying photo → re-prompt asking for a photo or "sin ticket" (do NOT silently treat as sin-ticket like the Phase 1 stub did). Use `_save_draft` (reassign, never mutate) in every branch that changes the draft.

    4. Add a module-level `DEFLECTION_REPLY` Spanish constant (concise Argentine Spanish describing what the bot does — e.g. it registers cash expenses; ask the user to describe a gasto). Exact copy is planner discretion per D-04.

    5. `_handle_awaiting_monto` (lines 394-444), `_handle_confirm` (471-497), `_confirm_summary` (499-510), and all pure helpers stay as-is except where the reply copy must reference ticket-first ordering. Keep `is_confirmation`/`is_cancel`/`patch_draft`/`_load_draft`/`_save_draft` unchanged.

    Update `backend/tests/test_conversation.py`: adjust the existing idle→ticket ordering assertions to expect AWAITING_TICKET after concepto is known (was AWAITING_MONTO in Phase 1), and ADD the new behavior tests listed above (media path, deflection, sin-ticket→awaiting_monto, vision-unreadable fallback). Reuse the existing `_make_orchestrator`/`_make_session_factory` fixtures.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_conversation.py tests/test_conversation_lock_sql.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ticket_amount" backend/app/services/conversation.py` returns matches in both handle_message and the ticket handler.
    - `grep -n "DEFLECTION_REPLY\|AWAITING_TICKET" backend/app/services/conversation.py` confirms deflection constant exists and _handle_idle routes to AWAITING_TICKET when concepto known.
    - `grep -v '^#' backend/app/services/conversation.py | grep -c "async def _handle_awaiting_ticket"` == 1 (handler is now async).
    - `grep -n "with_for_update(key_share=True)\|on_conflict_do_nothing\|session.begin()" backend/app/services/conversation.py` all still present (entry sequence preserved).
    - `cd backend && python -m pytest tests/test_conversation.py -q` passes including new tests: idle→AWAITING_TICKET, deflection-stays-idle, sin-ticket→AWAITING_MONTO, media-path→CONFIRM, vision-unreadable→AWAITING_MONTO.
  </acceptance_criteria>
  <done>FSM is ticket-first: concepto-known idle → AWAITING_TICKET; sin ticket → AWAITING_MONTO (type amount); photo media path sets monto+path → CONFIRM; unreadable vision → AWAITING_MONTO fallback; off-topic idle → deflection, stays idle; entry sequence (lock/idempotency/timeout/cancel/commit) unchanged; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router → orchestrator | media path + vision-derived amount cross into the FSM; orchestrator owns the DB write gate |
| orchestrator → OpenAI | image bytes leave to GPT-4o vision; refusal/None must not become a hallucinated amount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | TicketVisionService openai call | mitigate | NEVER log the API key or full image bytes; structlog binds sender/message_id only (extraction.py:197-198 pattern) |
| T-02-02 | Tampering | vision returns hallucinated amount | mitigate | TicketAmount.monto Optional + null>hallucination; check msg.refusal BEFORE parsed; None routes to type-the-amount fallback (D-01b), never a guessed monto |
| T-02-03 | Repudiation | confirm gate bypass | accept | GASTO-05 confirm gate unchanged — money written only on deterministic affirmative (is_confirmation); vision never writes |
| T-02-04 | Denial of Service | oversized image to vision | transfer | size/magic-byte guard enforced at the router boundary (Plan 02); orchestrator receives already-validated bytes/path |
| T-02-SC | Tampering | npm/pip installs | mitigate | No new packages introduced (openai/pydantic already pinned); slopcheck N/A this plan |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_ticket_vision.py tests/test_conversation.py tests/test_conversation_lock_sql.py -q` all pass.
- Ticket-first ordering confirmed: a concepto-known idle message advances to AWAITING_TICKET, not AWAITING_MONTO.
- Deflection confirmed: an off-topic idle message returns DEFLECTION_REPLY and leaves state IDLE with no draft.
</verification>

<success_criteria>
- ROADMAP success criterion 2 (vision reads monto / "sin ticket" asks amount) and 6 (off-topic deflection stays idle) are satisfied at the orchestrator level.
- The orchestrator entry path carries media (D-06) so Plan 02 can feed a stored path + extracted amount.
- D-01 ticket-first reorder, D-01a/b edge handling, D-02 amount-only vision, D-04 deflection all implemented and tested.
</success_criteria>

<output>
Create `.planning/phases/02-whatsapp-gastos-flow/02-01-SUMMARY.md` when done.
</output>
