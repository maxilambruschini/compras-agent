---
phase: 03-prompt-trigger-endpoint
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - backend/app/services/conversation.py
  - backend/app/services/cierre.py
autonomous: true
requirements: [TRIG-02, CAJA-01, CAJA-02]

must_haves:
  truths:
    - "A manager in AWAITING_CIERRE who replies a bare amount advances to AWAITING_CIERRE_CONFIRM with the parsed Decimal held in the draft"
    - "A manager in AWAITING_CIERRE who replies a recognized gasto intent is handed off to the existing gasto flow (no cierre recorded that cycle)"
    - "An explicit affirmative at AWAITING_CIERRE_CONFIRM writes a CajaCierre row and returns state to IDLE; GPT is never invoked at the confirm gate"
    - "hora_cierre is auto-derived in ART: '12:00' before 14:30, '17:00' at/after; fecha is today() in ART (not UTC)"
    - "Duplicate (fecha, hora_cierre) cierres are inserted as separate rows (no unique constraint)"
  artifacts:
    - path: "backend/app/services/cierre.py"
      provides: "CajaCierreService.save_cierre fully implemented (Decimal-safe, ART time, session.flush)"
      contains: "session.add"
    - path: "backend/app/services/conversation.py"
      provides: "AWAITING_CIERRE + AWAITING_CIERRE_CONFIRM states, DraftCierre, dispatch arms, _handle_awaiting_cierre, _handle_cierre_confirm"
      contains: "AWAITING_CIERRE_CONFIRM"
  key_links:
    - from: "backend/app/services/conversation.py"
      to: "parse_ars_amount"
      via: "_handle_awaiting_cierre tries parse BEFORE GPT slot extraction"
      pattern: "parse_ars_amount"
    - from: "backend/app/services/conversation.py"
      to: "CajaCierreService.save_cierre"
      via: "_handle_cierre_confirm after is_confirmation(text) gate"
      pattern: "save_cierre"
    - from: "backend/app/services/cierre.py"
      to: "CajaCierre row"
      via: "session.add + session.flush (caller owns commit)"
      pattern: "session\\.flush"
---

<objective>
Implement the caja-closing branch of the conversation FSM (CAJA-01, CAJA-02) and finish CajaCierreService.save_cierre. This is the reply half of TRIG-02: after the Plan 02 endpoint primes AWAITING_CIERRE, the existing webhook + orchestrator now route the manager's reply into either a cierre confirm flow or a gasto handoff. Turns Plan 01 test_conversation_cierre.py GREEN.

Purpose: Record cash-on-hand into a CajaCierre row with an auto-derived hora_cierre/fecha, behind a deterministic confirm gate, without breaking the existing ticket-first gasto FSM.
Output: extended services/conversation.py + completed services/cierre.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-prompt-trigger-endpoint/03-CONTEXT.md
@.planning/phases/03-prompt-trigger-endpoint/03-RESEARCH.md
@.planning/phases/03-prompt-trigger-endpoint/03-PATTERNS.md

<interfaces>
<!-- Verified from codebase 2026-05-30. -->

conversation.py current structure:
  ConvState (line 85): IDLE, AWAITING_MONTO, AWAITING_TICKET, CONFIRM
  is_confirmation(text) (line 122) / is_cancel(text) (line 144) — deterministic affirmative/cancel token sets; reuse verbatim, GPT never at gate
  _dispatch match block (line 312): cases IDLE(336), AWAITING_MONTO(339), AWAITING_TICKET(342), CONFIRM(349), default(355→IDLE)
  _load_draft(conv) -> DraftGasto (line 361): try DraftGasto.model_validate_json, except → fresh DraftGasto
  _handle_idle(self, session, conv, draft, text) (line 383) — call this for gasto handoff
  _handle_confirm (line 536) — analog for _handle_cierre_confirm; calls self._gasto_service.save_gasto then state→IDLE
  global cancel handled in handle_message Step 6 BEFORE _dispatch — covers ALL states incl. cierre. Do NOT add duplicate cancel handling.
  Imports: DraftGasto, GastoSlots from app.models.conversation (line 77); parse_ars_amount from app.services.amounts (line 78)
  Orchestrator deps: self._slot_service.extract(text) (returns GastoSlots), self._gasto_service, self._provider, self._log

cierre.py (Plan 01 skeleton): _ART, _CUTOFF=time(14,30), _derive_hora_cierre()→str (implemented), _today_art()→date (implemented), CajaCierreService.__init__, async save_cierre(self, session, efectivo_en_caja, sender_phone) → currently NotImplementedError. Use module-level `datetime` so tests patch app.services.cierre.datetime.

CajaCierre ORM (models.py:166): id (Uuid PK auto), fecha (Date), hora_cierre (String(5)), efectivo_en_caja (Numeric(14,2)), sender_phone (String(30)). NO unique constraint. Table exists — NO migration.

DraftCierre: NEW small Pydantic model `cierre_monto: Optional[Decimal] = None`. Define in conversation.py near DraftGasto usage (or import from app.models.conversation if that is where DraftGasto lives — DraftGasto is in app.models.conversation; place DraftCierre there for consistency OR define module-level in conversation.py per discretion, provided test imports `from app.services.conversation import DraftCierre` resolve — re-export if defined elsewhere).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CajaCierreService.save_cierre (Decimal-safe, ART, flush-not-commit)</name>
  <files>backend/app/services/cierre.py</files>
  <read_first>
    - backend/app/services/gasto.py lines 30-78 (GastoService.save_gasto: session.add, session.flush at line 72, structlog .info pattern, caller-owns-txn docstring)
    - backend/app/services/cierre.py (the Plan 01 skeleton — _derive_hora_cierre/_today_art already implemented; fill save_cierre)
    - backend/app/db/models.py lines 166-186 (CajaCierre fields, Numeric(14,2))
    - .planning/phases/03-prompt-trigger-endpoint/03-PATTERNS.md (services/cierre.py section — full save_cierre body) and 03-RESEARCH.md (Pattern 6, Decimal anti-pattern)
    - backend/tests/test_conversation_cierre.py (test_confirm_saves_cierre, test_fecha_art_not_utc, test_duplicate_cierres_allowed, test_hora_cierre_* — the behaviors to satisfy)
  </read_first>
  <behavior>
    - save_cierre builds a CajaCierre with hora_cierre = _derive_hora_cierre() ("12:00" before 14:30 ART else "17:00"), fecha = _today_art(), efectivo_en_caja = the passed Decimal (NO float() conversion), sender_phone stripped of "whatsapp:" prefix.
    - session.add + await session.flush() to populate id; does NOT commit (caller owns the transaction).
    - Two calls in the same (fecha,hora_cierre) produce two distinct rows (no unique constraint → no IntegrityError).
    - Logs "cierre.saved" with id, hora_cierre, monto (str) — never the raw float.
  </behavior>
  <action>
    Replace the NotImplementedError body of `save_cierre` in backend/app/services/cierre.py with the implementation per PATTERNS.md cierre.py section / RESEARCH Pattern 6. Compute clean_phone = sender_phone.removeprefix("whatsapp:").strip(); hora_cierre = _derive_hora_cierre(); fecha = _today_art(). Construct CajaCierre(fecha=fecha, hora_cierre=hora_cierre, efectivo_en_caja=efectivo_en_caja, sender_phone=clean_phone) — pass the Decimal straight through; NEVER `Decimal(float(...))` (binary FP error). session.add(cierre); await session.flush(); self._log.info("cierre.saved", id=str(cierre.id), hora_cierre=hora_cierre, monto=str(efectivo_en_caja)); return cierre. Keep the module-level `datetime` reference (used by _derive_hora_cierre/_today_art) so tests can patch app.services.cierre.datetime.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_conversation_cierre.py::test_hora_cierre_morning tests/test_conversation_cierre.py::test_hora_cierre_afternoon tests/test_conversation_cierre.py::test_fecha_art_not_utc tests/test_conversation_cierre.py::test_duplicate_cierres_allowed -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_conversation_cierre.py::test_hora_cierre_morning -x` exits 0.
    - `pytest tests/test_conversation_cierre.py::test_hora_cierre_afternoon -x` exits 0.
    - `pytest tests/test_conversation_cierre.py::test_fecha_art_not_utc -x` exits 0.
    - `pytest tests/test_conversation_cierre.py::test_duplicate_cierres_allowed -x` exits 0.
    - `grep -c "float(" backend/app/services/cierre.py` returns 0 (no float conversion).
    - `grep "session.flush" backend/app/services/cierre.py` present; `grep -c "session.commit\|\.commit()" backend/app/services/cierre.py` returns 0 (caller commits).
  </acceptance_criteria>
  <done>save_cierre persists a Decimal-safe CajaCierre with ART hora_cierre/fecha via flush; duplicates allowed; no commit, no float.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ConversationOrchestrator with the AWAITING_CIERRE branch + DraftCierre</name>
  <files>backend/app/services/conversation.py</files>
  <read_first>
    - backend/app/services/conversation.py (whole file — ConvState:85, is_confirmation:122, _dispatch match:312-359, _load_draft:361, _handle_idle:383, _handle_awaiting_monto:426, _handle_confirm:536; module docstring Pitfalls C/E)
    - backend/app/services/cierre.py (CajaCierreService.save_cierre + _derive_hora_cierre — import for the confirm echo + write)
    - backend/app/models/conversation.py (DraftGasto definition — place DraftCierre alongside or re-export)
    - .planning/phases/03-prompt-trigger-endpoint/03-PATTERNS.md (conversation.py section — full _handle_awaiting_cierre / _handle_cierre_confirm bodies + dispatch arms; Shared Patterns: Pitfall E reassign, is_confirmation reuse)
    - .planning/phases/03-prompt-trigger-endpoint/03-RESEARCH.md (Pattern 3, Pattern 4, Pitfall 3, Pitfall 4)
    - backend/tests/test_conversation_cierre.py (test_bare_amount_advances_to_confirm, test_gasto_intent_handoff, test_confirm_saves_cierre, test_confirm_requires_exact_token)
  </read_first>
  <behavior>
    - In AWAITING_CIERRE: parse_ars_amount(text) tried FIRST (no API cost). If amount → store DraftCierre(cierre_monto=amount) JSON in conv.draft_gasto (reassign, never mutate — Pitfall E), set state AWAITING_CIERRE_CONFIRM, reply "Cierre {hora}: ${monto} ¿confirmás?...".
    - Else slot_service.extract(text); if slots.concepto or slots.monto present → gasto intent: set conv.draft_gasto = None AND conv.state = IDLE FIRST (Pitfall 4), then return await self._handle_idle(session, conv, DraftGasto(), text).
    - Else → re-prompt (Spanish, asks for efectivo or a gasto description). State unchanged.
    - In AWAITING_CIERRE_CONFIRM: load DraftCierre from conv.draft_gasto (try DraftCierre.model_validate_json, except → fresh + warn). If is_confirmation(text) → await CajaCierreService().save_cierre(session, draft.cierre_monto, conv.sender_phone); set draft_gasto=None, state=IDLE; reply "Cierre registrado. ✓". Else → re-echo the confirm prompt (correction path). GPT NEVER invoked here.
    - Global cancel ("cancelar") already handled in handle_message Step 6 before _dispatch — do NOT add cancel handling in either cierre method.
  </behavior>
  <action>
    Edit backend/app/services/conversation.py. (1) Add to ConvState: AWAITING_CIERRE = "awaiting_cierre" and AWAITING_CIERRE_CONFIRM = "awaiting_cierre_confirm" (both fit String(30)). (2) Define `DraftCierre(BaseModel)` with `cierre_monto: Optional[Decimal] = None` — place in app/models/conversation.py next to DraftGasto and import it here, or define module-level in conversation.py; either way ensure `from app.services.conversation import DraftCierre` resolves (re-export if defined elsewhere). (3) In the `_dispatch` match block, after the `case ConvState.CONFIRM:` arm and before the default, add `case ConvState.AWAITING_CIERRE:` → `reply = await self._handle_awaiting_cierre(session, conv, text)` and `case ConvState.AWAITING_CIERRE_CONFIRM:` → `reply = await self._handle_cierre_confirm(session, conv, text)`. (4) Implement `_handle_awaiting_cierre` per PATTERNS.md: parse_ars_amount first; on amount → DraftCierre JSON reassign + AWAITING_CIERRE_CONFIRM + confirm echo using _derive_hora_cierre() (import from app.services.cierre); on parse failure → slot extraction; if concepto/monto → set draft_gasto=None and state=IDLE then delegate to _handle_idle(session, conv, DraftGasto(), text); else re-prompt. (5) Implement `_handle_cierre_confirm` per PATTERNS.md: load DraftCierre (try/except→warn), if is_confirmation(text) → CajaCierreService().save_cierre(session, draft.cierre_monto, conv.sender_phone), draft_gasto=None, state=IDLE, success reply; else re-echo confirm. Import CajaCierreService and _derive_hora_cierre from app.services.cierre (local import inside methods is acceptable to avoid any import cycle, mirroring how the codebase does lazy service imports). Always REASSIGN conv.draft_gasto (Pitfall E), never mutate. Do NOT touch the existing IDLE/AWAITING_MONTO/AWAITING_TICKET/CONFIRM arms or the global cancel/timeout/idempotency logic.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_conversation_cierre.py -x -q && python -m pytest tests/test_conversation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_conversation_cierre.py::test_bare_amount_advances_to_confirm -x` exits 0.
    - `pytest tests/test_conversation_cierre.py::test_gasto_intent_handoff -x` exits 0.
    - `pytest tests/test_conversation_cierre.py::test_confirm_saves_cierre -x` exits 0.
    - `pytest tests/test_conversation_cierre.py::test_confirm_requires_exact_token -x` exits 0.
    - `grep -c "AWAITING_CIERRE_CONFIRM" backend/app/services/conversation.py` ≥ 3 (constant + dispatch arm + handler).
    - `pytest tests/test_conversation.py -q` still green (existing ticket-first gasto FSM not broken — no regression).
    - In _handle_awaiting_cierre, parse_ars_amount is called before self._slot_service.extract (verified by reading method order).
    - No `is_cancel` / cancel handling added inside the two new methods (grep the methods — global handler owns cancel).
  </acceptance_criteria>
  <done>FSM has working AWAITING_CIERRE + AWAITING_CIERRE_CONFIRM arms with deterministic confirm gate and gasto handoff; cierre tests green; existing conversation tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound reply text → FSM | Untrusted free-text; parsed deterministically (parse_ars_amount) before any GPT call |
| FSM → CajaCierre write | Write gated by deterministic is_confirmation — GPT never decides to persist |
| cierre draft ↔ gasto draft (shared draft_gasto column) | Type confusion risk on handoff; reset to None before switching flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W1 | Tampering | auto-save without confirm | mitigate | deterministic is_confirmation gate before save_cierre (Task 2); test_confirm_requires_exact_token asserts no write on non-affirmative |
| T-03-W2 | Tampering | orphaned cierre draft on gasto handoff | mitigate | set conv.draft_gasto=None before _handle_idle (Task 2, Pitfall 4); _load_draft try/except is double-safety |
| T-03-W3 | Integrity | Decimal/float precision on efectivo_en_caja | mitigate | pass parse_ars_amount Decimal straight to Numeric(14,2); no float() (Task 1) |
| T-03-W4 | Tampering | SQLAlchemy misses draft change (in-place mutation) | mitigate | always reassign conv.draft_gasto (Task 2, Pitfall E) |
| T-03-W5 | Repudiation | cierre write not auditable | accept | structlog "cierre.saved" with id/hora/monto provides the audit trail; sender_phone stored on row (Task 1) |
| T-03-SC | Tampering | npm/pip installs | accept | No new packages (RESEARCH §Package Legitimacy Audit: satisfied by absence; zoneinfo/secrets stdlib). No install task. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_conversation_cierre.py -x -q` → all 8 cierre tests green.
- `cd backend && python -m pytest tests/test_conversation.py -x -q` → existing FSM tests green (no regression).
- Phase gate (full suite): `cd backend && python -m pytest tests/ -x -q` → all green, including Plan 02's test_prompt_trigger.py.
</verification>

<success_criteria>
- AWAITING_CIERRE disambiguation: bare amount → confirm; gasto intent → gasto handoff; neither → re-prompt (CAJA-01, TRIG-02 reply half).
- Confirm gate is deterministic; affirmative writes a CajaCierre, returns to IDLE (CAJA-01).
- hora_cierre/fecha auto-derived in ART; duplicates allowed (CAJA-02).
- Existing gasto FSM untouched; full suite green.
</success_criteria>

<output>
Create `.planning/phases/03-prompt-trigger-endpoint/03-03-SUMMARY.md` when done.
</output>
