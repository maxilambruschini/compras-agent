---
phase: 03-whatsapp-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/app/services/invoice.py
  - backend/app/routers/whatsapp.py
  - backend/tests/test_whatsapp.py
  - backend/tests/test_invoice_service.py
autonomous: false
requirements:
  - WA-03
  - WA-04
  - VAL-01
  - VAL-02
  - VAL-03
tags:
  - whatsapp
  - extraction
  - duplicate-detection
  - background-task
  - reply-formatter

must_haves:
  truths:
    - "After extraction completes successfully with confidence >= threshold, sender receives the D-08 auto_saved summary reply containing proveedor, número, fecha, and total"
    - "After extraction completes successfully with confidence < threshold, sender receives the D-09 pending_review summary reply with the same four fields"
    - "When InvoiceService.find_duplicate returns a matching Invoice, no new row is inserted and sender receives the D-12 duplicate reply containing the original record's fecha"
    - "When the second INSERT trips the UNIQUE constraint (race condition), IntegrityError is caught, rolled back, treated as duplicate, and the D-12 reply is sent"
    - "When ExtractionService raises ExtractionRefusalError or ExtractionFailedError, sender receives the D-11 unreadable-image reply and no row is inserted"
    - "MediaContentType0 outside SUPPORTED_IMAGE_TYPES results in the D-11 unreadable-image reply and the background task exits without calling ExtractionService"
  artifacts:
    - path: "backend/app/services/invoice.py"
      provides: "InvoiceService with find_duplicate(session, numero, proveedor) and save_invoice(session, result, message_id, sender_phone)"
      contains: "class InvoiceService"
    - path: "backend/app/routers/whatsapp.py"
      provides: "process_invoice background task replacing the Plan 01 placeholder; reply formatter helpers"
      contains: "async def process_invoice"
    - path: "backend/tests/test_invoice_service.py"
      provides: "Unit tests for find_duplicate (match / no match / nulls) and save_invoice (success path + IntegrityError path)"
      exports: []
  key_links:
    - from: "backend/app/routers/whatsapp.py::process_invoice"
      to: "backend/app/services/extraction.py::ExtractionService.extract"
      via: "await extraction_service.extract(image_bytes, filename)"
      pattern: "extraction_service\\.extract"
    - from: "backend/app/routers/whatsapp.py::process_invoice"
      to: "backend/app/services/invoice.py::InvoiceService.find_duplicate"
      via: "await invoice_service.find_duplicate(session, numero, proveedor)"
      pattern: "find_duplicate"
    - from: "backend/app/routers/whatsapp.py::process_invoice"
      to: "backend/app/services/invoice.py::InvoiceService.save_invoice"
      via: "await invoice_service.save_invoice(session, result, message_sid, sender_phone)"
      pattern: "save_invoice"
    - from: "backend/app/routers/whatsapp.py::format_summary_reply"
      to: "ExtractionResult.invoice fields (proveedor, numero_documento, fecha) + sum over line_items"
      via: "computed total = Σ bultos × precio_unitario_sin_iva × (1 + iva_rate) × (1 - descuento_pct)"
      pattern: "precio_unitario_sin_iva"
---

<objective>
Replace the Plan 01 background-task placeholder with the real `process_invoice` pipeline and complete the WhatsApp vertical slice end-to-end: download media → validate content type → run ExtractionService → check duplicate (app-level SELECT + DB UNIQUE backstop) → persist Invoice + line items → send a Spanish summary reply whose copy depends on `status`. After this plan, an allowlisted employee sending an invoice photo will see (a) the ack reply from Plan 01, then (b) the extraction summary or duplicate notice or unreadable-image error — covering WA-03, WA-04, VAL-01, VAL-02, VAL-03.

Purpose: Close every Phase 3 requirement that depends on extraction output. Plan 01 proved the request path under 5 seconds; this plan proves the asynchronous half: background extraction lands in the DB and produces a second reply on the same WhatsApp thread. After this plan, sending a duplicate produces the D-12 reply; sending a PDF or other unsupported type produces the D-11 reply without invoking ExtractionService.

Output: New `InvoiceService` with duplicate detection + persistence, replaced `process_invoice` body inside the whatsapp router, summary-reply formatter helpers, and unit + service tests covering all five reply-copy variants and both duplicate paths (app-level + race-condition).

This plan includes a `checkpoint:human-verify` task because INF-04 (response < 5s) and the live two-message Twilio flow can only be observed end-to-end against the live sandbox.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-whatsapp-pipeline/03-CONTEXT.md
@.planning/phases/03-whatsapp-pipeline/03-RESEARCH.md
@.planning/phases/03-whatsapp-pipeline/03-PATTERNS.md
@.planning/phases/03-whatsapp-pipeline/03-VALIDATION.md
@.planning/phases/03-whatsapp-pipeline/03-01-PLAN.md
@backend/app/services/extraction.py
@backend/app/services/storage.py
@backend/app/db/models.py
@backend/app/db/session.py
@backend/app/db/engine.py
@backend/app/models/extraction.py
@backend/app/routers/whatsapp.py
@backend/app/providers/base.py
@backend/tests/conftest.py
@backend/tests/test_extraction.py

<interfaces>
Defined in Plan 01 — consume, do not redefine:
- backend/app/routers/whatsapp.py::_background_tasks (set), _process_invoice_placeholder (signature: sender, message_sid, media_url, media_content_type, provider), ACK_REPLY, NON_ALLOWLISTED_REPLY, UNREADABLE_REPLY, SUPPORTED_IMAGE_TYPES, get_whatsapp_provider
- backend/app/providers/base.py::WhatsAppProvider Protocol (send_message, download_media, validate_signature)
- backend/app/providers/twilio.py::TwilioProvider (constructor + three methods)
- backend/alembic/versions/add_invoice_duplicate_constraint.py (UNIQUE INDEX uq_invoices_numero_proveedor_lower exists in DB)

Phase 2 surfaces consumed by this plan:
- backend/app/services/extraction.py:
  - class ExtractionResult(BaseModel): invoice: ExtractedInvoice, confidence_score: float, status: str ("auto_saved" | "pending_review"), image_path: str
  - async def extract(self, image_bytes: bytes, filename: str) -> ExtractionResult
  - class ExtractionRefusalError(ExtractionError), class ExtractionFailedError(ExtractionError)
  - get_extraction_service factory pattern from backend/app/routers/extraction.py (background task constructs its own service the same way — calls AsyncOpenAI + LocalStorageBackend)

- backend/app/models/extraction.py::ExtractedInvoice fields: tipo_comprobante (Optional[TipoComprobante]), numero_documento (Optional[str]), proveedor (Optional[str]), fecha (Optional[str] ISO YYYY-MM-DD), cuit_proveedor, cae, fecha_vencimiento_cae, line_items: List[ExtractedLineItem]
- backend/app/models/extraction.py::ExtractedLineItem fields: descripcion, codigo_sku, bultos (Decimal), unidades_por_bulto (Decimal), precio_unitario_sin_iva (Decimal), descuento_pct (Decimal), iva_rate (Decimal), percepciones_iibb (Decimal)
  NOTE: There is no document-level `total` field on ExtractedInvoice (REQUIREMENTS.md flags `precio_final_con_iva` as a calculated field). The summary reply MUST compute total = sum over line_items of `bultos × precio_unitario_sin_iva × (1 + iva_rate) × (1 - descuento_pct)`, treating None components as 0, and format with 2 decimal places. If total comes out as 0 because no usable line items, display `—` instead of `$0.00`.

- backend/app/db/models.py::Invoice columns: id (UUID), tipo_comprobante, numero_documento, proveedor, fecha (Date), cuit_proveedor, cae, fecha_vencimiento_cae, confidence_score (Numeric 4,3), status (String 30 not null), whatsapp_message_id (String 100), sender_phone (String 30), image_path (Text), raw_extraction (Text JSON dump), created_at, updated_at, plus relationship line_items.
- backend/app/db/models.py::InvoiceLineItem columns: descripcion, codigo_sku, bultos, unidades_por_bulto, precio_unitario_sin_iva, descuento_pct, iva_rate, percepciones_iibb (all Optional). Created via cascade from Invoice.line_items relationship.

- backend/app/db/engine.py::get_async_session_local() — background task uses this directly (NOT Depends(get_db), per RESEARCH.md note: background tasks run outside the request lifecycle)

Reply copy CONSTANTS (defined in this plan, module-level in whatsapp.py):
- D-08 auto_saved template — see Task 2 action body for the exact f-string layout
- D-09 pending_review template — same four fields with "⚠️ Algunos campos…" header
- D-12 duplicate template — `🔁 Esta factura ya fue registrada el {fecha_original}. No se guardó de nuevo.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InvoiceService (find_duplicate + save_invoice) with tests</name>
  <files>backend/app/services/invoice.py, backend/tests/test_invoice_service.py</files>
  <read_first>
    - backend/app/services/extraction.py lines 134-160, 205-280 — class shape, structlog binding pattern, error logging without secrets (T-02-02)
    - backend/app/db/models.py — Invoice + InvoiceLineItem column names, types, nullability
    - backend/app/models/extraction.py — ExtractedInvoice / ExtractedLineItem field names (mapping target for save_invoice)
    - backend/app/db/session.py — AsyncSession lifecycle
    - backend/tests/test_health.py lines 15-43 — db_session fixture seeding pattern (SenderAllowlist add + commit)
    - backend/tests/conftest.py — db_session fixture, env_setup, async_engine
    - .planning/phases/03-whatsapp-pipeline/03-PATTERNS.md §services/invoice.py + §test_invoice_service.py
    - .planning/phases/03-whatsapp-pipeline/03-RESEARCH.md §Pattern 6 + §Pitfall 6 (NULL behavior of UNIQUE)
    - .planning/phases/03-whatsapp-pipeline/03-CONTEXT.md D-13, D-14, D-15
  </read_first>
  <behavior>
    Tests (all must pass):
    - test_find_duplicate_returns_none_when_no_match: empty DB, find_duplicate("0001-001", "Acme") → None
    - test_find_duplicate_returns_none_when_numero_null: find_duplicate(None, "Acme") → None (early return guard)
    - test_find_duplicate_returns_none_when_proveedor_null: find_duplicate("0001-001", None) → None
    - test_find_duplicate_case_insensitive_match: seed Invoice(numero_documento="0001-00000001", proveedor="Acme SA"); find_duplicate("0001-00000001", "ACME SA") → returns that invoice
    - test_find_duplicate_returns_existing_when_both_match: seed and query with identical casing → returns invoice
    - test_save_invoice_persists_invoice_and_line_items: pass a stub ExtractionResult with 2 line items; save_invoice returns Invoice with id assigned; SELECT counts confirm 1 invoice + 2 line items in DB
    - test_save_invoice_sets_message_metadata: assert saved row has whatsapp_message_id and sender_phone matching args; confidence_score and status from result
    - test_save_invoice_re_raises_integrity_error: monkeypatch session.commit to raise IntegrityError; assert save_invoice rolls back (session.rollback called) and re-raises IntegrityError so caller can treat as duplicate
  </behavior>
  <action>
    Create `backend/app/services/invoice.py` with module docstring citing D-13 + D-15. Imports: `from __future__ import annotations`, `from decimal import Decimal`, `from datetime import date`, `import json`, `import structlog`, `from sqlalchemy import func, select`, `from sqlalchemy.exc import IntegrityError`, `from sqlalchemy.ext.asyncio import AsyncSession`, `from app.db.models import Invoice, InvoiceLineItem`, `from app.services.extraction import ExtractionResult`. Module-level `log = structlog.get_logger()`.

    Class `InvoiceService`: no-arg `__init__` storing `self._log = structlog.get_logger()`. Stateless utility class — all methods take session as the first arg.

    Method `async def find_duplicate(self, session: AsyncSession, numero: str | None, proveedor: str | None) -> Invoice | None`: early return `None` if either arg is None or empty string. Execute `select(Invoice).where(func.lower(Invoice.numero_documento) == func.lower(numero), func.lower(Invoice.proveedor) == func.lower(proveedor)).limit(1)`. Return `result.scalar_one_or_none()`. Log `invoice.duplicate_check` with `numero=numero, proveedor=proveedor, found=<bool>` bound at call time.

    Method `async def save_invoice(self, session: AsyncSession, result: ExtractionResult, message_id: str, sender_phone: str) -> Invoice`. Build an `Invoice` row by copying every header field from `result.invoice` into the matching ORM column (tipo_comprobante via `.value` if it is a TipoComprobante enum else str; numero_documento; proveedor; fecha — parse from ISO string into `date` via `date.fromisoformat(result.invoice.fecha)` if non-null else None; cuit_proveedor; cae; fecha_vencimiento_cae — same fromisoformat). Set `confidence_score=Decimal(str(result.confidence_score))`, `status=result.status`, `whatsapp_message_id=message_id`, `sender_phone=sender_phone.replace("whatsapp:", "").strip()`, `image_path=result.image_path`, `raw_extraction=result.invoice.model_dump_json()`. Append `InvoiceLineItem(...)` instances built from each `result.invoice.line_items` entry. Use `invoice.line_items.append(...)` (the relationship cascade) — do NOT manually set foreign keys. `session.add(invoice)` then `await session.commit()` inside a try/except. On `IntegrityError`: `await session.rollback()`; log `invoice.duplicate_race` with `message_id=message_id`; re-raise. On success: log `invoice.saved` with `id=str(invoice.id), status=result.status` and return `invoice`. Never log secrets.

    Create `backend/tests/test_invoice_service.py`. Use `pytest_asyncio`, `db_session` fixture (already in conftest). Build a helper `make_extraction_result(numero="0001-001", proveedor="Acme SA", status="auto_saved", confidence=0.9, line_items=2)` that returns a fully-formed `ExtractionResult` with the requested number of `ExtractedLineItem` entries (use Decimal values like bultos=1, precio_unitario_sin_iva=100, iva_rate=21.0, descuento_pct=0).

    For `test_save_invoice_re_raises_integrity_error`: use `monkeypatch` to replace `db_session.commit` with an AsyncMock that raises `IntegrityError("UNIQUE", None, None)`; verify `session.rollback` was awaited (also monkeypatched to AsyncMock) and `IntegrityError` propagates. Document the SQLite-vs-Postgres constraint difference in a module-level comment (per 03-PATTERNS.md note — SQLite does not enforce the functional unique index, so the race-condition test uses monkeypatched commit).

    For the case-insensitive tests, seed via `db_session.add(Invoice(...))` + `await db_session.commit()`, then call `InvoiceService().find_duplicate(...)`.

    NEVER place fenced code blocks inside the action.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_invoice_service.py -x 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && pytest tests/test_invoice_service.py -x` exits 0 with all 8 tests passing
    - `grep -E "auth_token|account_sid|openai_api_key" backend/app/services/invoice.py` returns no matches (no secret references)
    - `python -c "from app.services.invoice import InvoiceService; s = InvoiceService(); assert hasattr(s, 'find_duplicate') and hasattr(s, 'save_invoice')"` exits 0
    - `grep -c "func.lower" backend/app/services/invoice.py` >= 2 (case-insensitive on both columns)
    - `grep -c "IntegrityError" backend/app/services/invoice.py` >= 1 (race-condition handler present)
    - `grep -c "rollback" backend/app/services/invoice.py` >= 1
  </acceptance_criteria>
  <done>InvoiceService class created with both methods; 8 unit tests pass; duplicate detection is case-insensitive on both numero and proveedor; IntegrityError on commit triggers rollback and re-raise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace _process_invoice_placeholder with real process_invoice + reply formatters; complete remaining test_whatsapp.py cases</name>
  <files>backend/app/routers/whatsapp.py, backend/tests/test_whatsapp.py</files>
  <read_first>
    - backend/app/routers/whatsapp.py — current Plan 01 state (placeholder coroutine, _background_tasks set, reply constants, get_whatsapp_provider factory)
    - backend/app/services/extraction.py lines 87-100, 205-280 — ExtractionResult shape; how extract() raises ExtractionRefusalError/ExtractionFailedError
    - backend/app/services/invoice.py (just created in Task 1) — find_duplicate + save_invoice signatures and IntegrityError contract
    - backend/app/models/extraction.py — ExtractedLineItem field names for total computation
    - backend/app/db/engine.py — get_async_session_local() entry point used by the background task (it cannot use Depends(get_db) — see RESEARCH.md note)
    - backend/app/routers/extraction.py lines 27-42 — get_extraction_service factory: how to instantiate ExtractionService inside the background task (AsyncOpenAI(api_key=…) + LocalStorageBackend(root=…))
    - backend/tests/test_extraction.py lines 51-99 — make_mock_completion / make_mock_service mock-builder pattern to mirror for Plan 02 reply tests
    - .planning/phases/03-whatsapp-pipeline/03-CONTEXT.md D-08, D-09, D-11, D-12 (exact Spanish reply copy)
    - .planning/phases/03-whatsapp-pipeline/03-RESEARCH.md §System Architecture Diagram (process_invoice flow), §Pitfall 1, §Pitfall 4
    - .planning/REQUIREMENTS.md Calculated Fields table (precio_final_con_iva formula)
  </read_first>
  <behavior>
    Drive the remaining VALIDATION.md test rows from RED to GREEN. Tests to implement / un-skip in test_whatsapp.py:
    - test_unsupported_media_type: MediaContentType0="application/pdf" → after the background task runs, mock provider.send_message awaited with UNREADABLE_REPLY (D-11); ExtractionService.extract NEVER called; no Invoice row inserted
    - test_auto_saved_reply: ExtractionService returns ExtractionResult(status="auto_saved", invoice with proveedor="Acme SA", numero="0001-00001", fecha="2026-05-10", 1 line item bultos=2 precio=100 iva_rate=21 descuento=0) → provider.send_message called with the D-08 template containing all four fields and computed total $242.00
    - test_pending_review_reply: same as above but status="pending_review" → D-09 template with the "⚠️ Algunos campos no se pudieron leer con certeza. Revisar factura desde la web." header
    - test_summary_format: assert reply contains literal strings "Proveedor:", "Número:", "Fecha:", "Total:" exactly once each
    - test_summary_omits_missing_fields: one of proveedor/numero/fecha is None → the corresponding line shows `—` (em-dash) instead of None
    - test_duplicate_app_level: pre-seed an Invoice with numero/proveedor matching the mocked ExtractionResult → provider.send_message awaited with D-12 reply containing the seeded invoice's created_at formatted as ISO date; no second Invoice inserted (SELECT count stays 1)
    - test_duplicate_race_integrity_error: monkeypatch InvoiceService.save_invoice to raise IntegrityError → handler catches, sends D-12 reply, returns without re-raising
    - test_extraction_refusal_sends_error: ExtractionService.extract raises ExtractionRefusalError → provider.send_message awaited with UNREADABLE_REPLY; no Invoice persisted
    - test_extraction_failed_sends_error: ExtractionService.extract raises ExtractionFailedError → same UNREADABLE_REPLY path
    - test_background_task_cleared_after_completion: after process_invoice completes (await asyncio.sleep), _background_tasks set is empty (Plan 01's done_callback still working with the real coroutine)
  </behavior>
  <action>
    In `backend/app/routers/whatsapp.py`, add new module-level reply templates:
    - `AUTO_SAVED_HEADER: str = "✅ Factura registrada:"`
    - `PENDING_REVIEW_HEADER: str = "⚠️ Algunos campos no se pudieron leer con certeza. Revisar factura desde la web."`
    - `DUPLICATE_REPLY_TEMPLATE: str = "🔁 Esta factura ya fue registrada el {fecha_original}. No se guardó de nuevo."`
    - `EM_DASH: str = "—"`

    Add helper `def _compute_total(line_items) -> Decimal | None`: iterate `ExtractedLineItem` entries; for each, compute `(bultos or 0) * (precio_unitario_sin_iva or 0) * (Decimal(1) + (iva_rate or 0) / Decimal(100)) * (Decimal(1) - (descuento_pct or 0))` using `decimal.Decimal`; sum; return None if the total is exactly 0 AND there are no usable line items, else return the Decimal. Round to 2 decimal places via `.quantize(Decimal("0.01"))`. Note: REQUIREMENTS.md `precio_final_con_iva` uses `iva_rate` as a fraction; the extraction schema stores iva_rate as a percentage (e.g. 21.0 means 21%), so divide by 100 in the formula (CLAUDE.md §Calculated Fields).

    Add helper `def format_summary_reply(result: ExtractionResult) -> str`: pick header by `result.status` (AUTO_SAVED_HEADER for "auto_saved", PENDING_REVIEW_HEADER for "pending_review"). Format four field lines exactly:
    - `• Proveedor: {proveedor or EM_DASH}`
    - `• Número: {numero_documento or EM_DASH}`
    - `• Fecha: {fecha or EM_DASH}`
    - `• Total: {f"${total:,.2f}" if total is not None else EM_DASH}`
    Join header + "\n" + the four lines with "\n". The four fields are drawn from `result.invoice`; total is from `_compute_total(result.invoice.line_items)`.

    Add helper `def format_duplicate_reply(existing: Invoice) -> str`: return `DUPLICATE_REPLY_TEMPLATE.format(fecha_original=existing.created_at.date().isoformat() if existing.created_at else EM_DASH)`.

    Replace the body of `_process_invoice_placeholder` with the real `async def process_invoice(sender: str, message_sid: str, media_url: str, media_content_type: str | None, provider: WhatsAppProvider) -> None` body. Rename `_process_invoice_placeholder` → `process_invoice` and update the webhook handler's `asyncio.create_task(...)` call site to use the new name. Preserve the module-level `_background_tasks` set and the `add_done_callback(_background_tasks.discard)` wiring.

    `process_invoice` body sequence:
    1. Bind `log = structlog.get_logger().bind(sender=sender, message_sid=message_sid)`. Log `whatsapp.process_started`.
    2. Content-type guard: if `media_content_type is None` or `media_content_type.lower().split(";")[0].strip() not in SUPPORTED_IMAGE_TYPES`: `await provider.send_message(to=sender, text=UNREADABLE_REPLY)`; log `whatsapp.unsupported_media_type`; return.
    3. Construct services inside the task:
       - `from app.config import get_settings; settings = get_settings()`
       - `from openai import AsyncOpenAI; from app.services.extraction import ExtractionService; from app.services.storage import LocalStorageBackend`
       - `extraction_service = ExtractionService(openai_client=AsyncOpenAI(api_key=settings.openai_api_key), storage=LocalStorageBackend(root=settings.storage_path), settings=settings)`
       - `from app.services.invoice import InvoiceService; invoice_service = InvoiceService()`
    4. Download media: wrap `image_bytes = await provider.download_media(media_url)` in try/except (httpx.HTTPError, ValueError) — on failure: send UNREADABLE_REPLY; log `whatsapp.media_download_failed`; return.
    5. Extract: filename derived from MessageSid + content-type extension (e.g. `f"{message_sid}.jpg"` for image/jpeg, `.png` for image/png). Wrap `result = await extraction_service.extract(image_bytes, filename)` in try/except for `ExtractionRefusalError` and `ExtractionFailedError`: on either, send UNREADABLE_REPLY; log `whatsapp.extraction_failed` with `error=str(exc)`; return.
    6. Open a session: `from app.db.engine import get_async_session_local; session_local = get_async_session_local(); async with session_local() as session:` — inside:
       a. Duplicate check: `existing = await invoice_service.find_duplicate(session, result.invoice.numero_documento, result.invoice.proveedor)`. If existing: `await provider.send_message(to=sender, text=format_duplicate_reply(existing))`; log `whatsapp.duplicate_detected`; return (still inside the session context — it will close cleanly).
       b. Persist: try `await invoice_service.save_invoice(session, result, message_sid, sender)` except `IntegrityError`: race-condition duplicate. Re-query the existing row inside a fresh nested scope (or just send the duplicate template with `fecha_original=EM_DASH` and a log of `whatsapp.duplicate_race`). Send `DUPLICATE_REPLY_TEMPLATE.format(fecha_original=EM_DASH)` and return.
       c. Send summary: `await provider.send_message(to=sender, text=format_summary_reply(result))`; log `whatsapp.process_completed` with `status=result.status`.

    Update `backend/app/routers/whatsapp.py`'s webhook handler step 7 to call `asyncio.create_task(process_invoice(...))` with the new function name. Keep the `_background_tasks.add(task)` and `task.add_done_callback(_background_tasks.discard)` lines unchanged.

    In `backend/tests/test_whatsapp.py`, remove skip markers from the new test names and implement bodies. Use `monkeypatch` to patch `app.routers.whatsapp.ExtractionService` (or import it inside process_invoice so monkeypatching the local symbol works) so the test injects a `make_mock_service(parsed_invoice=...)` style mock returning the desired `ExtractionResult`. Alternative cleaner pattern: monkeypatch `app.routers.whatsapp.process_invoice` to wrap the real function with a service injected via a sentinel arg, OR patch `openai.AsyncOpenAI` AND the storage backend AND inject a fake ExtractionService factory. Choose the simplest mock — patch the symbol `ExtractionService` inside the whatsapp module so `ExtractionService(...)` returns a MagicMock whose `.extract` is an `AsyncMock` returning the seeded `ExtractionResult`. Same approach for `InvoiceService` when needed (for the race-condition test).

    For each background-task assertion, the test must `await asyncio.sleep(0.1)` (or use `await asyncio.gather(*_background_tasks)` snapshot) after the webhook response so the placeholder completes and `_background_tasks` empties.

    NEVER place fenced code blocks inside the action.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_whatsapp.py -x 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && pytest tests/test_whatsapp.py -x` exits 0 with all 16+ tests passing (Plan 01's 6 webhook tests + Plan 02's 10 new tests)
    - `grep -E "_process_invoice_placeholder" backend/app/routers/whatsapp.py | wc -l` == 0 (placeholder fully removed)
    - `grep -c "async def process_invoice" backend/app/routers/whatsapp.py` == 1
    - `grep -c "format_summary_reply\|format_duplicate_reply\|_compute_total" backend/app/routers/whatsapp.py` >= 3
    - `grep -c "Proveedor:\|Número:\|Fecha:\|Total:" backend/app/routers/whatsapp.py` >= 4 (all four summary field labels present)
    - `grep -c "IntegrityError" backend/app/routers/whatsapp.py` >= 1 (race-condition path wired)
    - `grep -E "auth_token|account_sid|openai_api_key" backend/app/routers/whatsapp.py | grep -v "^#\|settings\.openai_api_key\b" | wc -l` returns 0 (no secrets in log statements; only the safe `settings.openai_api_key` constructor reference)
    - `cd backend && pytest -m "not integration" -x` is fully green (Phase 2 still passes; Phase 3 fully covered)
  </acceptance_criteria>
  <done>process_invoice replaces the Plan 01 placeholder; summary, duplicate, and unreadable-media replies are formatted per D-08/D-09/D-11/D-12; all 5 reply-copy variants + both duplicate paths + extraction-error path are tested green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live Twilio sandbox end-to-end verification</name>
  <what-built>
    Full WhatsApp invoice capture pipeline against the live Twilio Sandbox: signature-validated webhook → allowlist gate → ack within 5 seconds → background extraction → summary/duplicate/error reply on the same WhatsApp thread.
  </what-built>
  <how-to-verify>
    Pre-conditions (one-time per machine):
    1. `cd backend && pip install -r requirements.txt` (ensures twilio==9.10.9 is installed)
    2. `cd backend && alembic upgrade head` (ensures uq_invoices_numero_proveedor_lower is in place)
    3. Activate Twilio Sandbox: text "join &lt;keyword&gt;" from your phone to the Twilio Sandbox WhatsApp number (Twilio Console → Messaging → Try it out → Send a WhatsApp message)
    4. Seed your phone in the allowlist: `psql $DATABASE_URL -c "INSERT INTO sender_allowlist (phone_number, display_name, is_active) VALUES ('+&lt;your-e164-number&gt;', 'Tester', true);"`
    5. Start the stack: `docker compose up -d` (or `cd backend && uvicorn app.main:app --reload`)
    6. Expose: `ngrok http 8000` — copy the https URL
    7. Set Twilio sandbox "WHEN A MESSAGE COMES IN" to `https://&lt;ngrok-id&gt;.ngrok-free.app/whatsapp/webhook` (POST)

    Verification scenarios:
    1. **Allowlisted happy path (WA-01 + WA-03 + INF-04):** Send a clear photo of a Factura A or Remito from your allowlisted phone. Verify (a) `✅ Factura recibida. Procesando...` arrives within 5 seconds; (b) a second message arrives within ~30 seconds beginning with `✅ Factura registrada:` and containing all four lines (Proveedor, Número, Fecha, Total). Inspect logs for the `whatsapp.ack_sent` event timestamp vs. the webhook return timestamp — confirm < 5 seconds. (c) `psql $DATABASE_URL -c "SELECT id, proveedor, numero_documento, status, sender_phone FROM invoices ORDER BY created_at DESC LIMIT 1;"` shows the new row.
    2. **Pending-review path (VAL-02):** Send an intentionally blurry or partially obscured invoice photo. Confirm reply begins with `⚠️ Algunos campos no se pudieron leer…`. Confirm `SELECT status FROM invoices ORDER BY created_at DESC LIMIT 1` returns `pending_review`.
    3. **Duplicate path (VAL-01):** Resend the same invoice photo from scenario 1. Confirm reply is `🔁 Esta factura ya fue registrada el YYYY-MM-DD. No se guardó de nuevo.` Confirm `SELECT count(*) FROM invoices WHERE numero_documento = '&lt;the-numero&gt;' AND proveedor = '&lt;the-proveedor&gt;'` returns 1 (not 2).
    4. **Unsupported media (WA-04):** Send a PDF (or any non-image attachment) — Twilio will deliver it with `MediaContentType0=application/pdf`. Confirm reply is `❌ No pudimos procesar la imagen. Asegurate de enviar una foto clara de la factura (JPG o PDF).` Confirm no new Invoice row.
    5. **Non-allowlisted sender (WA-02):** From a phone NOT in `sender_allowlist` (use a friend's phone or temporarily `UPDATE sender_allowlist SET is_active = false WHERE phone_number = '&lt;yours&gt;'`), send any image. Confirm reply is `❌ Este número no está autorizado para enviar facturas.` Confirm no new Invoice row.
    6. **Bad signature (INF-02):** From the host, run `curl -X POST https://&lt;ngrok-id&gt;.ngrok-free.app/whatsapp/webhook -H "X-Twilio-Signature: bogus" -d "From=whatsapp:+1&MessageSid=SMtest&NumMedia=0"` — confirm HTTP 401 response.

    For each scenario, paste a one-line confirmation: `Scenario N: PASS — &lt;observed reply text or DB count&gt;`. If any scenario fails, paste the relevant log lines and stop.
  </how-to-verify>
  <resume-signal>Type "approved" once all six scenarios pass, or describe the failing scenario.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Background task → ExtractionService | Trusted in-process call; image bytes already SSRF-guarded by TwilioProvider.download_media |
| Background task → InvoiceService → Postgres | Trusted DB writes via AsyncSession; race conditions backstopped by UNIQUE constraint |
| Background task → TwilioProvider.send_message | Outbound REST call; same auth token already validated on inbound |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-08 | Tampering | Concurrent duplicate INSERT | mitigate | InvoiceService.save_invoice catches IntegrityError, rolls back, re-raises; whatsapp.process_invoice catches the IntegrityError and sends the D-12 duplicate reply. Backstop: Plan 01's UNIQUE INDEX uq_invoices_numero_proveedor_lower. Verified by test_duplicate_race_integrity_error. |
| T-3-09 | Information Disclosure | Spoofed MediaContentType0 → arbitrary file extraction attempt | mitigate | Content-type guard: media_content_type.lower().split(";")[0].strip() must be in SUPPORTED_IMAGE_TYPES BEFORE download. Defense-in-depth alongside Plan 01's SSRF host guard. Verified by test_unsupported_media_type. |
| T-3-10 | Denial of Service | ExtractionService failure causes user to never receive reply | mitigate | All ExtractionRefusalError / ExtractionFailedError / httpx.HTTPError / ValueError caught with explicit send_message of UNREADABLE_REPLY before return. No silent failures. Verified by test_extraction_refusal_sends_error and test_extraction_failed_sends_error. |
| T-3-11 | Information Disclosure | raw_extraction JSON dump contains PII (CUIT) | accept | raw_extraction is stored in the invoices table protected by application-level access (Phase 4 admin UI). No new exposure surface in Phase 3; logs do not include raw_extraction. Rationale: audit trail value > marginal disclosure risk for invoice data the sender already submitted. |
| T-3-12 | Tampering | Reply copy injection via extracted proveedor / numero_documento | mitigate | format_summary_reply uses plain string substitution into a fixed Spanish template; no HTML/markdown rendering on WhatsApp side — all chars are inert. Long-field DoS bounded by Twilio's 1600-char message limit (auto-truncated by Twilio). No mitigation code beyond using the existing fields as-is. |
</threat_model>

<verification>
- `cd backend && pytest -m "not integration" -x` is fully green
- `cd backend && pytest tests/test_whatsapp.py tests/test_invoice_service.py -x` reports all Plan 01 + Plan 02 tests passing
- `grep -c "_process_invoice_placeholder" backend/app/routers/whatsapp.py` returns 0
- `psql $DATABASE_URL -c "SELECT indexname FROM pg_indexes WHERE indexname = 'uq_invoices_numero_proveedor_lower'"` returns one row (sanity — created by Plan 01)
- Six live-sandbox scenarios in Task 3 all marked PASS by the human verifier
</verification>

<success_criteria>
1. After Task 2 lands, every Phase 3 requirement except INF-04 (timing) and the operational ones is provable via `pytest -x` (WA-03, WA-04, VAL-01, VAL-02, VAL-03 plus Plan 01's WA-01, WA-02, INF-02)
2. The handler returns within 5 seconds and the extraction completes asynchronously, evidenced by Task 3 scenario 1's two-message timing (INF-04)
3. Duplicate submissions (both app-level SELECT and DB UNIQUE race path) produce exactly one Invoice row and one D-12 reply (VAL-01)
4. ExtractionResult.status drives the reply header — auto_saved → D-08, pending_review → D-09 (VAL-02, VAL-03)
5. Unsupported MediaContentType0 and ExtractionRefusalError/ExtractionFailedError both produce the D-11 reply (WA-04)
6. The `_background_tasks` retention pattern from Plan 01 is unmodified — its done_callback discards entries when `process_invoice` returns (no GC pitfall regression)
</success_criteria>

<output>
After completion, create `.planning/phases/03-whatsapp-pipeline/03-02-SUMMARY.md` per the standard summary template.
</output>
