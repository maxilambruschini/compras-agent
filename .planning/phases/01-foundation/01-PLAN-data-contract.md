---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/pyproject.toml
  - backend/app/__init__.py
  - backend/app/config.py
  - backend/app/db/__init__.py
  - backend/app/db/models.py
  - backend/app/db/engine.py
  - backend/app/db/session.py
  - backend/app/models/__init__.py
  - backend/app/models/extraction.py
  - backend/app/routers/__init__.py
  - backend/app/services/__init__.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/alembic/versions/.gitkeep
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_config.py
  - backend/tests/test_db.py
  - backend/tests/test_extraction_models.py
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - INF-01
  - INF-03
tags:
  - foundation
  - sqlalchemy
  - alembic
  - pydantic
  - python

must_haves:
  truths:
    - "Pydantic Settings raises ValidationError when DATABASE_URL or OPENAI_API_KEY is missing from environment"
    - "ExtractedInvoice can be instantiated with all fields None and TipoComprobante.UNKNOWN"
    - "SQLAlchemy Base.metadata contains 'invoices', 'invoice_line_items', and 'sender_allowlist' tables"
    - "Alembic autogenerate produces a migration that creates all three tables"
    - "pytest test suite runs and all Phase-1 unit/integration tests pass against SQLite+aiosqlite test DB"
  artifacts:
    - path: "backend/app/config.py"
      provides: "Settings BaseSettings class with required fields"
      contains: "class Settings(BaseSettings)"
    - path: "backend/app/db/models.py"
      provides: "Invoice, InvoiceLineItem, SenderAllowlist SQLAlchemy ORM models with Base"
      contains: "class Base(DeclarativeBase)"
    - path: "backend/app/db/engine.py"
      provides: "Async engine and AsyncSessionLocal factory"
      contains: "create_async_engine"
    - path: "backend/app/db/session.py"
      provides: "get_db FastAPI dependency"
      contains: "async def get_db"
    - path: "backend/app/models/extraction.py"
      provides: "ExtractedInvoice, LineItem, TipoComprobante (str, Enum)"
      contains: "class TipoComprobante(str, Enum)"
    - path: "backend/alembic/env.py"
      provides: "Async-compatible Alembic env with asyncio.run + NullPool"
      contains: "async_engine_from_config"
    - path: "backend/alembic/versions/*_initial_schema.py"
      provides: "Autogenerated initial migration creating all three tables"
      contains: "op.create_table('invoices'"
    - path: "backend/tests/test_config.py"
      provides: "INF-03 tests — missing env var raises, .env load succeeds"
      contains: "def test_missing_env_raises"
    - path: "backend/tests/test_db.py"
      provides: "INF-01 tests — allowlist table + CRUD"
      contains: "def test_allowlist"
    - path: "backend/tests/test_extraction_models.py"
      provides: "All-None instantiation + UNKNOWN enum test"
      contains: "def test_all_optional"
  key_links:
    - from: "backend/alembic/env.py"
      to: "backend/app/db/models.py"
      via: "from app.db.models import Base import"
      pattern: "from app.db.models import Base"
    - from: "backend/app/db/engine.py"
      to: "backend/app/config.py"
      via: "get_settings().database_url passed to create_async_engine"
      pattern: "create_async_engine.*database_url"
    - from: "backend/tests/conftest.py"
      to: "backend/app/db/models.py"
      via: "Base.metadata.create_all on test engine"
      pattern: "Base\\.metadata"
---

<objective>
Establish the Phase 1 data contract: the Pydantic settings, SQLAlchemy ORM schema, Pydantic extraction models, and Alembic async migration scaffold — plus the Wave 0 pytest test suite that proves INF-01 (allowlist table) and INF-03 (env var fail-fast) are satisfied.

Purpose: Lock the data contract every subsequent phase depends on, and prove it works with automated tests before Docker wiring is attempted in Plan 02.

Output: Backend package skeleton (`backend/app/**`), Alembic scaffold (`backend/alembic/**`), one autogenerated migration creating all three tables, and a green pytest suite covering INF-01, INF-03, and the extraction model contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-SKELETON.md

<interfaces>
<!-- All interface contracts originate in 01-RESEARCH.md and 01-PATTERNS.md (canonical excerpts).
     The executor MUST read those two files before writing any Python; they contain the exact
     module/class/function signatures, including async env.py wiring, mapped_column typing,
     and the use_enum_values config required for OpenAI Structured Outputs in Phase 2. -->

Settings (backend/app/config.py) required fields (no defaults — INF-03):
  database_url: str            # postgresql+asyncpg://user:pass@host:5432/db
  openai_api_key: str
  whatsapp_token: str
  whatsapp_phone_number_id: str
  whatsapp_verify_token: str
Settings optional (with defaults):
  debug: bool = False
  log_level: str = "INFO"
  confidence_threshold: float = 0.85
Module exports: class Settings, get_settings() -> Settings (lru_cache)

SQLAlchemy (backend/app/db/models.py):
  class Base(DeclarativeBase)
  class Invoice(Base) __tablename__ = "invoices"  — see RESEARCH.md Pattern 5 for full column list
  class InvoiceLineItem(Base) __tablename__ = "invoice_line_items" — FK invoices.id ON DELETE CASCADE
  class SenderAllowlist(Base) __tablename__ = "sender_allowlist" — UNIQUE(phone_number)
Module exports: Base, Invoice, InvoiceLineItem, SenderAllowlist

Engine + Session (backend/app/db/engine.py, backend/app/db/session.py):
  engine: AsyncEngine  (create_async_engine with pool_pre_ping=True, expire_on_commit=False on sessionmaker)
  AsyncSessionLocal: async_sessionmaker[AsyncSession]
  async def get_db() -> AsyncGenerator[AsyncSession, None]

Pydantic extraction (backend/app/models/extraction.py):
  class TipoComprobante(str, Enum)  values FACTURA_A/B/C, REMITO, LISTA_INFORMAL, UNKNOWN
  class LineItem(BaseModel) — all fields Optional[T] = None, model_config = ConfigDict(use_enum_values=True)
  class ExtractedInvoice(BaseModel) — all fields Optional[T] = None, line_items: List[LineItem] = []
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend package skeleton + Settings + extraction models + tests Wave 0</name>
  <files>
    backend/requirements.txt,
    backend/pyproject.toml,
    backend/app/__init__.py,
    backend/app/config.py,
    backend/app/models/__init__.py,
    backend/app/models/extraction.py,
    backend/app/routers/__init__.py,
    backend/app/services/__init__.py,
    backend/tests/__init__.py,
    backend/tests/conftest.py,
    backend/tests/test_config.py,
    backend/tests/test_extraction_models.py,
    .env.example,
    .gitignore
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md,
    .planning/phases/01-foundation/01-PATTERNS.md,
    .planning/phases/01-foundation/01-CONTEXT.md,
    .planning/phases/01-foundation/01-VALIDATION.md,
    CLAUDE.md
  </read_first>
  <action>
    Create the `backend/` Python package skeleton and the Wave 0 test infrastructure.

    1) `backend/requirements.txt` pins exact versions from RESEARCH.md Standard Stack:
       fastapi==0.136.1, uvicorn==0.38.0, pydantic==2.13.4, pydantic-settings==2.14.1,
       sqlalchemy==2.0.49, asyncpg==0.31.0, alembic==1.18.4, structlog==25.5.0,
       python-dotenv==1.2.2, openai==2.36.0,
       pytest, pytest-asyncio, aiosqlite, httpx (no version pin on test libs — latest at install).

    2) `backend/pyproject.toml` minimal config — `[tool.pytest.ini_options]` with `asyncio_mode = "auto"` and `testpaths = ["tests"]`. No build-system needed (we run from source).

    3) `backend/app/__init__.py`, `backend/app/models/__init__.py`, `backend/app/routers/__init__.py`, `backend/app/services/__init__.py` — empty package markers (D-02 directory layout).

    4) `backend/app/config.py` — implement Settings exactly per RESEARCH.md Pattern 4 (canonical excerpt in 01-PATTERNS.md). Required fields with NO default: `database_url`, `openai_api_key`, `whatsapp_token`, `whatsapp_phone_number_id`, `whatsapp_verify_token`. Optional with defaults: `debug=False`, `log_level="INFO"`, `confidence_threshold=0.85`. `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", case_sensitive=False)`. Provide `@lru_cache def get_settings() -> Settings`. This satisfies INF-03.

    5) `backend/app/models/extraction.py` — implement per RESEARCH.md Pattern 6. `class TipoComprobante(str, Enum)` with values FACTURA_A, FACTURA_B, FACTURA_C, REMITO, LISTA_INFORMAL, UNKNOWN (D-08). `class LineItem(BaseModel)` and `class ExtractedInvoice(BaseModel)` — every field `Optional[T] = None` per EXT-06 (NEVER omit `= None`; Pydantic v2 does not infer default from Optional — see RESEARCH.md Pitfall 4). `line_items: List[LineItem] = []`. `model_config = ConfigDict(use_enum_values=True)` on both Pydantic classes (required for OpenAI Structured Outputs compatibility in Phase 2). D-07 nesting (header + line_items list).

    6) `backend/tests/__init__.py` — empty.

    7) `backend/tests/conftest.py` — provide an async SQLite (aiosqlite) engine fixture so tests run without Docker/Postgres. Import `Base` from `app.db.models` and call `Base.metadata.create_all` via `run_sync` on engine startup; drop on teardown. Provide a `db_session` AsyncSession fixture using `async_sessionmaker(bind=engine, expire_on_commit=False)`. Also provide an `env_setup` fixture that monkeypatches the required env vars to dummy values so `get_settings()` works in tests that need it; clear `get_settings.cache_clear()` between tests.

    8) `backend/tests/test_config.py` — covers INF-03:
       - `test_missing_env_raises`: with required env vars unset and no `.env` file in cwd, `Settings()` raises `pydantic.ValidationError`.
       - `test_settings_load`: with required env vars set via monkeypatch, `Settings()` constructs and exposes all 5 required fields; `confidence_threshold` defaults to 0.85.

    9) `backend/tests/test_extraction_models.py` — covers EXT-06 + D-08:
       - `test_all_optional`: `ExtractedInvoice()` (no args) instantiates; all fields are None; `line_items == []`.
       - `test_unknown_enum`: `TipoComprobante.UNKNOWN.value == "UNKNOWN"`; `ExtractedInvoice(tipo_comprobante=TipoComprobante.UNKNOWN).tipo_comprobante` equals the string "UNKNOWN" (because `use_enum_values=True`).
       - `test_line_item_optional`: `LineItem()` instantiates with all None.

    10) `.env.example` at repo root — lists all 5 required env vars (DATABASE_URL example using `postgresql+asyncpg://compras:compras@postgres:5432/compras`, OPENAI_API_KEY, WHATSAPP_TOKEN, WHATSAPP_PHONE_NUMBER_ID, WHATSAPP_VERIFY_TOKEN) plus optional ones (DEBUG, LOG_LEVEL, CONFIDENCE_THRESHOLD, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB). Comments only — placeholder values only.

    11) `.gitignore` at repo root — ignore `.env`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `node_modules/`, `frontend/dist/`, `.venv/`, `backend/alembic/versions/__pycache__/`.

    Use existing patterns from RESEARCH.md Pattern 4 (config), Pattern 6 (extraction models), and the Wave 0 list in 01-VALIDATION.md. Do not deviate from pinned versions. Note: SQLAlchemy ORM models and engine are created in Task 2 — the test fixture in conftest.py imports from `app.db.models` which Task 2 will create; Task 2 runs after Task 1 within this plan, so tests added here that depend on `app.db.models` (none in this task) would be premature. The tests in this task touch only `app.config` and `app.models.extraction`.
  </action>
  <verify>
    <automated>cd backend && python -m pip install -r requirements.txt --quiet && pytest tests/test_config.py tests/test_extraction_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/requirements.txt` contains lines: `fastapi==0.136.1`, `pydantic==2.13.4`, `pydantic-settings==2.14.1`, `sqlalchemy==2.0.49`, `alembic==1.18.4`, `asyncpg==0.31.0`, `openai==2.36.0`, `pytest`, `pytest-asyncio`, `aiosqlite`.
    - `backend/pyproject.toml` contains `asyncio_mode = "auto"` under `[tool.pytest.ini_options]`.
    - `grep -c "Optional" backend/app/models/extraction.py` returns >= 14 (8 LineItem fields + ~7 ExtractedInvoice fields, all Optional).
    - `grep "= None" backend/app/models/extraction.py | grep -vc '^#'` returns >= 14 (every Optional has explicit `= None`).
    - `grep -c "use_enum_values=True" backend/app/models/extraction.py` returns >= 2.
    - `grep -c "class TipoComprobante(str, Enum)" backend/app/models/extraction.py` equals 1.
    - `grep -c "UNKNOWN" backend/app/models/extraction.py` >= 1.
    - `python -c "from app.config import Settings; import os; [os.environ.pop(k, None) for k in ['DATABASE_URL','OPENAI_API_KEY','WHATSAPP_TOKEN','WHATSAPP_PHONE_NUMBER_ID','WHATSAPP_VERIFY_TOKEN']]; Settings()"` (run from `backend/`, with `.env` absent or moved) exits non-zero with `ValidationError`.
    - `pytest backend/tests/test_config.py backend/tests/test_extraction_models.py -x -q` exits 0 with 5 tests passing.
    - `.env.example` exists at repo root and contains the strings `DATABASE_URL=`, `OPENAI_API_KEY=`, `WHATSAPP_TOKEN=`.
    - `.gitignore` exists at repo root and contains the string `.env`.
  </acceptance_criteria>
  <done>
    Backend package skeleton and Wave 0 test infrastructure committed. Settings fail-fast verified (INF-03 unit-test green). Extraction model contract verified (EXT-06 unit-test green). All-None ExtractedInvoice and UNKNOWN TipoComprobante work as designed.
  </done>
</task>

<task type="auto">
  <name>Task 2: SQLAlchemy ORM models + async engine + session + Alembic scaffold + initial migration</name>
  <files>
    backend/app/db/__init__.py,
    backend/app/db/models.py,
    backend/app/db/engine.py,
    backend/app/db/session.py,
    backend/alembic.ini,
    backend/alembic/env.py,
    backend/alembic/script.py.mako,
    backend/alembic/versions/.gitkeep,
    backend/tests/test_db.py
  </files>
  <read_first>
    backend/app/config.py,
    backend/app/models/extraction.py,
    backend/tests/conftest.py,
    .planning/phases/01-foundation/01-RESEARCH.md,
    .planning/phases/01-foundation/01-PATTERNS.md
  </read_first>
  <action>
    Create the SQLAlchemy ORM layer, the async engine, the per-request session dependency, and the Alembic scaffold with one autogenerated initial migration creating all three tables.

    1) `backend/app/db/__init__.py` — empty.

    2) `backend/app/db/models.py` — implement EXACTLY per RESEARCH.md Pattern 5 (canonical excerpt copied in 01-PATTERNS.md). Use SQLAlchemy 2.0 typed mapping: `class Base(DeclarativeBase)`, `Mapped[...]` + `mapped_column(...)`. Models:
       - `class Invoice(Base) __tablename__ = "invoices"`. PK `id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)`. Nullable document header columns: tipo_comprobante (String 50), numero_documento (String 100), proveedor (String 255), fecha (Date), cuit_proveedor (String 13), cae (String 20), fecha_vencimiento_cae (Date). Processing metadata: confidence_score (Numeric(4,3)), status (String 30, default "pending_review", NOT NULL), whatsapp_message_id (String 100), sender_phone (String 30), image_path (Text), raw_extraction (Text). Timestamps: created_at, updated_at (DateTime(timezone=True) with server_default=func.now(); updated_at also onupdate=func.now()). Relationship `line_items: Mapped[List["InvoiceLineItem"]] = relationship(back_populates="invoice", cascade="all, delete-orphan")`. `__table_args__` includes `Index("ix_invoices_numero_documento_proveedor", "numero_documento", "proveedor")`, `Index("ix_invoices_status", "status")`, `Index("ix_invoices_created_at", "created_at")`.
       - `class InvoiceLineItem(Base) __tablename__ = "invoice_line_items"`. PK integer autoincrement. `invoice_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("invoices.id", ondelete="CASCADE"))`. Optional columns: descripcion (Text), codigo_sku (String 100), bultos / unidades_por_bulto (Numeric(12,4)), precio_unitario_sin_iva (Numeric(14,4)), descuento_pct / iva_rate (Numeric(6,4)), percepciones_iibb (Numeric(14,4)). `invoice: Mapped["Invoice"] = relationship(back_populates="line_items")`.
       - `class SenderAllowlist(Base) __tablename__ = "sender_allowlist"`. PK integer autoincrement. `phone_number: Mapped[str] = mapped_column(String(30), unique=True, nullable=False)`. `display_name: Mapped[Optional[str]] = mapped_column(String(100))`. `is_active: Mapped[bool] = mapped_column(Boolean, default=True)`. `created_at` with server_default func.now(). This satisfies INF-01.

       Note: SQLite (aiosqlite) does not support `UUID(as_uuid=True)` natively. For test compatibility, models still use the dialect-specific UUID type — at test time, SQLAlchemy stores it as CHAR(32). No code change needed; rely on the dialect's automatic fallback.

    3) `backend/app/db/engine.py` — per RESEARCH.md Pattern 3. `from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker, AsyncSession`. `from app.config import get_settings`. Module-level: `settings = get_settings()`, `engine = create_async_engine(settings.database_url, echo=settings.debug, pool_pre_ping=True)`, `AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False, class_=AsyncSession)`. The `expire_on_commit=False` is mandatory (RESEARCH.md Pitfall 3).

    4) `backend/app/db/session.py` — per RESEARCH.md Pattern 3. `async def get_db() -> AsyncGenerator[AsyncSession, None]` yields a session from `AsyncSessionLocal` with try/finally close.

    5) `backend/alembic.ini` — generate via `alembic init alembic` from `backend/`, then edit:
       - Set `script_location = alembic`.
       - Comment out / leave placeholder `sqlalchemy.url =` line (env.py will override from `DATABASE_URL` env var per RESEARCH.md Pitfall 6).
       - Keep default logging config.

    6) `backend/alembic/env.py` — REPLACE the default sync template with the async version from RESEARCH.md Pattern 1 (canonical excerpt). MUST include:
       - `import asyncio`
       - `from sqlalchemy.ext.asyncio import async_engine_from_config`
       - `from sqlalchemy import pool` and `poolclass=pool.NullPool` in `async_engine_from_config` (RESEARCH.md Pattern 1 critical detail; missing this hangs migrations).
       - `from app.db.models import Base  # noqa: F401` BEFORE `target_metadata = Base.metadata` (RESEARCH.md Pitfall 1 — without this, autogenerate produces empty migrations).
       - URL override: `import os; database_url = os.environ.get("DATABASE_URL"); if database_url: config.set_main_option("sqlalchemy.url", database_url)` (RESEARCH.md Pitfall 6 + Code Examples §Alembic URL Override).
       - `async def run_async_migrations()` calling `await connection.run_sync(do_run_migrations)` and `await connectable.dispose()`.
       - `def run_migrations_online()` doing `asyncio.run(run_async_migrations())`.
       - Standard `run_migrations_offline()` block.

    7) `backend/alembic/script.py.mako` — keep the default template generated by `alembic init`.

    8) `backend/alembic/versions/.gitkeep` — empty file so the directory is tracked. The actual initial migration is generated next.

    9) Generate the initial migration: from `backend/`, with a temporary `DATABASE_URL` env var pointing at a throwaway SQLite file (`sqlite+aiosqlite:///./_autogen.db`) — autogenerate compares Base.metadata against the empty DB and produces the CREATE TABLE statements. Command: `DATABASE_URL=sqlite+aiosqlite:///./_autogen.db alembic revision --autogenerate -m "initial_schema"`. Remove the temporary `_autogen.db` after generation. The resulting `backend/alembic/versions/<hash>_initial_schema.py` must contain `op.create_table('invoices', ...)`, `op.create_table('invoice_line_items', ...)`, `op.create_table('sender_allowlist', ...)`. NOTE: SQLite UUID columns will autogenerate as CHAR — that's fine for the test DB; in production Postgres the asyncpg dialect maps UUID(as_uuid=True) correctly. If the executor needs Postgres-accurate DDL, alternative: spin up a temporary Postgres via `docker run --rm -p 5433:5432 -e POSTGRES_PASSWORD=x postgres:16-alpine` and point `DATABASE_URL` at it. Either approach acceptable — the migration file is regenerable.

    10) `backend/tests/test_db.py` — covers INF-01:
        - `test_allowlist_table_exists`: assert `"sender_allowlist" in Base.metadata.tables`; assert it has columns `phone_number`, `display_name`, `is_active`, `created_at`.
        - `test_allowlist_crud(db_session)`: insert a `SenderAllowlist(phone_number="+5491100000000", display_name="Test")` via `db_session.add` + `await db_session.commit()`. Query back via `select(SenderAllowlist).where(...)`. Assert retrieved row has correct phone_number and `is_active is True`.
        - `test_invoice_relationship(db_session)`: insert an `Invoice()` (all-None header is acceptable; status defaults to "pending_review"), add two `InvoiceLineItem` rows pointing at it, commit, query the invoice with `selectinload(Invoice.line_items)` (or refresh), assert `len(invoice.line_items) == 2`.

    Reference RESEARCH.md Pitfalls 1, 3, 6 throughout. Use existing patterns from 01-PATTERNS.md — do not invent column names or types.
  </action>
  <verify>
    <automated>cd backend && pytest tests/ -x -q && python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; cfg = Config('alembic.ini'); cfg.set_main_option('script_location', 'alembic'); s = ScriptDirectory.from_config(cfg); heads = list(s.get_revisions(s.get_heads())); assert len(heads) == 1, f'Expected 1 head revision, got {len(heads)}'; print('alembic head:', heads[0].revision)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class Base(DeclarativeBase)" backend/app/db/models.py` equals 1.
    - `grep -c "__tablename__ = \"invoices\"" backend/app/db/models.py` equals 1.
    - `grep -c "__tablename__ = \"invoice_line_items\"" backend/app/db/models.py` equals 1.
    - `grep -c "__tablename__ = \"sender_allowlist\"" backend/app/db/models.py` equals 1.
    - `grep -c "ondelete=\"CASCADE\"" backend/app/db/models.py` >= 1.
    - `grep -c "ix_invoices_numero_documento_proveedor" backend/app/db/models.py` >= 1.
    - `grep -c "expire_on_commit=False" backend/app/db/engine.py` equals 1.
    - `grep -c "async def get_db" backend/app/db/session.py` equals 1.
    - `grep -c "pool.NullPool" backend/alembic/env.py` >= 1.
    - `grep -c "from app.db.models import Base" backend/alembic/env.py` >= 1.
    - `grep -c "asyncio.run" backend/alembic/env.py` >= 1.
    - `grep -c "DATABASE_URL" backend/alembic/env.py` >= 1.
    - Exactly one file matching `backend/alembic/versions/*_initial_schema.py` exists.
    - That migration file contains the strings `op.create_table('invoices'`, `op.create_table('invoice_line_items'`, `op.create_table('sender_allowlist'`.
    - That migration file does NOT contain `def upgrade() -> None:\n    pass` (RESEARCH.md Pitfall 1 — empty migration indicates Base import missing).
    - `pytest backend/tests/ -x -q` exits 0 with all Phase-1 tests passing (config + extraction_models from Task 1, db tests from Task 2).
    - `python -c "from app.db.models import Base; print(sorted(Base.metadata.tables.keys()))"` (run from backend/ with PYTHONPATH=.) prints `['invoice_line_items', 'invoices', 'sender_allowlist']`.
  </acceptance_criteria>
  <done>
    SQLAlchemy ORM schema (Invoice, InvoiceLineItem, SenderAllowlist) defined per RESEARCH.md Pattern 5. Async engine and get_db dependency available. Alembic async env.py works (no NullPool / Base import / URL override pitfalls). One autogenerated initial migration creates all three tables. INF-01 covered by passing pytest. SQLAlchemy + Alembic data contract ready for Plan 02 (Docker + FastAPI app + walking-skeleton endpoint).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env → app | Required secrets cross from OS env / `.env` into `Settings`; treated as trusted but MUST never appear in logs or source. |
| developer disk → repo | `.env` contains real secrets locally; must never enter git history. |
| test DB → prod DB | Tests use in-memory aiosqlite; production uses asyncpg. No data crossover. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `backend/app/config.py` Settings | mitigate | Required-field BaseSettings (no defaults on secrets); `.env` listed in `.gitignore`; `.env.example` ships placeholder strings only. INF-03 satisfied by ValidationError on missing var. Test `test_missing_env_raises` proves fail-fast. |
| T-01-02 | Information Disclosure | log output (structlog) | mitigate | Never log `settings.database_url`, `openai_api_key`, or `whatsapp_token`. main.py log call in Plan 02 emits only `log_level`. No secret-bearing log statements in this plan. |
| T-01-03 | Tampering | SQLAlchemy queries on `sender_allowlist` | mitigate | All inserts/queries use ORM parameterized statements (`session.add`, `select(...).where(...)`). No raw string SQL concatenation anywhere in `models.py`, `engine.py`, `session.py`, or tests. |
| T-01-04 | Denial of Service | Alembic migration hang on container boot | mitigate | `pool.NullPool` in `env.py` per RESEARCH.md Pattern 1 critical detail; migrations run via Docker entrypoint command (Plan 02), not FastAPI lifespan (RESEARCH.md anti-pattern, alembic#1483). |
| T-01-05 | Spoofing | (deferred — INF-02 webhook HMAC is Phase 3) | accept | Out of phase scope — Phase 3 implements HMAC-SHA256 verification on inbound webhook. No webhook code exists in Phase 1. |
| T-01-06 | Repudiation | invoice writes lack audit trail | accept | `created_at` / `updated_at` server_default columns on every table provide minimal audit. Full audit log is out of v1 scope per REQUIREMENTS.md. |

</threat_model>

<verification>
- `pytest backend/tests/ -x -q` exits 0 (all 8+ tests green).
- `grep -L "ValidationError" backend/tests/test_config.py` returns nothing (test file references ValidationError).
- One migration file exists in `backend/alembic/versions/` whose body is non-empty (`op.create_table` for all three tables).
- `git diff --stat HEAD` shows new files only — no modifications to `.planning/` artifacts.
</verification>

<success_criteria>
- INF-03 satisfied: app refuses to start when required env vars are missing (proven by `test_missing_env_raises`).
- INF-01 satisfied: `sender_allowlist` table is defined in ORM and created by the initial Alembic migration; CRUD proven by `test_allowlist_crud`.
- D-04, D-06, D-08, D-09 honored: Alembic + SQLAlchemy ORM async + str Enum + split file layout all in place.
- ExtractedInvoice + LineItem can be imported and instantiated with all-None per EXT-06.
- The data contract is locked — Plan 02 can now wire Docker + FastAPI without revisiting model definitions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` capturing: files created, decisions honored (D-02, D-04, D-06, D-08, D-09), version pins applied (requirements.txt), test count green, and any deviations from the canonical RESEARCH.md/PATTERNS.md excerpts.
</output>
