---
phase: "04-admin-ui"
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - frontend/src/types/invoice.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/format.ts
  - frontend/src/hooks/useInvoices.ts
  - frontend/src/hooks/useInvoice.ts
  - frontend/src/components/LoadingSpinner.tsx
  - frontend/src/components/ErrorBanner.tsx
  - frontend/src/components/FormField.tsx
  - frontend/src/components/StatusBadge.tsx
  - frontend/src/components/FilterToolbar.tsx
  - frontend/src/components/InvoiceTable.tsx
  - frontend/src/components/Pagination.tsx
  - frontend/src/pages/InvoiceListPage.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-06

must_haves:
  truths:
    - "Navigating to / shows the invoice list with FilterToolbar, InvoiceTable, and Pagination"
    - "FilterToolbar has Estado select, Proveedor input, Desde/Hasta date inputs, Buscar input, and Filtrar button"
    - "InvoiceTable shows columns: Proveedor, Tipo, Número, Fecha, Estado, Total — Tipo/Número/Fecha hidden on mobile"
    - "Rows with status=pending_review have className bg-amber-50 on the TableRow"
    - "StatusBadge renders Spanish labels: Guardado / Revisar / Confirmado / Rechazado"
    - "Clicking a table row navigates to /invoices/:id"
    - "Pagination shows Página {n} de {total} with Anterior/Siguiente buttons"
    - "App.tsx uses BrowserRouter + Routes with / and /invoices/:id routes"
    - "QueryClientProvider wraps BrowserRouter in App.tsx"
    - "Error state renders ErrorBanner with Spanish message"
    - "Loading state renders LoadingSpinner"
  artifacts:
    - path: "frontend/src/types/invoice.ts"
      provides: "TypeScript interfaces for all API response shapes"
      exports: ["InvoiceListItem", "InvoiceDetailResponse", "InvoiceListResponse", "InvoiceListParams", "LineItemResponse"]
    - path: "frontend/src/lib/api.ts"
      provides: "fetch wrapper and all API call functions"
      exports: ["fetchInvoices", "fetchInvoice", "patchInvoice", "patchInvoiceStatus", "patchLineItem", "deleteInvoice", "imageUrl"]
    - path: "frontend/src/lib/format.ts"
      provides: "Argentine locale formatters"
      exports: ["formatCurrency", "formatDate", "formatCuit"]
    - path: "frontend/src/pages/InvoiceListPage.tsx"
      provides: "Invoice list route component"
    - path: "frontend/src/App.tsx"
      provides: "BrowserRouter + Routes + QueryClientProvider root"
  key_links:
    - from: "frontend/src/pages/InvoiceListPage.tsx"
      to: "frontend/src/hooks/useInvoices.ts"
      via: "useInvoices(params) hook"
      pattern: "useInvoices"
    - from: "frontend/src/components/InvoiceTable.tsx"
      to: "frontend/src/pages/InvoiceListPage.tsx"
      via: "navigate(/invoices/:id) on row click"
      pattern: "useNavigate.*invoices"
    - from: "frontend/src/hooks/useInvoices.ts"
      to: "frontend/src/lib/api.ts"
      via: "fetchInvoices(params) in queryFn"
      pattern: "fetchInvoices"
---

<objective>
Build the TypeScript contract layer (types + API client + formatters), TanStack Query hooks, shared utility components, and the complete Invoice List Page (FilterToolbar + InvoiceTable + Pagination + StatusBadge). Wire App.tsx with BrowserRouter + Routes + QueryClientProvider.

Purpose: Delivers UI-01 (paginated filterable list), UI-02 (search), and UI-06 (pending_review amber highlight). The detail page (Plan 04) depends on types/api/hooks created here.
Output: 14 new/modified files; the list page is functional end-to-end when the backend from Plan 01 is running.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-admin-ui/04-CONTEXT.md
@.planning/phases/04-admin-ui/04-RESEARCH.md
@.planning/phases/04-admin-ui/04-UI-SPEC.md
@.planning/phases/04-admin-ui/04-01-SUMMARY.md
@.planning/phases/04-admin-ui/04-02-SUMMARY.md

<interfaces>
<!-- Key contracts needed by this plan's executor. -->

Backend API contracts (from Plan 01 implementation):
  GET /invoices response shape (InvoiceListResponse):
    { items: InvoiceListItem[], total: number, page: number, page_size: number }

  InvoiceListItem shape:
    { id: string (UUID), tipo_comprobante: string|null, numero_documento: string|null,
      proveedor: string|null, fecha: string|null (ISO date), status: string,
      confidence_score: string|null (Decimal as string), created_at: string (ISO datetime) }

  Query params for GET /invoices: page, page_size, status, proveedor, fecha_from, fecha_to, q

  Status values: "auto_saved" | "pending_review" | "confirmed" | "rejected"

shadcn components available (from Plan 02):
  from "@/components/ui/button": Button (props: variant, size, className, disabled, onClick)
  from "@/components/ui/badge": Badge (props: variant, className, aria-label)
  from "@/components/ui/input": Input (props: type, value, onChange, placeholder, className)
  from "@/components/ui/label": Label (props: htmlFor, className)
  from "@/components/ui/select": Select, SelectContent, SelectItem, SelectTrigger, SelectValue
  from "@/components/ui/table": Table, TableBody, TableCell, TableHead, TableHeader, TableRow
  from "@/components/ui/alert": Alert, AlertDescription
  Imports use @/ alias (resolves to src/)

React Router v7 hooks (import from "react-router" NOT "react-router-dom"):
  BrowserRouter, Routes, Route, useNavigate, useParams, Link

TanStack Query v5:
  QueryClient, QueryClientProvider from "@tanstack/react-query"
  useQuery returns: { data, isPending, error, isFetching }
  useMutation returns: { mutate, isPending, error }
  queryClient.invalidateQueries({ queryKey: [...] })
  Use isPending NOT isLoading (v5 rename — isLoading causes silent undefined bug)

UI-SPEC copywriting (Spanish):
  Page title: "Facturas"
  Filter button: "Filtrar"
  Clear filters: "Limpiar filtros"
  Empty state: heading "Sin facturas", body "No se encontraron facturas..."
  Error state list: "No se pudo cargar la lista de facturas. Verificá la conexión con el servidor e intentá nuevamente."
  Pagination: "Página {n} de {total}", "← Anterior", "Siguiente →"
  Status labels: auto_saved="Guardado", pending_review="Revisar", confirmed="Confirmado", rejected="Rechazado"

UI-SPEC responsive classes (per D-17):
  FilterToolbar: "flex flex-col md:flex-row md:flex-wrap gap-2"
  Filtrar button: "w-full md:w-auto"
  Hidden columns on mobile: className="hidden md:table-cell" on Tipo, Número, Fecha TableHead and TableCell
  pending_review row: className="bg-amber-50" — literal class, NOT dynamic concatenation

Argentine locale formatters:
  formatCurrency: "$ " + Number(value).toLocaleString("es-AR", {minimumFractionDigits:2, maximumFractionDigits:2})
  formatDate: split ISO "YYYY-MM-DD" → "${d}/${m}/${y}"
  CUIT display: already stored as string; no reformatting needed in list view
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + API client + formatters + TanStack Query hooks</name>
  <files>
    frontend/src/types/invoice.ts
    frontend/src/lib/api.ts
    frontend/src/lib/format.ts
    frontend/src/hooks/useInvoices.ts
    frontend/src/hooks/useInvoice.ts
  </files>
  <read_first>
    - frontend/src/App.tsx (current skeleton — understand what exists)
    - frontend/package.json (verify react-router and @tanstack/react-query are installed from Plan 02)
  </read_first>
  <action>
    Create frontend/src/types/invoice.ts. Define TypeScript interfaces matching the backend API response shapes exactly:
      LineItemResponse: { id: number; invoice_id: string; descripcion: string|null; codigo_sku: string|null; bultos: string|null; unidades_por_bulto: string|null; precio_unitario_sin_iva: string|null; descuento_pct: string|null; iva_rate: string|null; percepciones_iibb: string|null }
      InvoiceListItem: { id: string; tipo_comprobante: string|null; numero_documento: string|null; proveedor: string|null; fecha: string|null; status: string; confidence_score: string|null; created_at: string }
      InvoiceDetailResponse: extends InvoiceListItem adding { cuit_proveedor: string|null; cae: string|null; fecha_vencimiento_cae: string|null; image_path: string|null; updated_at: string; line_items: LineItemResponse[] }
      InvoiceListResponse: { items: InvoiceListItem[]; total: number; page: number; page_size: number }
      InvoiceListParams: { page?: number; page_size?: number; status?: string; proveedor?: string; fecha_from?: string; fecha_to?: string; q?: string }
      InvoiceDocumentPatch: { tipo_comprobante?: string|null; numero_documento?: string|null; proveedor?: string|null; fecha?: string|null; cuit_proveedor?: string|null; cae?: string|null; fecha_vencimiento_cae?: string|null }
      LineItemPatch: { descripcion?: string|null; codigo_sku?: string|null; bultos?: string|null; unidades_por_bulto?: string|null; precio_unitario_sin_iva?: string|null; descuento_pct?: string|null; iva_rate?: string|null; percepciones_iibb?: string|null }

    Create frontend/src/lib/api.ts. Use: const BASE_URL = import.meta.env.VITE_API_URL ?? "". Define async function request<T>(path: string, init?: RequestInit): Promise<T> that awaits fetch(`${BASE_URL}${path}`, headers merged with Content-Type: application/json), checks res.ok (throws Error with detail from JSON or "HTTP {status}" fallback), returns res.json(). Export: fetchInvoices(params: InvoiceListParams) → request<InvoiceListResponse> building URLSearchParams from non-undefined, non-empty params (filter out undefined values before constructing params string); fetchInvoice(id: string) → request<InvoiceDetailResponse>; patchInvoice(id: string, data: Partial<InvoiceDocumentPatch>) → PATCH request<InvoiceDetailResponse>; patchInvoiceStatus(id: string, status: "confirmed" | "rejected") → PATCH /invoices/{id}/status request<InvoiceDetailResponse>; patchLineItem(id: string, itemId: number, data: Partial<LineItemPatch>) → PATCH /invoices/{id}/items/{itemId} request<LineItemResponse>; deleteInvoice(id: string) → DELETE /invoices/{id} with no response body parsing (204). imageUrl(imagePath: string): string — extracts filename via imagePath.split('/').pop() ?? imagePath then returns `${BASE_URL}/images/${filename}`.

    Create frontend/src/lib/format.ts. Export: formatCurrency(value: number | string | null | undefined): string — returns "—" for null/undefined, otherwise "$ " + Number(value).toLocaleString("es-AR", {minimumFractionDigits: 2, maximumFractionDigits: 2}). formatDate(iso: string | null | undefined): string — returns "—" for null/undefined/empty, splits "YYYY-MM-DD" and returns "${d}/${m}/${y}". formatCuit(cuit: string | null | undefined): string — returns "—" for null, otherwise returns cuit as-is (already stored correctly).

    Create frontend/src/hooks/useInvoices.ts: import useQuery from @tanstack/react-query and fetchInvoices from ../lib/api; export function useInvoices(params: InvoiceListParams) returns useQuery({ queryKey: ["invoices", params], queryFn: () => fetchInvoices(params) }).

    Create frontend/src/hooks/useInvoice.ts: export function useInvoice(id: string) returns useQuery({ queryKey: ["invoice", id], queryFn: () => fetchInvoice(id), enabled: !!id }).
  </action>
  <verify>
    <automated>cd /Users/maximolambruschini/NewCombin/compras_agent/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 with no TypeScript errors after creating these 5 files
    - frontend/src/types/invoice.ts exports InvoiceListItem, InvoiceDetailResponse, InvoiceListResponse, InvoiceListParams, LineItemResponse, InvoiceDocumentPatch, LineItemPatch
    - frontend/src/lib/api.ts exports fetchInvoices, fetchInvoice, patchInvoice, patchInvoiceStatus, patchLineItem, deleteInvoice, imageUrl
    - frontend/src/lib/format.ts exports formatCurrency, formatDate, formatCuit
    - frontend/src/lib/api.ts contains "imagePath.split('/')" for filename extraction (not hardcoded path)
    - frontend/src/hooks/useInvoices.ts queryKey is ["invoices", params] (params included so filter changes trigger refetch)
    - frontend/src/hooks/useInvoice.ts contains "enabled: !!id"
  </acceptance_criteria>
  <done>5 files created. TypeScript compiles clean. API functions, formatters, and hooks are exported and typed.</done>
</task>

<task type="auto">
  <name>Task 2: Shared components + InvoiceListPage + App router wiring</name>
  <files>
    frontend/src/components/LoadingSpinner.tsx
    frontend/src/components/ErrorBanner.tsx
    frontend/src/components/FormField.tsx
    frontend/src/components/StatusBadge.tsx
    frontend/src/components/FilterToolbar.tsx
    frontend/src/components/InvoiceTable.tsx
    frontend/src/components/Pagination.tsx
    frontend/src/pages/InvoiceListPage.tsx
    frontend/src/App.tsx
  </files>
  <read_first>
    - frontend/src/types/invoice.ts (interfaces to import — created in Task 1)
    - frontend/src/lib/api.ts (exported functions — created in Task 1)
    - frontend/src/hooks/useInvoices.ts (hook to consume — created in Task 1)
    - frontend/src/components/ui/button.tsx (shadcn Button props)
    - frontend/src/components/ui/badge.tsx (shadcn Badge props)
    - frontend/src/components/ui/table.tsx (shadcn Table component exports)
    - frontend/src/components/ui/alert.tsx (shadcn Alert props)
    - frontend/src/components/ui/input.tsx (shadcn Input props)
    - frontend/src/components/ui/select.tsx (shadcn Select exports)
  </read_first>
  <action>
    Create frontend/src/components/LoadingSpinner.tsx: a div with className "animate-spin rounded-full h-8 w-8 border-t-2 border-b-2 border-blue-600". Export as default. Used in: table loading area, and as child inside disabled buttons during mutations.

    Create frontend/src/components/ErrorBanner.tsx: accepts props { message: string }. Uses shadcn Alert with variant="destructive" wrapping AlertDescription with the message text. Export as default.

    Create frontend/src/components/FormField.tsx: accepts props { label: string; htmlFor: string; children: React.ReactNode; className?: string }. Renders shadcn Label (htmlFor) above children (the input element), wrapped in a div with className "space-y-2" merged with className prop. Used in edit modals. Export as default.

    Create frontend/src/components/StatusBadge.tsx: accepts props { status: string }. Uses shadcn Badge. Map status to variant and label: auto_saved → variant="secondary", label="Guardado"; pending_review → variant="outline" plus custom className="border-amber-500 text-amber-700 bg-amber-50", label="Revisar"; confirmed → variant="default" plus className="bg-green-600 text-white", label="Confirmado"; rejected → variant="destructive", label="Rechazado". Apply aria-label={label} to the Badge. Export as default.

    Create frontend/src/components/FilterToolbar.tsx: accepts props { onFilter: (params: InvoiceListParams) => void }. Internal state: status (string, default ""), proveedor (string, default ""), fechaFrom (string, default ""), fechaTo (string, default ""), q (string, default ""). Renders a div with className "flex flex-col md:flex-row md:flex-wrap gap-2 p-4 bg-gray-50 border-b border-gray-200". Children: shadcn Select for status (options: "" = "Todos los estados", "auto_saved" = "Guardado", "pending_review" = "Revisar", "confirmed" = "Confirmado", "rejected" = "Rechazado"); shadcn Input for proveedor (placeholder "Proveedor"); two shadcn Inputs type="date" for fechaFrom (placeholder "Desde") and fechaTo (placeholder "Hasta"); shadcn Input for q (placeholder "Buscar…"); Button onClick calls onFilter({ status: status||undefined, proveedor: proveedor||undefined, fecha_from: fechaFrom||undefined, fecha_to: fechaTo||undefined, q: q||undefined }) — pass undefined (not empty string) so the API client filters them out; a plain text button or span "Limpiar filtros" (text-sm text-blue-600 cursor-pointer) that resets all state and calls onFilter({}).

    Create frontend/src/components/InvoiceTable.tsx: accepts props { invoices: InvoiceListItem[] }. Uses shadcn Table, TableHeader, TableBody, TableRow, TableHead, TableCell. Header row: Proveedor, Tipo (hidden md:table-cell), Número (hidden md:table-cell), Fecha (hidden md:table-cell), Estado, Total. Table has no explicit "Total" field in InvoiceListItem — omit Total column from list (it's not in the list response shape; only in detail). Revised columns: Proveedor, Tipo (hidden md:table-cell), Número (hidden md:table-cell), Fecha (hidden md:table-cell), Estado, Creado. Each TableRow: className={invoice.status === "pending_review" ? "bg-amber-50 cursor-pointer hover:bg-amber-100" : "cursor-pointer hover:bg-gray-50"} — use this exact literal className check (not dynamic concatenation). Row onClick calls useNavigate() to navigate to "/invoices/" + invoice.id. Render StatusBadge for the Estado cell. Render formatDate for Fecha and Creado. If invoices is empty, render a single TableRow with a TableCell colSpan={6} containing "Sin facturas" empty state text.

    Create frontend/src/components/Pagination.tsx: accepts props { page: number; pageSize: number; total: number; onPageChange: (page: number) => void }. Computes totalPages = Math.max(1, Math.ceil(total / pageSize)). Renders: Button variant="outline" "← Anterior" disabled={page <= 1} onClick={() => onPageChange(page - 1)}; span "Página {page} de {totalPages}"; Button variant="outline" "Siguiente →" disabled={page >= totalPages} onClick={() => onPageChange(page + 1)}. Wrap in a div with className "flex items-center justify-center gap-4 py-4". All Buttons have min-h-[44px] for touch target compliance.

    Create frontend/src/pages/InvoiceListPage.tsx: manages state: params (InvoiceListParams, default { page: 1, page_size: 20 }). Calls useInvoices(params). Handles isPending → renders LoadingSpinner inside a centered div. Handles error → renders ErrorBanner with "No se pudo cargar la lista de facturas. Verificá la conexión con el servidor e intentá nuevamente." Renders: a div container with className "min-h-screen bg-white"; a header nav with className "px-4 py-3 border-b flex items-center" containing h1 "Facturas" with className "text-xl font-semibold"; FilterToolbar with onFilter={(newParams) => setParams({ ...newParams, page: 1, page_size: 20 })} — resets to page 1 on filter change; InvoiceTable with invoices={data?.items ?? []}; Pagination with page={params.page ?? 1} pageSize={params.page_size ?? 20} total={data?.total ?? 0} onPageChange={(p) => setParams(prev => ({ ...prev, page: p }))}.

    Replace frontend/src/App.tsx entirely with: import { BrowserRouter, Routes, Route } from "react-router"; import { QueryClient, QueryClientProvider } from "@tanstack/react-query"; import InvoiceListPage from "./pages/InvoiceListPage"; import InvoiceDetailPage from "./pages/InvoiceDetailPage"; const queryClient = new QueryClient(); export default function App() { return (<QueryClientProvider client={queryClient}><BrowserRouter><Routes><Route path="/" element={<InvoiceListPage />} /><Route path="/invoices/:id" element={<InvoiceDetailPage />} /></Routes></BrowserRouter></QueryClientProvider>); }. Note: InvoiceDetailPage does not exist yet (Plan 04 creates it). Create a temporary stub file frontend/src/pages/InvoiceDetailPage.tsx that exports a placeholder component: export default function InvoiceDetailPage() { return <div>Detail page — coming in Plan 04</div>; }. This stub allows App.tsx to compile now; Plan 04 replaces it.
  </action>
  <verify>
    <automated>cd /Users/maximolambruschini/NewCombin/compras_agent/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 with 0 TypeScript errors
    - npm run build exits 0 with no errors
    - frontend/src/App.tsx imports from "react-router" (not "react-router-dom") — grep -c '"react-router-dom"' frontend/src/App.tsx equals 0
    - frontend/src/App.tsx contains QueryClientProvider wrapping BrowserRouter (QueryClientProvider appears before BrowserRouter in the JSX tree)
    - frontend/src/components/InvoiceTable.tsx contains the literal string "bg-amber-50" (not a dynamic concatenation)
    - frontend/src/components/StatusBadge.tsx contains "border-amber-500" for pending_review variant
    - frontend/src/components/FilterToolbar.tsx contains "flex flex-col md:flex-row" responsive class
    - frontend/src/components/InvoiceTable.tsx contains "hidden md:table-cell" on column headers for Tipo, Número, Fecha
    - frontend/src/pages/InvoiceListPage.tsx uses useInvoices hook (grep -c "useInvoices" gives 1+)
    - frontend/src/pages/InvoiceListPage.tsx contains isPending check before rendering table
  </acceptance_criteria>
  <done>All 9 components + InvoiceListPage + App.tsx created. TypeScript compiles clean. npm run build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → FastAPI (via fetch) | API calls in lib/api.ts; error responses parsed safely |
| API response → React state | JSON deserialized into typed interfaces; no eval or innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | Tampering | FilterToolbar search input → GET /invoices?q= | accept at frontend | SQL injection is mitigated server-side (Plan 01 T-4-02); frontend passes q as a URL param, not SQL |
| T-4-frontend-01 | Information Disclosure | Error messages surfaced to UI | accept | Backend error details shown in ErrorBanner help devs debug; acceptable for v1 demo (no auth, localhost only) |
</threat_model>

<verification>
After Task 2 completes:

1. TypeScript: cd frontend && npx tsc --noEmit — 0 errors
2. Build: cd frontend && npm run build — exits 0
3. Amber row: grep -c "bg-amber-50" frontend/src/components/InvoiceTable.tsx — at least 1
4. Router import: grep '"react-router"' frontend/src/App.tsx — present; grep '"react-router-dom"' frontend/src/App.tsx — absent
5. Hidden columns: grep -c "hidden md:table-cell" frontend/src/components/InvoiceTable.tsx — at least 3 (Tipo, Número, Fecha head + cells)
6. useInvoices hook: grep -c "useInvoices" frontend/src/pages/InvoiceListPage.tsx — at least 1
</verification>

<success_criteria>
- npx tsc --noEmit: 0 errors
- npm run build: exits 0
- frontend/src/App.tsx: imports from "react-router"; QueryClientProvider wraps BrowserRouter; Routes has "/" and "/invoices/:id"
- frontend/src/components/InvoiceTable.tsx: "bg-amber-50" literal class on pending_review rows; "hidden md:table-cell" on Tipo/Número/Fecha columns
- frontend/src/components/FilterToolbar.tsx: "flex flex-col md:flex-row" responsive layout
- frontend/src/lib/api.ts: imageUrl() extracts filename via split('/').pop()
- UI-01 satisfied: InvoiceListPage renders FilterToolbar + InvoiceTable + Pagination; useInvoices drives data
- UI-02 satisfied: FilterToolbar passes q param to onFilter; useInvoices includes q in queryKey and queryFn
- UI-06 satisfied: InvoiceTable applies bg-amber-50 className when invoice.status === "pending_review"
</success_criteria>

<output>
After completion, create .planning/phases/04-admin-ui/04-03-SUMMARY.md using the template at @$HOME/.claude/get-shit-done/templates/summary.md
</output>
