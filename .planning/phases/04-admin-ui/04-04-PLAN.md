---
phase: "04-admin-ui"
plan: "04"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - frontend/src/pages/InvoiceDetailPage.tsx
  - frontend/src/components/DataPanel.tsx
  - frontend/src/components/ImagePanel.tsx
  - frontend/src/components/InvoiceHeader.tsx
  - frontend/src/components/ActionBar.tsx
  - frontend/src/components/LineItemsTable.tsx
  - frontend/src/components/EditDocumentModal.tsx
  - frontend/src/components/EditLineItemModal.tsx
  - frontend/src/components/DeleteConfirmation.tsx
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Navigating to /invoices/:id shows DataPanel on the left and ImagePanel on the right (desktop grid)"
    - "On mobile the layout is single-column: DataPanel then ImagePanel stacked vertically"
    - "InvoiceHeader displays all document-level fields with a '← Volver a la lista' link and 'Editar documento' button"
    - "ActionBar shows Confirmar and Rechazar buttons only when invoice.status === 'pending_review'"
    - "ActionBar always shows Eliminar factura button"
    - "Clicking Confirmar sends PATCH /invoices/{id}/status with status=confirmed and invalidates both invoice and invoices query keys"
    - "Clicking Rechazar sends PATCH /invoices/{id}/status with status=rejected and invalidates both keys"
    - "Clicking Eliminar shows the inline DeleteConfirmation strip; Sí eliminar sends DELETE and navigates to /"
    - "LineItemsTable shows all line item fields; each row has an Editar button opening EditLineItemModal"
    - "EditDocumentModal opens pre-filled with current invoice values; Guardar cambios sends PATCH /invoices/{id}"
    - "EditLineItemModal opens pre-filled with current line item values; Guardar cambios sends PATCH /invoices/{id}/items/{item_id}"
    - "Both modals show ErrorBanner inside the modal on save failure; close on success"
    - "ImagePanel renders img (or embed for PDF) with alt text including proveedor and numero_documento"
  artifacts:
    - path: "frontend/src/pages/InvoiceDetailPage.tsx"
      provides: "Invoice detail route — replaces stub from Plan 03"
    - path: "frontend/src/components/EditDocumentModal.tsx"
      provides: "Dialog for editing document-level fields (per D-01, D-02)"
    - path: "frontend/src/components/EditLineItemModal.tsx"
      provides: "Dialog for editing single line item fields (per D-01, D-02)"
    - path: "frontend/src/components/DeleteConfirmation.tsx"
      provides: "Inline confirmation strip for delete flow (per UI-05)"
    - path: "frontend/src/components/ActionBar.tsx"
      provides: "Confirm/Reject/Delete buttons with status-conditional rendering (per D-04, D-07)"
  key_links:
    - from: "frontend/src/pages/InvoiceDetailPage.tsx"
      to: "frontend/src/hooks/useInvoice.ts"
      via: "useInvoice(id) hook with id from useParams"
      pattern: "useInvoice.*useParams"
    - from: "frontend/src/components/ActionBar.tsx"
      to: "frontend/src/lib/api.ts"
      via: "patchInvoiceStatus(id, 'confirmed'|'rejected') in useMutation"
      pattern: "patchInvoiceStatus"
    - from: "frontend/src/components/EditDocumentModal.tsx"
      to: "frontend/src/lib/api.ts"
      via: "patchInvoice(id, data) in useMutation"
      pattern: "patchInvoice"
    - from: "frontend/src/components/DeleteConfirmation.tsx"
      to: "frontend/src/lib/api.ts"
      via: "deleteInvoice(id) in useMutation; navigate('/') on success"
      pattern: "deleteInvoice.*navigate"
---

<objective>
Build the complete Invoice Detail Page: side-by-side layout (DataPanel + ImagePanel), edit modals for document and line items, Confirm/Reject/Delete action bar, and the inline delete confirmation strip. Replaces the stub InvoiceDetailPage from Plan 03.

Purpose: Delivers UI-03 (detail view with image), UI-04 (edit any field), and UI-05 (delete with file retention). This plan completes all user-facing functionality.
Output: 9 new/modified files. The detail page is functional end-to-end: data displays, edits persist, status changes work, delete navigates away.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-admin-ui/04-CONTEXT.md
@.planning/phases/04-admin-ui/04-RESEARCH.md
@.planning/phases/04-admin-ui/04-UI-SPEC.md
@.planning/phases/04-admin-ui/04-01-SUMMARY.md
@.planning/phases/04-admin-ui/04-02-SUMMARY.md
@.planning/phases/04-admin-ui/04-03-SUMMARY.md

<interfaces>
<!-- Key contracts this plan's executor needs. -->

From Plan 03 (types/hooks/shared components now exist):
  useInvoice(id: string) → { data: InvoiceDetailResponse|undefined, isPending, error }
  useMutation pattern (TQ v5 — use isPending NOT isLoading):
    const mutation = useMutation({ mutationFn: ..., onSuccess: () => { queryClient.invalidateQueries({ queryKey: ["invoice", id] }); queryClient.invalidateQueries({ queryKey: ["invoices"] }) } })
  LoadingSpinner, ErrorBanner, FormField, StatusBadge — importable from @/components/

  InvoiceDetailResponse fields (beyond InvoiceListItem):
    cuit_proveedor: string|null; cae: string|null; fecha_vencimiento_cae: string|null;
    image_path: string|null; updated_at: string; line_items: LineItemResponse[]

  LineItemResponse fields:
    id: number; invoice_id: string; descripcion: string|null; codigo_sku: string|null;
    bultos: string|null; unidades_por_bulto: string|null; precio_unitario_sin_iva: string|null;
    descuento_pct: string|null; iva_rate: string|null; percepciones_iibb: string|null

  imageUrl(imagePath: string): string — from @/lib/api (extracts filename, builds /images/{filename} URL). A4 assumption applies: filenames must be globally unique in storage_path. See imageUrl JSDoc in api.ts for full note.
  InvoiceId — branded string type alias for UUID strings; use InvoiceId (not plain string) for id fields in all components that receive invoice.id or item.invoice_id.
  formatCurrency, formatDate — from @/lib/format

shadcn components available:
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter — from @/components/ui/dialog
  Button — from @/components/ui/button (variants: default, outline, destructive, ghost)
  Input — from @/components/ui/input
  Label — from @/components/ui/label
  Select, SelectContent, SelectItem, SelectTrigger, SelectValue — from @/components/ui/select
  Table, TableBody, TableCell, TableHead, TableHeader, TableRow — from @/components/ui/table
  Alert, AlertDescription — from @/components/ui/alert

UI-SPEC copywriting (Spanish):
  Edit document modal title: "Editar documento"; Save button: "Guardar cambios"
  Edit line item modal title: "Editar ítem"; Save button: "Guardar cambios"
  Back link: "← Volver a la lista"
  Edit document button: "Editar documento"
  Edit line item button: "Editar" (per row, small variant)
  Confirm button: "Confirmar"; Reject button: "Rechazar"; Delete button: "Eliminar factura"
  Delete confirmation heading: "¿Eliminar esta factura?"
  Delete body: "Se eliminará el registro de la base de datos. El archivo original se conserva."
  Delete confirm: "Sí, eliminar"; Delete cancel: "Cancelar"
  Error — detail fetch: "No se pudo cargar la factura. Verificá la conexión con el servidor e intentá nuevamente."
  Error — save edit: "No se pudo guardar. Intentá nuevamente."
  Error — status update: "No se pudo actualizar el estado. Intentá nuevamente."
  Error — delete: "No se pudo eliminar la factura. Intentá nuevamente."

UI-SPEC responsive layout (per D-08, D-17):
  Detail page grid: "grid grid-cols-1 md:grid-cols-2 gap-8" — single column mobile, two column desktop
  ImagePanel: static on mobile, "md:sticky md:top-6" on desktop
  ActionBar buttons: "flex flex-col md:flex-row gap-2 md:gap-3"; each Button: "w-full md:w-auto"
  Modals: DialogContent with className "w-full mx-4 md:max-w-[560px]" (document), "w-full mx-4 md:max-w-[640px]" (line item)
  All interactive elements: min-h-[44px]

Confirm/Reject logic (per D-04, D-05, D-06, D-07):
  Buttons visible only when invoice.status === "pending_review"
  One-click — no reason input
  Confirmar → patchInvoiceStatus(id, "confirmed")
  Rechazar → patchInvoiceStatus(id, "rejected")
  Both invalidate ["invoice", id] and ["invoices"]

Delete logic (per UI-05, D-09):
  DELETE /invoices/{id} removes DB row only — image file is retained
  On success: invalidate ["invoices"]; navigate("/")
  Confirmation is an inline strip (not a Dialog modal) appearing below ActionBar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImagePanel + InvoiceHeader + ActionBar + DeleteConfirmation</name>
  <files>
    frontend/src/components/ImagePanel.tsx
    frontend/src/components/InvoiceHeader.tsx
    frontend/src/components/ActionBar.tsx
    frontend/src/components/DeleteConfirmation.tsx
  </files>
  <read_first>
    - frontend/src/types/invoice.ts (InvoiceDetailResponse interface — all field names)
    - frontend/src/lib/api.ts (imageUrl function, patchInvoiceStatus, deleteInvoice)
    - frontend/src/lib/format.ts (formatDate, formatCurrency, formatCuit)
    - frontend/src/components/ui/button.tsx (Button variants and props)
    - frontend/src/components/ErrorBanner.tsx (ErrorBanner props)
    - frontend/src/hooks/useInvoice.ts (verify hook exists and returns correct type)
  </read_first>
  <action>
    Create frontend/src/components/ImagePanel.tsx: accepts props { imagePath: string|null; proveedor: string|null; numeroDocumento: string|null }. If imagePath is null, render a div with className "flex items-center justify-center h-64 bg-gray-100 text-gray-500 rounded" and text "Sin imagen". Otherwise: derive the file extension from imagePath.split('.').pop()?.toLowerCase(). If extension is "pdf", render embed element with src={imageUrl(imagePath)} type="application/pdf" className="w-full max-h-[80vh]". Otherwise render img with src={imageUrl(imagePath)} alt={`Factura original de ${proveedor ?? "proveedor"} — ${numeroDocumento ?? "sin número"}`} className="w-full max-h-[80vh] object-contain rounded border border-gray-200". Wrap in a div with className "sticky top-6" — but only on desktop (not sticky on mobile per D-17). Use Tailwind: div className="md:sticky md:top-6".

    Create frontend/src/components/InvoiceHeader.tsx: accepts props { invoice: InvoiceDetailResponse; onEditClick: () => void }. Renders a section with a header row containing h2 "Encabezado" (text-lg font-semibold) and a Button variant="outline" size="sm" onClick={onEditClick} "Editar documento". Below that, a definition-list-style grid (dl with className "grid grid-cols-2 gap-x-4 gap-y-2 text-sm") displaying these field pairs as dt (text-gray-500 text-xs font-semibold uppercase) + dd (text-gray-900): Proveedor / invoice.proveedor ?? "—"; Tipo comprobante / invoice.tipo_comprobante ?? "—"; Número / invoice.numero_documento ?? "—"; Fecha / formatDate(invoice.fecha); CUIT proveedor / formatCuit(invoice.cuit_proveedor); CAE / invoice.cae ?? "—"; Vencimiento CAE / formatDate(invoice.fecha_vencimiento_cae); Estado / StatusBadge with status={invoice.status}; Confianza / invoice.confidence_score ? `${(Number(invoice.confidence_score) * 100).toFixed(0)}%` : "—". Also render a Link to "/" (from "react-router") styled as "← Volver a la lista" text-sm text-blue-600 above the header row.

    Create frontend/src/components/ActionBar.tsx: accepts props { id: string; status: string; onDeleteClick: () => void; onEditClick?: never }. Internally uses useMutation for status changes and useQueryClient for invalidation. The confirm mutation: mutationFn: () => patchInvoiceStatus(id, "confirmed"), onSuccess: invalidate ["invoice", id] and ["invoices"]. The reject mutation: mutationFn: () => patchInvoiceStatus(id, "rejected"), onSuccess: same invalidation. Local state: statusError (string|null). Clear statusError on success. On mutation error: set statusError to "No se pudo actualizar el estado. Intentá nuevamente." Renders: div className="flex flex-col gap-3 py-4". First child: div className="flex flex-col md:flex-row gap-2 md:gap-3" containing — if status === "pending_review": Button onClick={confirmMutation.mutate} disabled={confirmMutation.isPending || rejectMutation.isPending} className="w-full md:w-auto" "Confirmar" (with LoadingSpinner child when isPending); Button variant="outline" onClick={rejectMutation.mutate} disabled={...} className="w-full md:w-auto" "Rechazar" (with LoadingSpinner). Always: Button variant="destructive" onClick={onDeleteClick} className="w-full md:w-auto" "Eliminar factura". Second child: if statusError, render ErrorBanner with message={statusError}.

    Create frontend/src/components/DeleteConfirmation.tsx: accepts props { id: string; onCancel: () => void }. Uses useMutation with mutationFn: () => deleteInvoice(id). On success: queryClient.invalidateQueries({ queryKey: ["invoices"] }) then navigate("/") via useNavigate. Local state: deleteError (string|null). On mutation error: set deleteError to "No se pudo eliminar la factura. Intentá nuevamente." Renders: an inline div (not a Dialog) with className "border border-red-200 rounded-lg p-4 bg-red-50 flex flex-col gap-3". Heading p "¿Eliminar esta factura?" font-semibold text-red-800. Body p "Se eliminará el registro de la base de datos. El archivo original se conserva." text-sm text-red-700. Button row div className="flex gap-2": Button variant="destructive" onClick={deleteMutation.mutate} disabled={deleteMutation.isPending} "Sí, eliminar" with LoadingSpinner when isPending; Button variant="ghost" onClick={onCancel} disabled={deleteMutation.isPending} "Cancelar". If deleteError, render ErrorBanner below. aria-describedby on the "Sí, eliminar" button pointing to the body paragraph id.
  </action>
  <verify>
    <automated>cd /Users/maximolambruschini/NewCombin/compras_agent/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 with 0 errors after creating these 4 files
    - frontend/src/components/ImagePanel.tsx contains "md:sticky md:top-6" (desktop-only sticky)
    - frontend/src/components/ImagePanel.tsx renders embed for PDF extension and img otherwise (grep -c "embed\|img" gives 2+)
    - frontend/src/components/ImagePanel.tsx img element has alt attribute containing "Factura original de"
    - frontend/src/components/ActionBar.tsx contains "pending_review" conditional check for Confirmar/Rechazar buttons
    - frontend/src/components/ActionBar.tsx uses isPending NOT isLoading (grep -c "isPending" gives 2+; grep -c "isLoading" gives 0)
    - frontend/src/components/ActionBar.tsx contains "w-full md:w-auto" on button classNames (responsive per D-17)
    - frontend/src/components/DeleteConfirmation.tsx contains navigate("/") in onSuccess handler
    - frontend/src/components/DeleteConfirmation.tsx does NOT contain "Dialog" import — inline strip, not modal
    - frontend/src/components/DeleteConfirmation.tsx contains "El archivo original se conserva" (exact copy per UI-SPEC)
  </acceptance_criteria>
  <done>4 component files created. TypeScript compiles clean. ImagePanel, InvoiceHeader, ActionBar, DeleteConfirmation are correctly implemented.</done>
</task>

<task type="auto">
  <name>Task 2: LineItemsTable + EditDocumentModal + EditLineItemModal + InvoiceDetailPage</name>
  <files>
    frontend/src/components/LineItemsTable.tsx
    frontend/src/components/EditDocumentModal.tsx
    frontend/src/components/EditLineItemModal.tsx
    frontend/src/pages/InvoiceDetailPage.tsx
  </files>
  <read_first>
    - frontend/src/types/invoice.ts (LineItemResponse, InvoiceDetailResponse, InvoiceDocumentPatch, LineItemPatch)
    - frontend/src/lib/api.ts (patchInvoice, patchLineItem — signatures to call)
    - frontend/src/components/ui/dialog.tsx (Dialog, DialogContent, DialogHeader, DialogTitle props)
    - frontend/src/components/ui/input.tsx (Input props)
    - frontend/src/components/ui/select.tsx (Select, SelectTrigger, SelectContent, SelectItem, SelectValue)
    - frontend/src/components/FormField.tsx (FormField props — label, htmlFor, children)
    - frontend/src/hooks/useInvoice.ts (hook signature and return type)
    - frontend/src/pages/InvoiceDetailPage.tsx (current stub from Plan 03 — will be replaced entirely)
    - frontend/src/components/ActionBar.tsx (onDeleteClick prop — how it wires to DeleteConfirmation)
    - frontend/src/components/InvoiceHeader.tsx (onEditClick prop — how it wires to EditDocumentModal)
  </read_first>
  <action>
    Create frontend/src/components/LineItemsTable.tsx: accepts props { invoiceId: string; items: LineItemResponse[] }. Uses shadcn Table, TableHeader, TableBody, TableHead, TableRow, TableCell. Column headers: #, Descripción, SKU, Bultos, U/Bulto, P.Unit. s/IVA, Dto%, IVA%, Percepciones, (actions). Each data row: renders each field or "—" if null; formatCurrency for precio_unitario_sin_iva and percepciones_iibb; a percentage display for descuento_pct (if value: `${(Number(value) * 100).toFixed(1)}%` else "—"); for iva_rate: `${(Number(value) * 100).toFixed(0)}%` else "—". Last cell: Button variant="ghost" size="sm" className="min-h-[44px]" onClick={() => setEditingItem(item)} "Editar". Local state: editingItem (LineItemResponse|null). If editingItem is not null, render EditLineItemModal with the item and onClose={() => setEditingItem(null)}. If items is empty, render single row with TableCell colSpan={11} "Sin ítems registrados".

    Create frontend/src/components/EditDocumentModal.tsx: accepts props { open: boolean; onOpenChange: (open: boolean) => void; invoice: InvoiceDetailResponse }. Uses shadcn Dialog. Manages local form state initialized from invoice props: tipo_comprobante, numero_documento, proveedor, fecha (use invoice.fecha ?? ""), cuit_proveedor, cae, fecha_vencimiento_cae. Uses useMutation: mutationFn: (data: InvoiceDocumentPatch) => patchInvoice(invoice.id, data). onSuccess: queryClient.invalidateQueries(["invoice", invoice.id]); queryClient.invalidateQueries(["invoices"]); onOpenChange(false). Local state: saveError (string|null). On mutation error: set saveError to "No se pudo guardar. Intentá nuevamente." Renders: DialogContent className="w-full mx-4 md:max-w-[560px]". DialogHeader with DialogTitle "Editar documento". Form body with className "flex flex-col gap-4 py-4" containing FormField components for each editable field: Proveedor (Input text), Tipo comprobante (Input text), Número (Input text), Fecha (Input type="date"), CUIT proveedor (Input text), CAE (Input text), Vencimiento CAE (Input type="date"). Footer: if saveError render ErrorBanner; div className="flex justify-end gap-2": Button variant="ghost" onClick={() => onOpenChange(false)} "Cancelar"; Button onClick={() => mutation.mutate({tipo_comprobante: tipo_comprobante||null, numero_documento: numero_documento||null, proveedor: proveedor||null, fecha: fecha||null, cuit_proveedor: cuit_proveedor||null, cae: cae||null, fecha_vencimiento_cae: fecha_vencimiento_cae||null})} disabled={mutation.isPending} "Guardar cambios" with LoadingSpinner when isPending.

    Create frontend/src/components/EditLineItemModal.tsx: accepts props { open: boolean; onOpenChange: (open: boolean) => void; invoiceId: string; item: LineItemResponse }. Same Dialog pattern as EditDocumentModal but for line item fields. Form state initialized from item: descripcion, codigo_sku, bultos, unidades_por_bulto, precio_unitario_sin_iva, descuento_pct, iva_rate, percepciones_iibb (all string form values). useMutation: mutationFn: (data: LineItemPatch) => patchLineItem(invoiceId, item.id, data). onSuccess: invalidate ["invoice", invoiceId] and ["invoices"]; onOpenChange(false). DialogContent className="w-full mx-4 md:max-w-[640px]" (wider for numeric columns). DialogTitle "Editar ítem". FormFields: Descripción (Input text), Código SKU (Input text), Bultos (Input type="number" step="0.0001"), Unidades por bulto (Input type="number" step="0.0001"), Precio unit. s/IVA (Input type="number" step="0.0001"), Descuento % (Input type="number" step="0.01" placeholder="0 a 1 — ej: 0.10 = 10%"), IVA rate (Input type="number" step="0.005" placeholder="0, 0.105, o 0.21"), Percepciones IIBB (Input type="number" step="0.0001"). Submit passes all values as strings (Decimal fields are string on the API).

    Replace frontend/src/pages/InvoiceDetailPage.tsx entirely (removing the stub). Use useParams<{ id: string }>() to get id. Call useInvoice(id ?? ""). If isPending render LoadingSpinner centered. If error render ErrorBanner "No se pudo cargar la factura. Verificá la conexión con el servidor e intentá nuevamente." If no data render null. Local state: showEditModal (boolean, default false); showDeleteConfirm (boolean, default false). Layout: div className="min-h-screen bg-white". Nav row: div className="px-4 py-3 border-b" (back link is inside InvoiceHeader). Main content: div className="p-4 md:p-6 grid grid-cols-1 md:grid-cols-2 gap-8". Left column (DataPanel inline): div className="flex flex-col gap-6" containing: InvoiceHeader with invoice={data} onEditClick={() => setShowEditModal(true)}; ActionBar with id={data.id} status={data.status} onDeleteClick={() => setShowDeleteConfirm(true)}; if showDeleteConfirm: DeleteConfirmation with id={data.id} onCancel={() => setShowDeleteConfirm(false)}; LineItemsTable with invoiceId={data.id} items={data.line_items}. Right column: div className="md:sticky md:top-6": ImagePanel with imagePath={data.image_path} proveedor={data.proveedor} numeroDocumento={data.numero_documento}. After main content div, render EditDocumentModal with open={showEditModal} onOpenChange={setShowEditModal} invoice={data}.
  </action>
  <verify>
    <automated>cd /Users/maximolambruschini/NewCombin/compras_agent/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 with 0 TypeScript errors
    - npm run build exits 0
    - frontend/src/pages/InvoiceDetailPage.tsx contains "grid grid-cols-1 md:grid-cols-2 gap-8" (responsive two-column layout per D-08, D-17)
    - frontend/src/pages/InvoiceDetailPage.tsx contains "showDeleteConfirm" local state wiring DeleteConfirmation
    - frontend/src/pages/InvoiceDetailPage.tsx contains "showEditModal" local state wiring EditDocumentModal
    - frontend/src/components/EditDocumentModal.tsx contains "md:max-w-[560px]" modal width
    - frontend/src/components/EditLineItemModal.tsx contains "md:max-w-[640px]" modal width (wider for numeric columns)
    - frontend/src/components/EditDocumentModal.tsx uses isPending NOT isLoading on the save Button
    - frontend/src/components/EditDocumentModal.tsx title is "Editar documento" (exact Spanish per UI-SPEC)
    - frontend/src/components/EditLineItemModal.tsx title is "Editar ítem" (exact Spanish per UI-SPEC)
    - frontend/src/components/LineItemsTable.tsx renders an Edit button per row with variant="ghost" and min-h-[44px]
    - frontend/src/components/EditDocumentModal.tsx onSuccess calls invalidateQueries for both ["invoice", id] and ["invoices"] (per D-03)
  </acceptance_criteria>
  <done>LineItemsTable, EditDocumentModal, EditLineItemModal created. InvoiceDetailPage stub replaced with full implementation. TypeScript compiles clean. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit modal form → PATCH API | User-supplied field values sent as JSON; validated server-side by Pydantic |
| ImagePanel img src → GET /images/{filename} | Browser makes direct cross-origin request; CORS from Plan 01 enables this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | ImagePanel img src construction via imageUrl() | mitigate at frontend | imageUrl() extracts only the filename component via split('/').pop() — path separators cannot be injected into the URL; server-side Path regex is the primary defense (Plan 01) |
| T-4-04 | Denial of Service | EditDocumentModal / EditLineItemModal PATCH body size | accept | FastAPI 1MB default body limit; Pydantic rejects unknown fields; form inputs are plain text fields with no file upload |
| T-4-frontend-02 | Information Disclosure | Invoice image served without auth | accept | v1 demo; UI-07 auth deferred; documented in Plan 01 admin.py docstring; image URL includes only filename, not full path |
</threat_model>

<verification>
After Task 2 completes:

1. TypeScript: cd frontend && npx tsc --noEmit — 0 errors
2. Build: cd frontend && npm run build — exits 0
3. Responsive layout: grep -c "grid-cols-1 md:grid-cols-2" frontend/src/pages/InvoiceDetailPage.tsx — at least 1
4. Modal widths: grep "max-w-\[560px\]" frontend/src/components/EditDocumentModal.tsx — present; grep "max-w-\[640px\]" frontend/src/components/EditLineItemModal.tsx — present
5. No isLoading: grep -rn "isLoading" frontend/src/components/Edit*.tsx — 0 occurrences
6. Dual invalidation: grep -c "invalidateQueries" frontend/src/components/EditDocumentModal.tsx — at least 2
7. Delete navigation: grep -c 'navigate("/")' frontend/src/components/DeleteConfirmation.tsx — at least 1
8. Delete non-modal: grep -c "Dialog" frontend/src/components/DeleteConfirmation.tsx — 0 (inline strip, not a Dialog)
</verification>

<success_criteria>
- npx tsc --noEmit: 0 errors
- npm run build: exits 0
- UI-03 satisfied: InvoiceDetailPage shows InvoiceHeader (all document fields), LineItemsTable (all line items), ImagePanel (img or embed) in side-by-side grid
- UI-04 satisfied: EditDocumentModal and EditLineItemModal open pre-filled, send PATCH on save, invalidate and re-render on success, show error on failure
- UI-05 satisfied: DeleteConfirmation sends DELETE /invoices/{id}, navigates to / on success, does not delete filesystem file
- D-04/D-07 satisfied: Confirmar and Rechazar buttons render only when status === "pending_review"
- D-08/D-17 satisfied: "grid grid-cols-1 md:grid-cols-2 gap-8" on the detail page outer div
- All Spanish copy matches UI-SPEC exactly ("Editar documento", "Editar ítem", "Guardar cambios", "¿Eliminar esta factura?", "Sí, eliminar", etc.)
</success_criteria>

<output>
After completion, create .planning/phases/04-admin-ui/04-04-SUMMARY.md using the template at @$HOME/.claude/get-shit-done/templates/summary.md
</output>
