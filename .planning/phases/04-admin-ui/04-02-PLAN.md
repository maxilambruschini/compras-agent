---
phase: "04-admin-ui"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.app.json
  - frontend/index.html
  - frontend/src/index.css
autonomous: true
requirements:
  - UI-01
  - UI-06

must_haves:
  truths:
    - "Vite dev server starts without errors after toolchain changes"
    - "Tailwind v4 utility classes compile (bg-amber-50, grid, flex, etc.) in the browser"
    - "shadcn/ui components exist in frontend/src/components/ui/ (button.tsx, dialog.tsx, badge.tsx, input.tsx, label.tsx, select.tsx, table.tsx, alert.tsx, separator.tsx)"
    - "The @/* path alias resolves to frontend/src/ in both TypeScript and Vite"
    - "index.css has @import tailwindcss as first meaningful line with no duplicate"
    - "#root CSS block does not contain width or text-align constraints"
    - "All custom CSS tokens (--text, --bg, --border, --accent-admin, --pending-bg, etc.) are declared in :root"
  artifacts:
    - path: "frontend/src/components/ui/button.tsx"
      provides: "shadcn Button component"
    - path: "frontend/src/components/ui/dialog.tsx"
      provides: "shadcn Dialog component"
    - path: "frontend/src/components/ui/badge.tsx"
      provides: "shadcn Badge component"
    - path: "frontend/src/components/ui/table.tsx"
      provides: "shadcn Table component"
    - path: "frontend/src/index.css"
      provides: "Tailwind v4 import + custom tokens + shadcn theme block + clean #root"
    - path: "frontend/vite.config.ts"
      provides: "tailwindcss() plugin + @/* alias + extended proxy"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "@tailwindcss/vite"
      via: "tailwindcss() in plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "frontend/tsconfig.app.json"
      to: "frontend/src/"
      via: "@/* path alias"
      pattern: '"@/\\*".*"./src/\\*"'
    - from: "frontend/src/index.css"
      to: "tailwindcss"
      via: "@import \"tailwindcss\""
      pattern: '@import "tailwindcss"'
---

<objective>
Install and configure the complete frontend toolchain: Tailwind CSS v4, shadcn/ui (init + 9 components), React Router v7, and TanStack Query v5. Extend vite.config.ts with the tailwindcss plugin, @/* alias, and additional proxy routes. Fix index.css and remove the walking skeleton #root constraints.

Purpose: Wave 2 plans (InvoiceListPage, InvoiceDetailPage) cannot begin until shadcn components exist and Tailwind classes compile. This plan makes the frontend buildable with the full design system in place.
Output: Updated package.json, vite.config.ts, tsconfig.app.json, index.css; 9 shadcn component files in src/components/ui/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-admin-ui/04-CONTEXT.md
@.planning/phases/04-admin-ui/04-RESEARCH.md
@.planning/phases/04-admin-ui/04-UI-SPEC.md

<interfaces>
<!-- Key facts the executor needs from codebase reads. -->

From frontend/vite.config.ts (current state):
  import { defineConfig } from 'vite'
  import react from '@vitejs/plugin-react'
  const apiTarget = process.env.VITE_API_URL || 'http://localhost:8000'
  plugins: [react()]
  proxy: { '/api': apiTarget, '/health': apiTarget }
  -- Must ADD: tailwindcss() plugin, path alias, '/invoices' and '/images' proxy entries

From frontend/src/index.css (current state):
  - Has #root { width: 1126px; text-align: center; ... } -- MUST remove width and text-align
  - Has --text, --bg, --border, --sans, --heading, --mono, --code-bg custom properties -- keep all
  - Has purple scaffold accent --accent: #aa3bff -- replace with --accent-admin: #2563eb
  - shadcn init will write @import "tailwindcss" and @theme inline { ... } block -- do not pre-add

From frontend/package.json (current state):
  dependencies: react ^19.2.6, react-dom ^19.2.6
  devDependencies: vite ^8.0.12, @vitejs/plugin-react ^6.0.1, typescript ~6.0.2, @types/node ^24.12.3
  -- react-router, @tanstack/react-query, tailwindcss, @tailwindcss/vite are NOT yet installed

Critical sequencing (from PATTERNS.md):
  1. npm install tailwindcss @tailwindcss/vite (before shadcn -- shadcn detects Tailwind version)
  2. npm install react-router @tanstack/react-query (can run alongside step 1)
  3. Add @/* alias to tsconfig.app.json BEFORE shadcn init (shadcn writes @/components/ui/ imports)
  4. npx shadcn@2 init (writes @import "tailwindcss" and @theme inline to index.css) — pinned to @2, not @latest
  5. npx shadcn@2 add button dialog badge input label select table alert separator (one at a time or batched)
  6. After shadcn init, manually restore custom tokens and fix #root in index.css
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages + configure vite.config.ts + tsconfig.app.json alias</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.app.json
  </files>
  <read_first>
    - frontend/package.json (current deps to avoid downgrading anything)
    - frontend/vite.config.ts (current config to extend, not replace)
    - frontend/tsconfig.app.json (find compilerOptions to add paths to)
  </read_first>
  <action>
    From the frontend/ directory, run: npm install tailwindcss @tailwindcss/vite react-router @tanstack/react-query

    Update frontend/vite.config.ts to add: import tailwindcss from '@tailwindcss/vite'; import path from 'path'; add tailwindcss() to the plugins array after react(); add resolve.alias with "@": path.resolve(__dirname, "./src"); extend proxy to include '/invoices': apiTarget and '/images': apiTarget alongside existing '/api' and '/health' entries.

    Update frontend/tsconfig.app.json: in compilerOptions, add "baseUrl": "." and "paths": { "@/*": ["./src/*"] }. The paths alias is required by shadcn — it will fail to init without it. Find the existing compilerOptions block and add these two fields to it; do not replace any existing settings.
  </action>
  <verify>
    <automated>cd /Users/maximolambruschini/NewCombin/compras_agent/frontend && node -e "const c = require('./tsconfig.app.json'); console.log(c.compilerOptions.paths)" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json dependencies include "react-router" and "@tanstack/react-query" and devDependencies include "tailwindcss" and "@tailwindcss/vite"
    - frontend/vite.config.ts contains the string "tailwindcss()" in the plugins array
    - frontend/vite.config.ts contains the string 'path.resolve(__dirname, "./src")' in resolve.alias
    - frontend/vite.config.ts proxy object contains '/invoices' and '/images' entries
    - frontend/tsconfig.app.json compilerOptions.paths contains "@/*" mapping to ["./src/*"]
    - node_modules/tailwindcss exists: ls frontend/node_modules/tailwindcss exits 0
    - node_modules/react-router exists: ls frontend/node_modules/react-router exits 0
  </acceptance_criteria>
  <done>Packages installed. vite.config.ts has tailwindcss plugin and alias. tsconfig.app.json has @/* paths.</done>
</task>

<task type="auto">
  <name>Task 2: shadcn init + add 9 components + fix index.css</name>
  <files>
    frontend/src/index.css
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/components/ui/input.tsx
    frontend/src/components/ui/label.tsx
    frontend/src/components/ui/select.tsx
    frontend/src/components/ui/table.tsx
    frontend/src/components/ui/alert.tsx
    frontend/src/components/ui/separator.tsx
  </files>
  <read_first>
    - frontend/src/index.css (full current state — note all existing custom properties to preserve)
    - frontend/src/App.tsx (verify current content — do not modify in this plan)
  </read_first>
  <action>
    From the frontend/ directory, run shadcn init with non-interactive defaults: npx shadcn@2 init --defaults. **Use `shadcn@2` (pinned major) instead of `shadcn@latest` to avoid reproducibility risk — `@latest` can change generated files or Tailwind v4 behavior between runs (REVIEWS.md MEDIUM concern).** If shadcn requires interactive input, accept: style=default, base color=neutral, CSS variables=yes. shadcn will write @import "tailwindcss" as the first line of src/index.css and append an @theme inline { ... } block with its color tokens (--background, --foreground, --primary, etc.).

    After shadcn init completes, add all 9 components one at a time (or use a single batched command if shadcn CLI supports it): npx shadcn@2 add button dialog badge input label select table alert separator. **Use `shadcn@2` consistently — same major pin as init.** Each command writes to src/components/ui/. Accept all prompts with defaults.

    After all components are added, edit frontend/src/index.css to restore and extend the custom property tokens. The final index.css structure must be:
    - Line 1: @import "tailwindcss"; (written by shadcn — do not duplicate)
    - @layer base or :root block from shadcn (leave shadcn's @theme inline block untouched)
    - :root { block containing ALL of the following custom properties:
        --text: #6b6375; --text-h: #08060d; --bg: #fff; --border: #e5e4e7; --code-bg: #f4f3ec;
        --sans: system-ui, 'Segoe UI', Roboto, sans-serif;
        --heading: system-ui, 'Segoe UI', Roboto, sans-serif;
        --mono: ui-monospace, Consolas, monospace;
        --accent-admin: #2563eb; --accent-admin-bg: rgba(37, 99, 235, 0.08);
        --destructive-hover: #b91c1c;
        --pending-bg: #fef3c7; --pending-badge: #d97706;
        --surface-secondary: #f8f7fa;
        font: 14px/1.5 var(--sans); color: var(--text); background: var(--bg);
      }
    - #root { min-height: 100svh; box-sizing: border-box; } -- remove width, max-width, text-align, border-inline, display, flex-direction
    Do NOT duplicate @import "tailwindcss" if shadcn already wrote it.
    Do NOT remove shadcn's @theme inline { ... } block — those are shadcn's component color tokens.
    Remove the old --accent: #aa3bff scaffold token (replaced by --accent-admin).
  </action>
  <verify>
    <automated>cd /Users/maximolambruschini/NewCombin/compras_agent/frontend && ls src/components/ui/*.tsx 2>&1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - ls frontend/src/components/ui/ lists: button.tsx, dialog.tsx, badge.tsx, input.tsx, label.tsx, select.tsx, table.tsx, alert.tsx, separator.tsx (9 files minimum)
    - grep -c '@import "tailwindcss"' frontend/src/index.css equals 1 (exactly one, no duplicate)
    - grep -c 'width.*1126px\|text-align.*center' frontend/src/index.css equals 0 (walking skeleton styles removed)
    - grep -c '\-\-accent-admin' frontend/src/index.css is at least 1
    - grep -c '\-\-pending-bg' frontend/src/index.css is at least 1
    - grep -c '#root' frontend/src/index.css is at least 1 and that block contains only min-height and box-sizing
    - npm run build exits 0 from frontend/ (Vite can compile the project with new plugins)
  </acceptance_criteria>
  <done>9 shadcn components present in src/components/ui/. index.css has single Tailwind import, admin tokens, clean #root. npm run build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Package installation; resolved by npm lockfile |
| npx shadcn CLI → local source files | shadcn init/add writes to src/; executor reviews output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | Denial of Service | Frontend build artifact size | accept | Tailwind v4 tree-shakes unused classes; no large bundle risk from this plan |
| T-4-shadcn | Tampering | shadcn component source | accept | Only official shadcn registry used per D-16; no third-party registries; components committed to source so content is auditable |
</threat_model>

<verification>
After Task 2 completes:

1. shadcn components installed: ls frontend/src/components/ui/*.tsx | wc -l — at least 9 files
2. Tailwind import: grep -c '@import "tailwindcss"' frontend/src/index.css — equals 1
3. Admin tokens: grep --count '\-\-accent-admin\|--pending-bg\|--pending-badge' frontend/src/index.css — at least 3
4. Alias works: cat frontend/tsconfig.app.json | grep -A2 '"paths"' shows @/* → ./src/*
5. Build succeeds: cd frontend && npm run build exits 0
6. React Router installed: node -e "require('react-router')" from frontend/ exits 0
7. TanStack Query installed: node -e "require('@tanstack/react-query')" from frontend/ exits 0
</verification>

<success_criteria>
- npm run build (from frontend/) exits 0 with no TypeScript or Vite errors
- 9 shadcn component files exist in frontend/src/components/ui/
- frontend/src/index.css: single @import "tailwindcss", all admin tokens present, #root has only min-height + box-sizing
- frontend/vite.config.ts: tailwindcss() in plugins, @/* alias in resolve.alias, /invoices and /images in proxy
- frontend/tsconfig.app.json: @/* paths alias present in compilerOptions
- react-router and @tanstack/react-query in frontend/node_modules/
</success_criteria>

<output>
After completion, create .planning/phases/04-admin-ui/04-02-SUMMARY.md using the template at @$HOME/.claude/get-shit-done/templates/summary.md
</output>
