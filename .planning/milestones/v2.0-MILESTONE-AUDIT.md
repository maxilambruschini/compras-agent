---
milestone: v2.0
milestone_name: Gastos Bot
audited: 2026-06-02
status: tech_debt
scores:
  requirements: 18/18 satisfied (all wired & code-verified)
  phases: 4/4 complete (P1/P2 passed; P3/P4 human_needed — browser/Twilio UAT deferred by user)
  integration: 4/4 E2E flows wired (PASS)
  flows: 4/4 complete
gaps:
  requirements: []   # none unsatisfied
  integration: []    # no cross-phase wiring gaps
  flows: []          # no broken flows
tech_debt:
  - phase: 01-data-conversation-core / 02-whatsapp-gastos-flow
    items:
      - "WARNING (latent edge case, from integration check): if GPT extracts monto but never extracts concepto across turns, CONFIRM → GastoService.save_gasto(concepto=None) raises IntegrityError (NOT NULL), rolls back safely, logs gastos.process_failed, but the manager gets no recovery reply and the gasto is lost. Hardening: add a concepto-None guard in _handle_confirm that re-prompts (mirrors CONV-06). Safe failure (no crash/corruption); happy path unaffected."
  - phase: 03-prompt-trigger-endpoint
    items:
      - "Deferred human UAT: live Twilio sandbox round-trip (POST /gastos/prompt → WhatsApp prompt delivered → reply → CajaCierre row). All code paths automated-verified; only network delivery requires a human. See 03-VERIFICATION.md."
      - "Nyquist: 03-VALIDATION.md frontmatter still nyquist_compliant:false / wave_0_complete:false though all 16 tests are green — post-execution flag flip not applied (cosmetic)."
  - phase: 04-admin-ui
    items:
      - "Deferred human UAT: browser rendering (gastos list + filter/search, gasto detail + ticket image, cierres read-only, loading/empty/error states). Frontend builds + lints clean; no test runner by design. Docker users must `docker compose build frontend` first. See 04-VERIFICATION.md."
      - "Advisory UI polish (04-UI-REVIEW.md, 20/24): dark-mode error-banner alpha; three off-scale 20px spacings vs the 8-point scale; filter bar visible during loading. Non-blocking."
      - "WR-03 (accepted): admin read endpoints have no auth — locked PROJECT.md decision (deferred to v3). Must be addressed before real company data is exposed beyond the demo."
      - "Nyquist: 04-VALIDATION.md frontmatter still nyquist_compliant:false (cosmetic; backend tests green)."
nyquist:
  compliant_phases: ["01"]
  partial_phases: ["03", "04"]   # VALIDATION.md exists, frontmatter flag not flipped though tests green
  missing_phases: ["02"]          # no VALIDATION.md (Phase 2 predates the validation step)
  overall: "partial — functionally validated by green suites; frontmatter flags not maintained"
---

# v2.0 Gastos Bot — Milestone Audit

## Verdict: tech_debt (no blockers; accumulated deferred items)

All 18 milestone requirements are satisfied and verified wired end-to-end by the integration
check. The full backend suite is green (184 passed, 1 skipped) and the frontend builds + lints
clean. There are **no critical gaps, no unsatisfied requirements, and no broken cross-phase
flows.** The `tech_debt` status reflects intentionally-deferred human UAT (live Twilio + browser),
one latent edge-case warning, advisory UI polish, and the accepted no-auth demo decision — none
of which block milestone completion. The user explicitly chose to proceed to the lifecycle with
these items deferred.

## Requirements Coverage (18/18)

| Group | IDs | Phase | Status |
|-------|-----|-------|--------|
| Conversation engine | CONV-01..06 | 1 | ✅ satisfied (passed + wired) |
| Gasto capture | GASTO-01,02,04,05,06 | 1 | ✅ satisfied |
| Ticket capture | GASTO-03 | 2 | ✅ satisfied (traceability checkbox corrected during audit) |
| Caja closing | CAJA-01,02 | 3 | ✅ satisfied (automated; live UAT deferred) |
| Prompt trigger | TRIG-01,02 | 3 | ✅ satisfied (automated; live UAT deferred) |
| Admin UI | UI-01,02 | 4 | ✅ satisfied (backend automated; browser UAT deferred) |

3-source note: SUMMARY `requirements_completed` frontmatter was left empty `[]` across all phases
(executors didn't populate it) — coverage was instead confirmed via VERIFICATION.md statuses +
the REQUIREMENTS traceability table + the integration check's per-requirement wiring map.

## Phases (4/4)

| Phase | Verification | Notes |
|-------|--------------|-------|
| 1 Data + Conversation Core | passed | — |
| 2 WhatsApp Gastos Flow | passed | — |
| 3 Prompt Trigger Endpoint | human_needed | 7/8 must-haves automated-verified; live Twilio deferred |
| 4 Admin UI | human_needed | 8/8 automatable verified; browser rendering deferred |

## Integration (PASS — 4/4 flows)

1. Reactive gasto capture (webhook → orchestrator → Gasto → /api/gastos) — WIRED
2. Caja closing via prompt (/gastos/prompt → AWAITING_CIERRE → CajaCierre → /api/cierres) — WIRED
3. Shared conversation state + FOR NO KEY UPDATE lock coexistence — WIRED
4. Committed-only boundary (admin never exposes conversations.draft_gasto) — WIRED

## Tech Debt Summary

See the YAML `tech_debt` block above. Recommended follow-ups for a future milestone:
the concepto-None guard (hardening), admin auth (before non-demo data), and the advisory UI polish.
All deferred human-UAT items are documented in their phase VERIFICATION.md files.
