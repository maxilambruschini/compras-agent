---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/requirements-dev.txt
  - backend/pyproject.toml
  - backend/app/__init__.py
  - backend/app/config.py
  - backend/app/db/__init__.py
  - backend/app/db/models.py
  - backend/app/db/engine.py
  - backend/app/db/session.py
  - backend/app/models/__init__.py
  - backend/app/models/extraction.py
  - backend/app/routers/__init__.py
  - backend/app/services/__init__.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/alembic/versions/.gitkeep
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_config.py
  - backend/tests/test_db.py
  - backend/tests/test_extraction_models.py
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - INF-01
  - INF-03
tags:
  - foundation
  - sqlalchemy
  - alembic
  - pydantic
  - python

must_haves:
  truths:
    - "Pydantic Settings raises ValidationError when DATABASE_URL or OPENAI_API_KEY is missing from environment"
    - "ExtractedInvoice can be instantiated with all fields None and TipoComprobante.UNKNOWN"
    - "SQLAlchemy Base.metadata contains 'invoices', 'invoice_line_items', and 'sender_allowlist' tables"
    - "Alembic autogenerate produces a migration that creates all three tables"
    - "pytest test suite runs and all Phase-1 unit/integration tests pass against SQLite+aiosqlite test DB"
  artifacts:
    - path: "backend/app/config.py"
      provides: "Settings BaseSettings class with required fields"
      contains: "class Settings(BaseSettings)"
    - path: "backend/app/db/models.py"
      provides: "Invoice, InvoiceLineItem, SenderAllowlist SQLAlchemy ORM models with Base"
      contains: "class Base(DeclarativeBase)"
    - path: "backend/app/db/engine.py"
      provides: "Lazy AsyncEngine factory and AsyncSessionLocal factory (get_engine, get_async_session_local)"
      contains: "def get_engine"
    - path: "backend/app/db/session.py"
      provides: "get_db FastAPI dependency"
      contains: "async def get_db"
    - path: "backend/app/models/extraction.py"
      provides: "ExtractedInvoice, LineItem, TipoComprobante (str, Enum)"
      contains: "class TipoComprobante(str, Enum)"
    - path: "backend/alembic/env.py"
      provides: "Async-compatible Alembic env with asyncio.run + NullPool"
      contains: "async_engine_from_config"
    - path: "backend/alembic/versions/*_initial_schema.py"
      provides: "Autogenerated initial migration creating all three tables (Postgres DDL)"
      contains: "op.create_table('invoices'"
    - path: "backend/tests/test_config.py"
      provides: "INF-03 tests — missing env var raises, .env load succeeds"
      contains: "def test_missing_env_raises"
    - path: "backend/tests/test_db.py"
      provides: "INF-01 tests — allowlist table + CRUD"
      contains: "def test_allowlist"
    - path: "backend/tests/test_extraction_models.py"
      provides: "All-None instantiation + UNKNOWN enum test"
      contains: "def test_all_optional"
  key_links:
    - from: "backend/alembic/env.py"
      to: "backend/app/db/models.py"
      via: "from app.db.models import Base import"
      pattern: "from app.db.models import Base"
    - from: "backend/app/db/engine.py"
      to: "backend/app/config.py"
      via: "get_settings() called lazily inside get_engine() factory (NOT at module import)"
      pattern: "def get_engine"
    - from: "backend/tests/conftest.py"
      to: "backend/app/db/models.py"
      via: "Base.metadata.create_all on test engine"
      pattern: "Base\\.metadata"
---

<objective>
Establish the Phase 1 data contract: the Pydantic settings, SQLAlchemy ORM schema, Pydantic extraction models, and Alembic async migration scaffold — plus the Wave 0 pytest test suite that proves INF-01 (allowlist table) and INF-03 (env var fail-fast) are satisfied.

Purpose: Lock the data contract every subsequent phase depends on, and prove it works with automated tests before Docker wiring is attempted in Plan 02.

Output: Backend package skeleton (`backend/app/**`), Alembic scaffold (`backend/alembic/**`), one autogenerated migration creating all three tables, and a green pytest suite covering INF-01, INF-03, and the extraction model contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-REVIEWS.md
@.planning/phases/01-foundation/01-SKELETON.md

<interfaces>
<!-- All interface contracts originate in 01-RESEARCH.md and 01-PATTERNS.md (canonical excerpts).
     The executor MUST read those two files before writing any Python; they contain the exact
     module/class/function signatures, including async env.py wiring, mapped_column typing,
     and the use_enum_values config required for OpenAI Structured Outputs in Phase 2.
     CRITICAL: per 01-REVIEWS.md, get_settings() MUST be invoked lazily — never at module
     import time in engine.py — so that pytest env_setup fixture can patch env vars first. -->

Settings (backend/app/config.py) required fields (no defaults — INF-03):
  database_url: str            # postgresql+asyncpg://user:pass@host:5432/db
  openai_api_key: str
  whatsapp_token: str
  whatsapp_phone_number_id: str
  whatsapp_verify_token: str
Settings optional (with defaults):
  debug: bool = False
  log_level: str = "INFO"
  confidence_threshold: float = 0.85
Module exports: class Settings, get_settings() -> Settings (lru_cache)

SQLAlchemy (backend/app/db/models.py):
  class Base(DeclarativeBase)
  class Invoice(Base) __tablename__ = "invoices"  — see RESEARCH.md Pattern 5 for full column list
  class InvoiceLineItem(Base) __tablename__ = "invoice_line_items" — FK invoices.id ON DELETE CASCADE
  class SenderAllowlist(Base) __tablename__ = "sender_allowlist" — UNIQUE(phone_number)
  UUID columns use dialect-agnostic `sqlalchemy.Uuid` (NOT `sqlalchemy.dialects.postgresql.UUID`)
    — required so aiosqlite test backend can bind UUID values; asyncpg in prod still maps native.
Module exports: Base, Invoice, InvoiceLineItem, SenderAllowlist

Engine + Session (backend/app/db/engine.py, backend/app/db/session.py):
  Lazy factories (NO module-level Settings()/engine instantiation):
    def get_engine() -> AsyncEngine   (memoized singleton; calls get_settings() inside)
    def get_async_session_local() -> async_sessionmaker[AsyncSession]
  Both use a private module-level `_engine = None` / `_session_local = None` and
  initialize on first call. expire_on_commit=False is mandatory.
  async def get_db() -> AsyncGenerator[AsyncSession, None]  — uses get_async_session_local()

Pydantic extraction (backend/app/models/extraction.py):
  class TipoComprobante(str, Enum)  values FACTURA_A/B/C, REMITO, LISTA_INFORMAL, UNKNOWN
  class LineItem(BaseModel) — all fields Optional[T] = None, model_config = ConfigDict(use_enum_values=True)
  class ExtractedInvoice(BaseModel) — all fields Optional[T] = None, line_items: List[LineItem] = []
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend package skeleton + Settings + extraction models + DB stubs + Wave 0 tests</name>
  <files>
    backend/requirements.txt,
    backend/requirements-dev.txt,
    backend/pyproject.toml,
    backend/app/__init__.py,
    backend/app/config.py,
    backend/app/db/__init__.py,
    backend/app/db/models.py,
    backend/app/models/__init__.py,
    backend/app/models/extraction.py,
    backend/app/routers/__init__.py,
    backend/app/services/__init__.py,
    backend/tests/__init__.py,
    backend/tests/conftest.py,
    backend/tests/test_config.py,
    backend/tests/test_extraction_models.py,
    .env.example,
    .gitignore
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md,
    .planning/phases/01-foundation/01-PATTERNS.md,
    .planning/phases/01-foundation/01-CONTEXT.md,
    .planning/phases/01-foundation/01-VALIDATION.md,
    .planning/phases/01-foundation/01-REVIEWS.md,
    CLAUDE.md
  </read_first>
  <action>
    Create the `backend/` Python package skeleton, the Wave 0 test infrastructure, AND a minimal `app/db/models.py` stub that conftest.py can import. The stub addresses the HIGH conftest ordering bug flagged in 01-REVIEWS.md: pytest loads conftest at collection time, so `from app.db.models import Base` must resolve even though no DB tests run in this task.

    1) `backend/requirements.txt` — PRODUCTION-ONLY dependencies, pinned exactly per RESEARCH.md Standard Stack:
       fastapi==0.136.1, uvicorn==0.38.0, pydantic==2.13.4, pydantic-settings==2.14.1,
       sqlalchemy==2.0.49, asyncpg==0.31.0, alembic==1.18.4, structlog==25.5.0,
       python-dotenv==1.2.2, openai==2.36.0.
       Do NOT include aiosqlite, pytest, pytest-asyncio, or httpx here — those go in requirements-dev.txt (keeps the production image lean per REVIEWS.md LOW suggestion).

    2) `backend/requirements-dev.txt` — test-only deps (no version pin, latest at install):
       `-r requirements.txt` on line 1 so dev install gets prod + test in one command,
       then: pytest, pytest-asyncio, aiosqlite, httpx.

    3) `backend/pyproject.toml` minimal config — `[tool.pytest.ini_options]` with `asyncio_mode = "auto"` and `testpaths = ["tests"]`. No build-system needed.

    4) `backend/app/__init__.py`, `backend/app/db/__init__.py`, `backend/app/models/__init__.py`, `backend/app/routers/__init__.py`, `backend/app/services/__init__.py` — empty package markers (D-02 directory layout).

    5) `backend/app/config.py` — implement Settings exactly per RESEARCH.md Pattern 4 (canonical excerpt in 01-PATTERNS.md). Required fields with NO default: `database_url`, `openai_api_key`, `whatsapp_token`, `whatsapp_phone_number_id`, `whatsapp_verify_token`. Optional with defaults: `debug=False`, `log_level="INFO"`, `confidence_threshold=0.85`. `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", case_sensitive=False)`. Provide `@lru_cache def get_settings() -> Settings`. This satisfies INF-03.

    6) `backend/app/db/models.py` — MINIMAL STUB ONLY in this task. Define just enough to let conftest.py import without ModuleNotFoundError (REVIEWS.md HIGH fix):
       - `from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column`
       - `from sqlalchemy import String, Boolean, Integer`
       - `class Base(DeclarativeBase): pass`
       - `class SenderAllowlist(Base): __tablename__ = "sender_allowlist"; id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True); phone_number: Mapped[str] = mapped_column(String(30), unique=True, nullable=False)`
       Task 2 in this plan will REPLACE this stub with the full schema (Invoice + InvoiceLineItem + full SenderAllowlist columns). The stub is sufficient for conftest's `Base.metadata.create_all` to succeed and for Task 1's tests (which do not exercise DB models) to collect.

    7) `backend/app/models/extraction.py` — implement per RESEARCH.md Pattern 6. `class TipoComprobante(str, Enum)` with values FACTURA_A, FACTURA_B, FACTURA_C, REMITO, LISTA_INFORMAL, UNKNOWN (D-08). `class LineItem(BaseModel)` and `class ExtractedInvoice(BaseModel)` — every field `Optional[T] = None` per EXT-06 (NEVER omit `= None`; Pydantic v2 does not infer default from Optional — see RESEARCH.md Pitfall 4). `line_items: List[LineItem] = []`. `model_config = ConfigDict(use_enum_values=True)` on both Pydantic classes (required for OpenAI Structured Outputs compatibility in Phase 2). D-07 nesting (header + line_items list).

    8) `backend/tests/__init__.py` — empty.

    9) `backend/tests/conftest.py` — REVIEWS.md HIGH fix for test isolation. Implement:
       - A `session`-scoped, `autouse=True` `env_setup` fixture using `monkeypatch.setenv` via `pytest.MonkeyPatch.context()` (since `monkeypatch` is function-scoped, use `pytest.MonkeyPatch()` directly in a session fixture; remember to `mp.undo()` in teardown). Set dummy values for DATABASE_URL (sqlite+aiosqlite:///:memory:), OPENAI_API_KEY, WHATSAPP_TOKEN, WHATSAPP_PHONE_NUMBER_ID, WHATSAPP_VERIFY_TOKEN. After setting env vars, call `from app.config import get_settings; get_settings.cache_clear()` so any prior cached Settings is invalidated.
       - An async aiosqlite engine fixture (`scope="function"`): `create_async_engine("sqlite+aiosqlite:///:memory:")`, run `Base.metadata.create_all` via `engine.begin()` + `conn.run_sync(Base.metadata.create_all)` on entry; dispose on exit.
       - A `db_session` AsyncSession fixture: `async_sessionmaker(engine, expire_on_commit=False, class_=AsyncSession)` per test; yield session inside `async with`; rollback/close in teardown.
       - Import `Base` via `from app.db.models import Base` (resolves because Task 1 ships the stub).

    10) `backend/tests/test_config.py` — covers INF-03:
        - `test_missing_env_raises(monkeypatch)`: unset all 5 required env vars via `monkeypatch.delenv(..., raising=False)`, call `get_settings.cache_clear()`, then assert `Settings()` raises `pydantic.ValidationError`. Use `tmp_path` + `monkeypatch.chdir(tmp_path)` to ensure no `.env` is picked up from repo root.
        - `test_settings_load(monkeypatch)`: set required env vars via monkeypatch, call `get_settings.cache_clear()`, `Settings()` constructs and exposes all 5 required fields; `confidence_threshold` defaults to 0.85.

    11) `backend/tests/test_extraction_models.py` — covers EXT-06 + D-08:
        - `test_all_optional`: `ExtractedInvoice()` (no args) instantiates; all fields are None; `line_items == []`.
        - `test_unknown_enum`: `TipoComprobante.UNKNOWN.value == "UNKNOWN"`; `ExtractedInvoice(tipo_comprobante=TipoComprobante.UNKNOWN).tipo_comprobante` equals the string "UNKNOWN" (because `use_enum_values=True`).
        - `test_line_item_optional`: `LineItem()` instantiates with all None.

    12) `.env.example` at repo root — lists all 5 required env vars (DATABASE_URL example using `postgresql+asyncpg://compras:compras@postgres:5432/compras`, OPENAI_API_KEY, WHATSAPP_TOKEN, WHATSAPP_PHONE_NUMBER_ID, WHATSAPP_VERIFY_TOKEN) plus optional ones (DEBUG, LOG_LEVEL, CONFIDENCE_THRESHOLD, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB). Placeholder values only.

    13) `.gitignore` at repo root — ignore `.env`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `node_modules/`, `frontend/dist/`, `.venv/`, `backend/alembic/versions/__pycache__/`, `backend/_autogen.db`.

    Use existing patterns from RESEARCH.md Pattern 4 (config), Pattern 6 (extraction models), and the Wave 0 list in 01-VALIDATION.md. Do not deviate from pinned versions.
  </action>
  <verify>
    <automated>cd backend && python -m pip install -r requirements-dev.txt --quiet && pytest tests/test_config.py tests/test_extraction_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/requirements.txt` contains lines: `fastapi==0.136.1`, `pydantic==2.13.4`, `pydantic-settings==2.14.1`, `sqlalchemy==2.0.49`, `alembic==1.18.4`, `asyncpg==0.31.0`, `openai==2.36.0`.
    - `backend/requirements.txt` does NOT contain `aiosqlite`, `pytest`, `pytest-asyncio`, or `httpx` (test deps belong in requirements-dev.txt).
    - `backend/requirements-dev.txt` exists and contains the strings `pytest`, `pytest-asyncio`, `aiosqlite`, `httpx`, and `-r requirements.txt`.
    - `backend/pyproject.toml` contains `asyncio_mode = "auto"` under `[tool.pytest.ini_options]`.
    - `grep -c "Optional" backend/app/models/extraction.py` returns >= 14.
    - `grep -c "= None$" backend/app/models/extraction.py` returns >= 14 (every Optional has explicit `= None`).
    - `grep -c "use_enum_values=True" backend/app/models/extraction.py` returns >= 2.
    - `grep -c "class TipoComprobante(str, Enum)" backend/app/models/extraction.py` equals 1.
    - `grep -c "UNKNOWN" backend/app/models/extraction.py` >= 1.
    - `grep -c "class Base(DeclarativeBase)" backend/app/db/models.py` equals 1 (stub present so conftest can import).
    - `grep -c "class SenderAllowlist(Base)" backend/app/db/models.py` equals 1 (stub present).
    - `python -c "import os; [os.environ.pop(k, None) for k in ['DATABASE_URL','OPENAI_API_KEY','WHATSAPP_TOKEN','WHATSAPP_PHONE_NUMBER_ID','WHATSAPP_VERIFY_TOKEN']]; from app.config import Settings, get_settings; get_settings.cache_clear(); Settings()"` (run from `backend/`, with `.env` absent or moved) exits non-zero with `ValidationError`.
    - `pytest backend/tests/test_config.py backend/tests/test_extraction_models.py -x -q` exits 0 with 5 tests passing (conftest collects cleanly because `app.db.models` stub exists).
    - `.env.example` exists at repo root and contains the strings `DATABASE_URL=`, `OPENAI_API_KEY=`, `WHATSAPP_TOKEN=`.
    - `.gitignore` exists at repo root and contains the string `.env`.
  </acceptance_criteria>
  <done>
    Backend package skeleton, Wave 0 test infrastructure, and the `app.db.models` import stub committed. Settings fail-fast verified (INF-03 unit-test green). Extraction model contract verified (EXT-06 unit-test green). conftest.py collects without ModuleNotFoundError. Test isolation guaranteed via session-scoped autouse env fixture + `get_settings.cache_clear()`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full ORM schema (Uuid + Invoice + LineItem) + lazy engine + session + Alembic + Postgres-DDL migration</name>
  <files>
    backend/app/db/models.py,
    backend/app/db/engine.py,
    backend/app/db/session.py,
    backend/alembic.ini,
    backend/alembic/env.py,
    backend/alembic/script.py.mako,
    backend/alembic/versions/.gitkeep,
    backend/tests/test_db.py
  </files>
  <read_first>
    backend/app/config.py,
    backend/app/db/models.py,
    backend/app/models/extraction.py,
    backend/tests/conftest.py,
    .planning/phases/01-foundation/01-RESEARCH.md,
    .planning/phases/01-foundation/01-PATTERNS.md,
    .planning/phases/01-foundation/01-REVIEWS.md
  </read_first>
  <action>
    Replace the Task 1 stub with the full SQLAlchemy ORM schema, add the lazy async engine + session dependency, scaffold Alembic, and generate the initial migration against a real Postgres container (REVIEWS.md MEDIUM fix — SQLite autogenerate produces wrong DDL).

    1) `backend/app/db/models.py` — REPLACE Task 1 stub with full implementation per RESEARCH.md Pattern 5. Use SQLAlchemy 2.0 typed mapping: `class Base(DeclarativeBase)`, `Mapped[...]` + `mapped_column(...)`.

       CRITICAL UUID FIX (REVIEWS.md MEDIUM): Use `from sqlalchemy import Uuid` (dialect-agnostic, SQLAlchemy 2.0+). Do NOT import `from sqlalchemy.dialects.postgresql import UUID`. The `Uuid` type renders as native UUID in Postgres (via asyncpg) and as CHAR(32) in SQLite (via aiosqlite), with Python `uuid.UUID` round-tripping on both backends.

       Models:
       - `class Invoice(Base) __tablename__ = "invoices"`. PK `id: Mapped[uuid.UUID] = mapped_column(Uuid, primary_key=True, default=uuid.uuid4)`. Nullable document header columns: tipo_comprobante (String 50), numero_documento (String 100), proveedor (String 255), fecha (Date), cuit_proveedor (String 13), cae (String 20), fecha_vencimiento_cae (Date). Processing metadata: confidence_score (Numeric(4,3)), status (String 30, default "pending_review", NOT NULL), whatsapp_message_id (String 100), sender_phone (String 30), image_path (Text), raw_extraction (Text). Timestamps: created_at, updated_at (DateTime(timezone=True) with server_default=func.now(); updated_at also onupdate=func.now()). Relationship `line_items: Mapped[List["InvoiceLineItem"]] = relationship(back_populates="invoice", cascade="all, delete-orphan")`. `__table_args__` includes `Index("ix_invoices_numero_documento_proveedor", "numero_documento", "proveedor")`, `Index("ix_invoices_status", "status")`, `Index("ix_invoices_created_at", "created_at")`.
       - `class InvoiceLineItem(Base) __tablename__ = "invoice_line_items"`. PK integer autoincrement. `invoice_id: Mapped[uuid.UUID] = mapped_column(Uuid, ForeignKey("invoices.id", ondelete="CASCADE"))`. Optional columns: descripcion (Text), codigo_sku (String 100), bultos / unidades_por_bulto (Numeric(12,4)), precio_unitario_sin_iva (Numeric(14,4)), descuento_pct / iva_rate (Numeric(6,4)), percepciones_iibb (Numeric(14,4)). `invoice: Mapped["Invoice"] = relationship(back_populates="line_items")`.
       - `class SenderAllowlist(Base) __tablename__ = "sender_allowlist"`. PK integer autoincrement. `phone_number: Mapped[str] = mapped_column(String(30), unique=True, nullable=False)`. `display_name: Mapped[Optional[str]] = mapped_column(String(100))`. `is_active: Mapped[bool] = mapped_column(Boolean, default=True)`. `created_at` with server_default func.now(). This satisfies INF-01.

       No `sqlalchemy.dialects.postgresql.UUID` import anywhere in this file.

    2) `backend/app/db/engine.py` — REVIEWS.md HIGH fix: LAZY initialization. Do NOT call `get_settings()` or `create_async_engine()` at module import time. Pattern:
       - Imports: `from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker, AsyncSession, AsyncEngine`, `from app.config import get_settings`.
       - Module-level private singletons: `_engine: AsyncEngine | None = None`, `_session_local: async_sessionmaker[AsyncSession] | None = None`.
       - `def get_engine() -> AsyncEngine`: if `_engine is None`, call `settings = get_settings()` inside the function, build `_engine = create_async_engine(settings.database_url, echo=settings.debug, pool_pre_ping=True)`, then return `_engine`. Use `global _engine`.
       - `def get_async_session_local() -> async_sessionmaker[AsyncSession]`: if `_session_local is None`, build `_session_local = async_sessionmaker(get_engine(), expire_on_commit=False, class_=AsyncSession)`. Use `global _session_local`. `expire_on_commit=False` mandatory (RESEARCH.md Pitfall 3).
       - Optional helper `def reset_engine_for_tests() -> None`: sets both globals to None and disposes engine if present — useful if any test needs to swap DATABASE_URL mid-suite.

    3) `backend/app/db/session.py` — `async def get_db() -> AsyncGenerator[AsyncSession, None]`. Call `session_local = get_async_session_local()` inside the function (lazy). Yield a session via `async with session_local() as session: yield session` (handles close).

    4) `backend/alembic.ini` — generate via `alembic init alembic` from `backend/`, then edit:
       - Set `script_location = alembic`.
       - Leave `sqlalchemy.url =` empty / placeholder (env.py overrides from `DATABASE_URL` env var per RESEARCH.md Pitfall 6).
       - Keep default logging config.

    5) `backend/alembic/env.py` — REPLACE default sync template with async version from RESEARCH.md Pattern 1. MUST include:
       - `import asyncio`
       - `from sqlalchemy.ext.asyncio import async_engine_from_config`
       - `from sqlalchemy import pool` and `poolclass=pool.NullPool` in `async_engine_from_config` (RESEARCH.md Pattern 1 critical detail; missing this hangs migrations).
       - `from app.db.models import Base  # noqa: F401` BEFORE `target_metadata = Base.metadata` (RESEARCH.md Pitfall 1).
       - URL override: `import os; database_url = os.environ.get("DATABASE_URL"); if database_url: config.set_main_option("sqlalchemy.url", database_url)` (RESEARCH.md Pitfall 6).
       - `async def run_async_migrations()` calling `await connection.run_sync(do_run_migrations)` and `await connectable.dispose()`.
       - `def run_migrations_online()` doing `asyncio.run(run_async_migrations())`.
       - Standard `run_migrations_offline()` block.

    6) `backend/alembic/script.py.mako` — keep default template generated by `alembic init`.

    7) `backend/alembic/versions/.gitkeep` — empty file.

    8) Generate the initial migration AGAINST POSTGRES (REVIEWS.md MEDIUM fix — SQLite produces wrong DDL types for UUID/Numeric/Date). Procedure:
       a) Ensure docker-compose.yml exists from Plan 02 OR start a throwaway Postgres:
          `docker run --rm -d --name compras-alembic-pg -p 5433:5432 -e POSTGRES_USER=compras -e POSTGRES_PASSWORD=compras -e POSTGRES_DB=compras postgres:16-alpine`
          (If Plan 02 hasn't run yet — which is the case in Wave 1 — use the throwaway container approach. If executing this plan after Plan 02 exists, the alternative `docker compose up -d postgres --wait` also works.)
       b) Wait for readiness: `until docker exec compras-alembic-pg pg_isready -U compras > /dev/null 2>&1; do sleep 1; done`.
       c) From `backend/`, run: `DATABASE_URL=postgresql+asyncpg://compras:compras@localhost:5433/compras alembic revision --autogenerate -m "initial_schema"`.
       d) Stop & remove the throwaway: `docker rm -f compras-alembic-pg`.
       e) Resulting `backend/alembic/versions/<hash>_initial_schema.py` MUST contain `op.create_table('invoices', ...)`, `op.create_table('invoice_line_items', ...)`, `op.create_table('sender_allowlist', ...)` with Postgres-native types (`sa.UUID()`, `sa.Numeric(...)`, `sa.DateTime(timezone=True)`, etc.) — NOT SQLite CHAR/REAL fallbacks.

       This addresses both the SQLite-DDL-divergence MEDIUM concern and avoids needing to hand-edit the autogenerated migration.

    9) `backend/tests/test_db.py` — covers INF-01:
        - `test_allowlist_table_exists`: assert `"sender_allowlist" in Base.metadata.tables`; assert it has columns `phone_number`, `display_name`, `is_active`, `created_at`.
        - `test_allowlist_crud(db_session)`: insert a `SenderAllowlist(phone_number="+5491100000000", display_name="Test")` via `db_session.add` + `await db_session.commit()`. Query back via `select(SenderAllowlist).where(...)`. Assert retrieved row has correct phone_number and `is_active is True`.
        - `test_invoice_relationship(db_session)`: insert an `Invoice()` (all-None header is acceptable; status defaults to "pending_review"), add two `InvoiceLineItem` rows referencing its id, commit, query the invoice with `selectinload(Invoice.line_items)`, assert `len(invoice.line_items) == 2`. UUID PK round-trips correctly via `sqlalchemy.Uuid` (Python uuid.UUID in, uuid.UUID out — works on both aiosqlite and asyncpg).

    Reference RESEARCH.md Pitfalls 1, 3, 6 and REVIEWS.md HIGH/MEDIUM findings throughout.
  </action>
  <verify>
    <automated>cd backend && pytest tests/ -x -q && python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; cfg = Config('alembic.ini'); cfg.set_main_option('script_location', 'alembic'); s = ScriptDirectory.from_config(cfg); heads = list(s.get_revisions(s.get_heads())); assert len(heads) == 1, f'Expected 1 head revision, got {len(heads)}'; print('alembic head:', heads[0].revision)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class Base(DeclarativeBase)" backend/app/db/models.py` equals 1.
    - `grep -c "__tablename__ = \"invoices\"" backend/app/db/models.py` equals 1.
    - `grep -c "__tablename__ = \"invoice_line_items\"" backend/app/db/models.py` equals 1.
    - `grep -c "__tablename__ = \"sender_allowlist\"" backend/app/db/models.py` equals 1.
    - `grep -c "ondelete=\"CASCADE\"" backend/app/db/models.py` >= 1.
    - `grep -c "ix_invoices_numero_documento_proveedor" backend/app/db/models.py` >= 1.
    - `grep -c "from sqlalchemy import.*Uuid\|from sqlalchemy.types import.*Uuid" backend/app/db/models.py` >= 1 (dialect-agnostic Uuid imported).
    - `grep -c "from sqlalchemy.dialects.postgresql import UUID" backend/app/db/models.py` equals 0 (REVIEWS.md MEDIUM fix).
    - `grep -c "expire_on_commit=False" backend/app/db/engine.py` equals 1.
    - `grep -c "def get_engine" backend/app/db/engine.py` >= 1 (lazy factory present — REVIEWS.md HIGH fix).
    - `grep -c "^settings = get_settings()\|^engine = create_async_engine" backend/app/db/engine.py` equals 0 (NO module-level Settings()/engine instantiation).
    - `grep -c "async def get_db" backend/app/db/session.py` equals 1.
    - `grep -c "pool.NullPool" backend/alembic/env.py` >= 1.
    - `grep -c "from app.db.models import Base" backend/alembic/env.py` >= 1.
    - `grep -c "asyncio.run" backend/alembic/env.py` >= 1.
    - `grep -c "DATABASE_URL" backend/alembic/env.py` >= 1.
    - Exactly one file matching `backend/alembic/versions/*_initial_schema.py` exists.
    - That migration file contains `op.create_table('invoices'`, `op.create_table('invoice_line_items'`, `op.create_table('sender_allowlist'`.
    - That migration file contains `sa.Uuid` or `sa.UUID` (Postgres-native UUID DDL, not SQLite CHAR(32) fallback).
    - That migration file does NOT contain `def upgrade() -> None:\n    pass` (RESEARCH.md Pitfall 1 — empty migration indicates Base import missing).
    - `pytest backend/tests/ -x -q` exits 0 with all Phase-1 tests passing (config + extraction_models from Task 1, db tests from Task 2).
    - `python -c "from app.db.models import Base; print(sorted(Base.metadata.tables.keys()))"` (run from backend/ with PYTHONPATH=.) prints `['invoice_line_items', 'invoices', 'sender_allowlist']`.
  </acceptance_criteria>
  <done>
    Full SQLAlchemy ORM schema (Invoice, InvoiceLineItem, SenderAllowlist) defined per RESEARCH.md Pattern 5 using dialect-agnostic `sqlalchemy.Uuid`. Lazy async engine (no import-time Settings() call) and get_db dependency available. Alembic async env.py works (no NullPool / Base import / URL override pitfalls). One Postgres-DDL initial migration creates all three tables. INF-01 covered by passing pytest. Data contract ready for Plan 02.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env → app | Required secrets cross from OS env / `.env` into `Settings`; treated as trusted but MUST never appear in logs or source. |
| developer disk → repo | `.env` contains real secrets locally; must never enter git history. |
| test DB → prod DB | Tests use in-memory aiosqlite; production uses asyncpg. No data crossover. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `backend/app/config.py` Settings | mitigate | Required-field BaseSettings (no defaults on secrets); `.env` listed in `.gitignore`; `.env.example` ships placeholder strings only. INF-03 satisfied by ValidationError on missing var. Test `test_missing_env_raises` proves fail-fast. |
| T-01-02 | Information Disclosure | log output (structlog) | mitigate | Never log `settings.database_url`, `openai_api_key`, or `whatsapp_token`. main.py log call in Plan 02 emits only `log_level`. No secret-bearing log statements in this plan. |
| T-01-03 | Tampering | SQLAlchemy queries on `sender_allowlist` | mitigate | All inserts/queries use ORM parameterized statements (`session.add`, `select(...).where(...)`). No raw string SQL concatenation anywhere in `models.py`, `engine.py`, `session.py`, or tests. |
| T-01-04 | Denial of Service | Alembic migration hang on container boot | mitigate | `pool.NullPool` in `env.py` per RESEARCH.md Pattern 1 critical detail; migrations run via Docker entrypoint command (Plan 02), not FastAPI lifespan (RESEARCH.md anti-pattern, alembic#1483). |
| T-01-05 | Spoofing | (deferred — INF-02 webhook HMAC is Phase 3) | accept | Out of phase scope. |
| T-01-06 | Repudiation | invoice writes lack audit trail | accept | `created_at` / `updated_at` server_default columns on every table provide minimal audit. Full audit log out of v1 scope. |
| T-01-07 | Tampering | Lazy engine factory could be invoked before env vars set (in tests) | mitigate | conftest session-scoped autouse `env_setup` runs before any test collection; `get_settings.cache_clear()` invoked after env patching so first `get_engine()` call sees correct DATABASE_URL. |

</threat_model>

<verification>
- `pytest backend/tests/ -x -q` exits 0 (all 8+ tests green).
- `grep -L "ValidationError" backend/tests/test_config.py` returns nothing.
- One migration file exists in `backend/alembic/versions/` whose body contains Postgres-native UUID DDL.
- `grep -c "from sqlalchemy.dialects.postgresql import UUID" backend/app/db/models.py` equals 0.
- `grep -c "^settings = get_settings()" backend/app/db/engine.py` equals 0 (no module-level eager init).
- `git diff --stat HEAD` shows new files only — no modifications to `.planning/` artifacts.
</verification>

<success_criteria>
- INF-03 satisfied: app refuses to start when required env vars are missing (proven by `test_missing_env_raises`).
- INF-01 satisfied: `sender_allowlist` table is defined in ORM and created by the initial Alembic migration; CRUD proven by `test_allowlist_crud`.
- D-04, D-06, D-08, D-09 honored.
- ExtractedInvoice + LineItem can be imported and instantiated with all-None per EXT-06.
- All REVIEWS.md HIGH/MEDIUM concerns addressed: conftest import-stub, lazy engine factory, dialect-agnostic Uuid, Postgres-DDL autogenerate, requirements-dev split.
- The data contract is locked — Plan 02 can wire Docker + FastAPI without revisiting model definitions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` capturing: files created, decisions honored (D-02, D-04, D-06, D-08, D-09), REVIEWS.md fixes applied (conftest stub, lazy engine, Uuid, Postgres-DDL, requirements-dev), version pins applied, test count green, and any deviations from canonical RESEARCH.md/PATTERNS.md excerpts.
</output>
