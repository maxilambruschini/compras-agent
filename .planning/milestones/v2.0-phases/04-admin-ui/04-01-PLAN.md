---
phase: 04-admin-ui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_admin.py
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "A RED test file exists asserting every read-endpoint behavior before any endpoint code is written"
    - "Tests cover: list gastos newest-first, ?from/?to date filter, ?q concepto search, detail, detail-404, ticket-404 (no path), drafts-not-exposed, Decimal-as-string, cierres list, CORS header"
    - "All tests in test_admin.py FAIL or ERROR until Plan 02 implements the endpoints (RED phase)"
  artifacts:
    - path: "backend/tests/test_admin.py"
      provides: "Nyquist automated gate for all UI-01 / UI-02 backend behaviors"
      contains: "def test_list_gastos_empty"
      min_lines: 200
  key_links:
    - from: "backend/tests/test_admin.py"
      to: "app.main.create_app"
      via: "httpx.ASGITransport(app=create_app())"
      pattern: "ASGITransport"
    - from: "backend/tests/test_admin.py"
      to: "get_db dependency override"
      via: "app.dependency_overrides[get_db]"
      pattern: "dependency_overrides"
---

<objective>
Write the RED test suite that defines every read-endpoint behavior for the admin UI backend
BEFORE any endpoint code exists. This is the Nyquist automated gate for the whole phase: when
Plan 02 implements `backend/app/routers/admin.py`, these tests turn green with no edits.

Purpose: Lock the contract for the four GET endpoints (list/detail/ticket/cierres) and their
security boundaries (committed-only, Decimal precision, CORS) as executable assertions.
Output: `backend/tests/test_admin.py` — all tests RED (fail/error) at the end of this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-ui/04-CONTEXT.md
@.planning/phases/04-admin-ui/04-RESEARCH.md
@.planning/phases/04-admin-ui/04-VALIDATION.md

<interfaces>
<!-- Exact contracts the test must assert against. Mirror test_prompt_trigger.py. -->

The endpoints under test (Plan 02 will mount them with prefix="/api"):
  GET /api/gastos              -> list[GastoOut], newest-first; ?from=<date>&to=<date>&q=<text>
  GET /api/gastos/{id}         -> GastoOut; 404 if unknown
  GET /api/gastos/{id}/ticket  -> FileResponse; 404 if no ticket_image_path
  GET /api/cierres             -> list[CierreOut], newest-first

Gasto model (backend/app/db/models.py — DO NOT re-read, fields are final):
  id: uuid.UUID, fecha: date (NOT NULL), concepto: str (NOT NULL),
  monto: Decimal Numeric(14,2) (NOT NULL), ticket_image_path: str | None,
  sender_phone: str (NOT NULL), created_at: datetime (server_default now())

CajaCierre model:
  id: uuid.UUID, fecha: date, hora_cierre: str(5) "12:00"|"17:00",
  efectivo_en_caja: Decimal Numeric(14,2), sender_phone: str, created_at: datetime

Conversation model (used ONLY to seed a draft that must NOT appear in /api/gastos):
  sender_phone: str (PK), state: str, draft_gasto: str | None (JSON), last_message_id, updated_at

Fixture pattern (copy EXACTLY from test_prompt_trigger.py::prompt_client):
  monkeypatch.setenv("AGENT_MODE","gastos"); get_settings.cache_clear()
  from app.db.session import get_db; from app.main import create_app
  app = create_app()
  async def override_get_db(): yield db_session
  app.dependency_overrides[get_db] = override_get_db
  async with httpx.AsyncClient(transport=httpx.ASGITransport(app=app), base_url="http://testserver") as client: ...
  Teardown: app.dependency_overrides.clear(); get_settings.cache_clear()

conftest fixtures available (backend/tests/conftest.py): db_session (in-memory aiosqlite),
env_setup (session autouse, sets AGENT_MODE=gastos). NO new fixtures needed.

Seeding rows: db_session.add(Gasto(...)); await db_session.flush(). created_at has a
server_default — to force a deterministic newest-first order, set created_at explicitly
on seeded rows (import datetime) OR seed in order and assert relative ordering by id/concepto.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the gastos-list + detail + ticket + drafts + Decimal RED tests</name>
  <read_first>
    - backend/tests/test_prompt_trigger.py (fixture pattern: ASGITransport + dependency_overrides + cache_clear — copy verbatim)
    - backend/tests/conftest.py (db_session, env_setup — what is already provided)
    - backend/app/db/models.py interface block above (Gasto / Conversation field names)
  </read_first>
  <action>
    Create backend/tests/test_admin.py. Build a module-level `admin_client` fixture that mirrors
    test_prompt_trigger.py::prompt_client EXACTLY but WITHOUT the WhatsApp provider override (read
    endpoints need none): monkeypatch AGENT_MODE=gastos, get_settings.cache_clear(), create_app(),
    override get_db to yield db_session, yield (client, app) via httpx.AsyncClient over ASGITransport,
    teardown clears overrides + cache. Defer imports of app.main/app.db.session into the fixture body
    (so collection never ImportErrors before Plan 02). Write these async tests, all hitting the /api prefix:
      - test_list_gastos_empty: GET /api/gastos with no rows → 200, body == [].
      - test_list_gastos_newest_first: seed 2+ Gasto rows with explicit increasing created_at; assert
        response is ordered created_at desc (newest concepto first).
      - test_list_gastos_date_filter: seed gastos on different fecha values; GET /api/gastos?from=&to=
        returns only rows within range. Use ISO date strings for params.
      - test_list_gastos_search: seed gastos with distinct concepto; GET /api/gastos?q=<substr> returns
        only ILIKE-matching rows; assert case-insensitive (query lowercase matches mixed-case concepto).
      - test_get_gasto: seed one Gasto; GET /api/gastos/{id} → 200 with matching fields.
      - test_get_gasto_not_found: GET /api/gastos/{random uuid4} → 404.
      - test_get_ticket_no_path: seed Gasto with ticket_image_path=None; GET /api/gastos/{id}/ticket → 404.
      - test_drafts_not_exposed: seed a Conversation row with draft_gasto set to a JSON string and NO
        committed Gasto; GET /api/gastos → 200 and body == [] (the draft must never surface). This is
        the committed-only correctness boundary (T-04-01).
      - test_decimal_serialization: seed Gasto with monto=Decimal("1234567.89"); GET the list/detail and
        assert the JSON monto value is the STRING "1234567.89" (Pydantic v2 default), not a float —
        assert isinstance(body["monto"], str) and body["monto"] == "1234567.89".
    Reference the exact pytest node IDs from 04-VALIDATION.md. Do NOT implement any endpoint — these
    MUST be RED. No fenced production code; this is test code only.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_admin.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_admin.py --collect-only -q` lists at minimum:
      test_list_gastos_empty, test_list_gastos_newest_first, test_list_gastos_date_filter,
      test_list_gastos_search, test_get_gasto, test_get_gasto_not_found, test_get_ticket_no_path,
      test_drafts_not_exposed, test_decimal_serialization — with zero collection errors.
    - `cd backend && python -m pytest tests/test_admin.py -x -q` FAILS (RED) — endpoints return 404
      because admin.py is not yet mounted.
  </acceptance_criteria>
  <done>test_admin.py collects cleanly and all gastos/ticket/draft/Decimal tests are present and RED.</done>
</task>

<task type="auto">
  <name>Task 2: Write the cierres-list + CORS-header RED tests</name>
  <read_first>
    - backend/tests/test_admin.py (the fixture + tests created in Task 1 — extend, do not duplicate the fixture)
    - .planning/phases/04-admin-ui/04-RESEARCH.md Pattern 2 (CORS: allow_origins http://localhost:5173, allow_methods GET)
  </read_first>
  <action>
    Append to backend/tests/test_admin.py:
      - test_list_cierres: seed 2+ CajaCierre rows with explicit increasing created_at; GET /api/cierres
        → 200, ordered created_at desc, each row exposing fecha, hora_cierre, efectivo_en_caja,
        sender_phone. Assert efectivo_en_caja serializes as a string (Decimal precision).
      - test_list_cierres_empty: GET /api/cierres with no rows → 200, body == [].
      - test_cors_header: issue a GET to /api/gastos with header Origin: http://localhost:5173 and
        assert the response carries access-control-allow-origin == "http://localhost:5173" (CORSMiddleware
        echoes the allowed origin on simple requests). This is the T-04-03 CORS mitigation gate.
    Use the same admin_client fixture from Task 1. These tests MUST also be RED until Plan 02.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_admin.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_admin.py --collect-only -q` additionally lists
      test_list_cierres, test_list_cierres_empty, test_cors_header — zero collection errors.
    - `cd backend && python -m pytest tests/test_admin.py -x -q` still FAILS (RED) — confirming the
      gate is armed before implementation.
    - `cd backend && python -m pytest tests/ -q --collect-only` succeeds (no regression to existing
      collection from the new file).
  </acceptance_criteria>
  <done>All ten+ RED tests present in test_admin.py; full suite still collects; admin tests are RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → FastAPI read API | Untrusted query params (`from`, `to`, `q`) + path param (`id`) cross here |
| DB → API response | `conversations.draft_gasto` (in-progress, unconfirmed) must never cross into responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | GET /api/gastos | mitigate | `test_drafts_not_exposed` asserts a seeded `Conversation.draft_gasto` never appears in the gastos list — committed-only boundary as an executable gate (implemented in Plan 02 via `select(Gasto)` only) |
| T-04-02 | Tampering (SQLi) | GET /api/gastos?q= | mitigate | `test_list_gastos_search` exercises the `q` param; Plan 02 uses SQLAlchemy `.ilike()` bind parameter (no string interpolation). Test confirms behavior, not injection, but locks the param surface |
| T-04-03 | Elevation of Privilege | CORS policy | mitigate | `test_cors_header` asserts explicit `access-control-allow-origin: http://localhost:5173` (Plan 02 adds CORSMiddleware with `allow_credentials=False`, `allow_methods=["GET"]`) |
| T-04-04 | Information Disclosure | GET /api/gastos/{id}/ticket | mitigate | `test_get_ticket_no_path` locks the 404-on-no-ticket behavior; the path-traversal `commonpath` defense is implemented + tested in Plan 02 |
| T-04-SC | Tampering | npm/pip installs | accept | No package installs in this plan (test-only). Frontend `[ASSUMED-OK]` packages (react-router, @tanstack/react-query) are well-known official-org packages per RESEARCH Package Legitimacy Audit; installed in Plan 03 |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_admin.py --collect-only -q` → zero errors, all named tests present.
- `cd backend && python -m pytest tests/test_admin.py -x -q` → RED (expected; endpoints not built).
- `cd backend && python -m pytest tests/ -q --collect-only` → no collection regression.
</verification>

<success_criteria>
- backend/tests/test_admin.py exists with every UI-01 + UI-02 behavior from 04-VALIDATION.md as a test.
- All admin tests are RED (the Nyquist gate is armed for Plan 02).
- No existing test is broken by the new file at collection time.
</success_criteria>

<output>
Create `.planning/phases/04-admin-ui/04-01-SUMMARY.md` when done.
</output>
