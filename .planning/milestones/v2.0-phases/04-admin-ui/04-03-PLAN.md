---
phase: 04-admin-ui
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - frontend/src/index.css
  - frontend/src/admin.css
  - frontend/src/api/client.ts
  - frontend/src/utils/formatARS.ts
  - frontend/src/utils/formatDate.ts
  - frontend/src/components/Spinner.tsx
  - frontend/src/components/NavTabs.tsx
  - frontend/src/App.tsx
  - frontend/src/main.tsx
  - frontend/src/pages/GastosListPage.tsx
  - frontend/src/pages/GastoDetailPage.tsx
  - frontend/src/pages/CierresListPage.tsx
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "The frontend builds and lints clean with react-router v7 + @tanstack/react-query installed"
    - "Two-tab nav (Gastos | Cierres de Caja) routes to /gastos, /gastos/:id, /cierres; / redirects to /gastos"
    - "GastosListPage fetches /api/gastos via the typed client, renders a filterable/searchable table, row click → detail"
    - "GastoDetailPage shows all fields + ticket image (or 'Sin ticket adjunto'); CierresListPage renders read-only cierres"
    - "Money renders as $1.234,56 via manual formatARS (no Intl); dates as '31 may 2026'; loading/empty/error states present"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "QueryClientProvider + createBrowserRouter + RouterProvider wiring"
      contains: "RouterProvider"
    - path: "frontend/src/api/client.ts"
      provides: "Typed fetch client reading import.meta.env.VITE_API_URL with /api fallback"
      contains: "import.meta.env.VITE_API_URL"
    - path: "frontend/src/pages/GastosListPage.tsx"
      provides: "Gastos list + filter bar + search"
      contains: "useQuery"
    - path: "frontend/src/utils/formatARS.ts"
      provides: "Manual Argentine money formatter (no toLocaleString)"
      contains: "formatARS"
  key_links:
    - from: "frontend/src/pages/GastosListPage.tsx"
      to: "/api/gastos"
      via: "useQuery → api.listGastos"
      pattern: "listGastos"
    - from: "frontend/src/pages/GastoDetailPage.tsx"
      to: "/api/gastos/:id/ticket"
      via: "<img src={api.ticketUrl(id)}>"
      pattern: "ticketUrl"
    - from: "frontend/src/App.tsx"
      to: "react-router routes"
      via: "createBrowserRouter"
      pattern: "createBrowserRouter"
---

<objective>
Build the read-only React admin UI: install react-router v7 + @tanstack/react-query, wire the
router + query client, add the design-system CSS token + component styles, the typed API client and
display utilities, and the three pages (Gastos list, Gasto detail, Cierres list) exactly per the
UI-SPEC. Read-only — no edit/delete controls, no auth.

Purpose: Deliver UI-01 (list + view gastos) and UI-02 (list + view cierres) against the Plan 02 read API.
Output: a building, linting frontend rendering all three views with the locked visual contract.

NOTE: There is NO frontend test runner (no vitest/jest — confirmed in RESEARCH). Acceptance is
`pnpm build` / `pnpm lint` clean + source assertions. Runtime behavior is manual UAT (04-VALIDATION.md).
Do NOT add vitest or any test toolchain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-admin-ui/04-CONTEXT.md
@.planning/phases/04-admin-ui/04-RESEARCH.md
@.planning/phases/04-admin-ui/04-UI-SPEC.md

<interfaces>
<!-- API response shapes (from Plan 02 admin.py). JSON keys, Decimal as STRING. -->
GastoOut:  { id: string, fecha: string (ISO date), concepto: string, monto: string,
             ticket_image_path: string | null, sender_phone: string, created_at: string (ISO datetime) }
CierreOut: { id: string, fecha: string, hora_cierre: "12:00" | "17:00",
             efectivo_en_caja: string, sender_phone: string, created_at: string }

Endpoints (mounted under /api by Plan 02):
  GET /api/gastos?from=&to=&q=   GET /api/gastos/{id}   GET /api/gastos/{id}/ticket   GET /api/cierres

Vite proxy (frontend/vite.config.ts — already present, DO NOT change): '/api' and '/health' proxy to
apiTarget with NO rewrite → client must call paths WITH the /api prefix. Client BASE: use
import.meta.env.VITE_API_URL ? `${VITE_API_URL}/api` : "/api" (falls back to the dev proxy path).

Existing index.css tokens (frontend/src/index.css): --text, --text-h, --bg, --border, --accent,
--accent-bg, --accent-border, --sans. The ROOT block sets font:18px and #root has text-align:center +
width:1126px. Phase 4 standardises body to 16px and left-aligns table content via admin.css overrides.

UI-SPEC is the authoritative visual + copy contract: 2-tab nav (48px, active = 2px accent bottom-border),
max-width 1126px, Spanish copy table (all strings), table/filter-bar/badge/spinner/empty/error contracts,
detail dl layout, formatARS ($1.234,56), formatDate ("31 may 2026"), staleTime 30_000, query keys
["gastos"], ["gastos", id], ["cierres"]. Ticket badge text "con ticket"; no-ticket "—" (list) /
"Sin ticket adjunto" (detail). Route "/" → redirect to /gastos.

react-router v7: import { createBrowserRouter, RouterProvider, Navigate, Link, useParams } from "react-router".
@tanstack/react-query v5: import { QueryClient, QueryClientProvider, useQuery } from "@tanstack/react-query".
Use v5 `isPending` (NOT v4 `isLoading`) for initial-load state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps + wire router/query/CSS/client/utils + nav shell</name>
  <read_first>
    - frontend/package.json (current deps: react, react-dom only; scripts dev/build/lint/preview)
    - frontend/src/main.tsx + frontend/src/App.tsx (current scaffold to replace)
    - frontend/src/index.css (token set + #root rules — add --secondary-surface, do not rewrite)
    - frontend/vite.config.ts (proxy contract — informs client BASE, do not edit)
    - .planning/phases/04-admin-ui/04-RESEARCH.md Patterns 6 & 7 + Code Examples (App wiring, client, formatARS, formatDate)
    - .planning/phases/04-admin-ui/04-UI-SPEC.md (nav, color, spacing, Copywriting Contract)
  </read_first>
  <action>
    Run `pnpm add react-router @tanstack/react-query` inside frontend/ (NOT react-router-dom; v7 package
    is react-router). This updates package.json + pnpm-lock.yaml. Then create the foundation:
      - frontend/src/index.css: add `--secondary-surface: #f7f6f9;` to the :root light block and
        `--secondary-surface: #1e1f28;` to the dark-mode block. Do NOT rewrite existing rules.
      - frontend/src/admin.css: new stylesheet implementing the UI-SPEC component contracts — nav bar
        (48px, active tab 2px solid var(--accent) bottom-border + var(--text-h)), table (thead bg
        var(--secondary-surface), th 14px uppercase, td 16px, row-hover var(--secondary-surface),
        gastos rows cursor:pointer), filter bar, ticket badge pill (var(--accent-bg)/--accent-border),
        spinner keyframe (24px ring, border-top var(--accent), spin 0.8s), empty-state, error banner
        (rgba(239,68,68,0.08) + left border #dc2626), detail dl. Left-align table cells (override #root
        text-align:center). Use the existing tokens; body text 16px.
      - frontend/src/utils/formatARS.ts: export formatARS(value: string|number): string producing
        $1.234,56 via manual string manipulation (parseFloat then thousands "." / decimal "," / 2 places).
        Do NOT use Intl.NumberFormat / toLocaleString.
      - frontend/src/utils/formatDate.ts: export formatDate(iso: string): "31 may 2026" using the Spanish
        month-abbrev array [ene feb mar abr may jun jul ago sep oct nov dic]. Also export a formatDateTime
        for "31 may 2026, 14:23" used by the detail "Creado" field.
      - frontend/src/api/client.ts: BASE = import.meta.env.VITE_API_URL ? `${import.meta.env.VITE_API_URL}/api`
        : "/api". Typed `get<T>` via fetch (throw on !res.ok). Export api with listGastos(params?), getGasto(id),
        ticketUrl(id) (returns `${BASE}/gastos/${id}/ticket`), listCierres(). Define GastoOut/CierreOut TS types
        matching the interfaces block (monto/efectivo_en_caja as string).
      - frontend/src/components/Spinner.tsx: the CSS-ring spinner (class from admin.css), centered.
      - frontend/src/components/NavTabs.tsx: two-tab nav using react-router Link/NavLink to /gastos and
        /cierres with active-tab styling per UI-SPEC. Copy: "Gastos" | "Cierres de Caja".
      - frontend/src/App.tsx: QueryClient with defaultOptions.queries.staleTime = 30_000; createBrowserRouter
        with routes { "/" → <Navigate to="/gastos" replace/> }, /gastos → GastosListPage, /gastos/:id →
        GastoDetailPage, /cierres → CierresListPage; render NavTabs above an <Outlet/> (use a layout route
        so NavTabs persists), wrapped in QueryClientProvider + RouterProvider. Import the page components
        (created in Tasks 2-3) — leave the imports in place; build will pass once those files exist (do this
        task LAST in the executor's ordering OR stub the pages, but since all three tasks are one plan,
        create the page files in Tasks 2-3 before running the build gate).
      - frontend/src/main.tsx: import './index.css' and './admin.css'; keep StrictMode + createRoot rendering <App/>.
    Set the document.title / index.html title to "Compras Agent" if not already.
  </action>
  <verify>
    <automated>cd frontend && grep -q '"react-router"' package.json && grep -q '"@tanstack/react-query"' package.json && echo DEPS_OK</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies include `react-router` and `@tanstack/react-query`; pnpm-lock.yaml updated.
    - react-router-dom is NOT added: `grep -c "react-router-dom" frontend/package.json` returns 0.
    - index.css contains `--secondary-surface` in both light and dark blocks.
    - client.ts contains `import.meta.env.VITE_API_URL` and `ticketUrl`; uses `/api` fallback.
    - App.tsx contains `createBrowserRouter`, `RouterProvider`, `QueryClientProvider`, `staleTime`.
    - formatARS.ts does NOT contain `toLocaleString`: `grep -c "toLocaleString" frontend/src/utils/formatARS.ts` returns 0.
    - (Final build gate runs in Task 3 once all pages exist.)
  </acceptance_criteria>
  <done>Deps installed; router + query client + CSS token + admin.css + client + utils + nav shell in place.</done>
</task>

<task type="auto">
  <name>Task 2: GastosListPage (table + filter/search) + GastoDetailPage (fields + ticket image)</name>
  <read_first>
    - frontend/src/api/client.ts + frontend/src/utils/formatARS.ts + formatDate.ts (created in Task 1 — consume these)
    - frontend/src/admin.css (table / filter-bar / badge / detail dl / spinner classes from Task 1)
    - .planning/phases/04-admin-ui/04-UI-SPEC.md (Table, Filter Bar, Monto Display, Ticket Indicator,
      Ticket Image, Detail Page Layout, Empty/Loading/Error states, full Copywriting Contract)
  </read_first>
  <action>
    Create frontend/src/pages/GastosListPage.tsx:
      - Local filter state: desde, hasta, buscar (controlled inputs). useQuery({ queryKey: ["gastos",
        {from: desde, to: hasta, q: buscar}], queryFn: () => api.listGastos({from: desde||undefined,
        to: hasta||undefined, q: buscar||undefined}), staleTime: 30_000 }). Debounce or fetch-on-change
        is the executor's choice; server-side filtering via params is the contract.
      - Filter bar above the table: "Desde" date input, "Hasta" date input, "Buscar" text input
        (placeholder "Buscar concepto..."), "Limpiar" text button that resets all three to "".
      - Table columns per Copywriting Contract: Fecha (formatDate), Concepto, Monto (formatARS, right-aligned),
        Ticket (badge "con ticket" when ticket_image_path present, else "—"), Registrado por (sender_phone).
      - Each row clickable → navigate to /gastos/:id (cursor pointer).
      - States: isPending → <Spinner/> ("Cargando..."); error → error banner ("No se pudo cargar la
        información" / body copy); empty → empty state. Use the with-filter vs no-filter empty copy:
        no filter → "No hay gastos registrados" / body; with any filter active → "Sin resultados" / body.
    Create frontend/src/pages/GastoDetailPage.tsx:
      - useParams id; useQuery({ queryKey: ["gastos", id], queryFn: () => api.getGasto(id!) }).
      - Back link "← Volver a gastos" → /gastos (var(--accent), underline on hover).
      - Two-column dl with fields in order: Fecha, Concepto, Monto (formatARS), Ticket (badge or "—"),
        "Registrado por" (sender_phone), "Creado" (formatDateTime of created_at).
      - Ticket image section below the dl (separated by border-top): if ticket_image_path present, render
        <img src={api.ticketUrl(id!)} ...> (max-height 480px, object-fit contain, border + radius); click
        → window.open(ticketUrl, "_blank"). If absent → text "Sin ticket adjunto".
      - States: isPending → Spinner; error → error banner; 404 (getGasto throws) handled by the error state.
    All copy from the UI-SPEC Copywriting Contract (Spanish). No edit/delete controls anywhere.
  </action>
  <verify>
    <automated>cd frontend && grep -q "useQuery" src/pages/GastosListPage.tsx && grep -q "ticketUrl" src/pages/GastoDetailPage.tsx && echo PAGES_OK</automated>
  </verify>
  <acceptance_criteria>
    - GastosListPage.tsx contains `useQuery`, `listGastos`, `formatARS`, and a "Limpiar" reset control.
    - GastosListPage renders columns for fecha, concepto, monto, ticket, sender_phone; rows navigate to /gastos/:id.
    - GastoDetailPage.tsx contains `useParams`, `getGasto`, `ticketUrl`, and the "← Volver a gastos" back link.
    - Neither page contains edit/delete controls: `grep -ci "delete\|eliminar\|editar\|onSubmit" frontend/src/pages/GastosListPage.tsx frontend/src/pages/GastoDetailPage.tsx` returns 0.
    - (Build gate runs in Task 3.)
  </acceptance_criteria>
  <done>Gastos list + detail pages implemented against the API client and UI-SPEC, read-only.</done>
</task>

<task type="auto">
  <name>Task 3: CierresListPage (read-only) + build + lint gate</name>
  <read_first>
    - frontend/src/api/client.ts + frontend/src/utils/formatARS.ts + formatDate.ts (consume)
    - frontend/src/admin.css (table / spinner / empty / error classes)
    - .planning/phases/04-admin-ui/04-UI-SPEC.md (Cierres table columns: Fecha, Cierre, Efectivo en caja,
      Registrado por; rows NOT clickable; no edit controls; empty/loading/error copy)
  </read_first>
  <action>
    Create frontend/src/pages/CierresListPage.tsx:
      - useQuery({ queryKey: ["cierres"], queryFn: () => api.listCierres(), staleTime: 30_000 }).
      - Table columns per Copywriting Contract: Fecha (formatDate), Cierre (hora_cierre, e.g. "12:00"),
        "Efectivo en caja" (efectivo_en_caja via formatARS, right-aligned), "Registrado por" (sender_phone).
      - Rows are NOT clickable (no pointer cursor, no navigation, no hover action beyond bg).
      - States: isPending → Spinner; error → error banner ("No se pudo cargar la información"); empty →
        "No hay cierres registrados" / "Los cierres de caja reportados por WhatsApp aparecerán aquí."
      - Strictly read-only — no edit/delete controls rendered.
    Then run the build + lint gate. If the build surfaces a react-router v7 import path issue for
    RouterProvider, fall back to importing RouterProvider from "react-router" (per RESEARCH Assumption A3).
    Fix any TS/lint errors until both commands pass. Note in the SUMMARY that the Docker frontend container
    needs `docker compose build frontend` (anonymous node_modules volume) before the new deps are available
    in-container — this is a manual UAT prerequisite, not part of the automated gate.
  </action>
  <verify>
    <automated>cd frontend && pnpm install && pnpm build && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - CierresListPage.tsx contains `useQuery`, `listCierres`, and the Cierres column copy; rows are NOT
      clickable: `grep -c "navigate\|to=\|onClick" frontend/src/pages/CierresListPage.tsx` returns 0.
    - `cd frontend && pnpm build` exits 0 (tsc -b && vite build clean).
    - `cd frontend && pnpm lint` exits 0.
    - SUMMARY notes the `docker compose build frontend` rebuild prerequisite for in-container UAT.
  </acceptance_criteria>
  <done>Cierres page implemented read-only; frontend builds and lints clean; Docker rebuild note recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ FastAPI read API | Client sends `from`/`to`/`q` + `id`; receives JSON + streamed image. Backend enforces all access control (Plan 02) |
| npm registry → frontend deps | Two new packages (`react-router`, `@tanstack/react-query`) installed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Elevation of Privilege | cross-origin fetch | mitigate | Client calls same-origin `/api/*` via the Vite proxy in dev; CORS allow-list enforced server-side (Plan 02). No credentials sent (`allow_credentials=False`) |
| T-04-06 | Information Disclosure | ticket `<img>` | accept | Ticket image fetched directly from `/api/gastos/{id}/ticket`; access control + path-traversal guard live server-side (Plan 02 T-04-04). Client only constructs the URL from a UUID |
| T-04-07 | Tampering (no write path) | UI controls | mitigate | Read-only by design — no edit/delete/create controls rendered. Grep gates assert absence of mutation controls on all three pages |
| T-04-SC | Tampering (supply chain) | `pnpm add react-router @tanstack/react-query` | accept | Both packages are `[ASSUMED-OK]` per RESEARCH Package Legitimacy Audit: official orgs (remix-run, TanStack), ~10M+ weekly downloads, CLAUDE.md-recommended. slopcheck unavailable at research time; well-known provenance → no blocking-human gate required (no `[SUS]`/`[SLOP]` verdicts) |
</threat_model>

<verification>
- `cd frontend && pnpm build` exits 0; `cd frontend && pnpm lint` exits 0.
- Source-assertion grep gates pass (client BASE, no toLocaleString, no mutation controls, react-router not -dom).
- Manual UAT (04-VALIDATION.md): browser navigation, filter/search, ticket image load, cierres read-only —
  after `docker compose build frontend` (anonymous node_modules volume rebuild).
</verification>

<success_criteria>
- Three pages render the Gastos list (filterable/searchable), Gasto detail (with ticket image), and
  read-only Cierres list per the UI-SPEC.
- Money/date formatting via manual utilities (no Intl); loading/empty/error states present.
- Frontend builds and lints clean; no test runner introduced.
</success_criteria>

<output>
Create `.planning/phases/04-admin-ui/04-03-SUMMARY.md` when done. Record the `docker compose build frontend`
rebuild step as a manual UAT prerequisite.
</output>
