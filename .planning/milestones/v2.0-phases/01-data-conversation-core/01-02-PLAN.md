---
phase: 01-data-conversation-core
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/app/models/conversation.py
  - backend/app/services/amounts.py
  - backend/app/services/slot_extraction.py
  - backend/tests/test_amounts.py
  - backend/tests/test_slot_extraction.py
autonomous: true
requirements: [CONV-05, GASTO-01]
must_haves:
  truths:
    - "parse_ars_amount('1.500') == Decimal('1500') and parse_ars_amount('1.234,56') == Decimal('1234.56')"
    - "parse_ars_amount returns None on unparseable input (null over hallucination)"
    - "SlotExtractionService.extract('queso en supermercado $1500') returns GastoSlots(concepto, monto) via mocked gpt-4o-mini .parse()"
    - "A refusal or null parsed result yields an empty GastoSlots (all None) so the orchestrator can re-prompt"
  artifacts:
    - path: "backend/app/models/conversation.py"
      provides: "GastoSlots + DraftGasto all-Optional DTOs"
      contains: "class GastoSlots"
    - path: "backend/app/services/amounts.py"
      provides: "parse_ars_amount() pure utility"
      contains: "def parse_ars_amount"
    - path: "backend/app/services/slot_extraction.py"
      provides: "SlotExtractionService using gpt-4o-mini .parse()"
      contains: "gpt-4o-mini"
  key_links:
    - from: "backend/app/services/slot_extraction.py"
      to: "GastoSlots"
      via: "response_format in client.chat.completions.parse()"
      pattern: "response_format=GastoSlots"
---

<objective>
Build the slot-extraction half of the conversation engine: the GastoSlots/DraftGasto Pydantic DTOs (all Optional), the `parse_ars_amount()` utility that defuses the Decimal("1.500") trap, and the SlotExtractionService that mirrors the existing ExtractionService .parse()/.refusal pattern on gpt-4o-mini.

Purpose: This is the GPT-text→slots layer the orchestrator (Plan 04) calls. It runs in Wave 2 (after the Wave 1 schema in 01-01). It is NOT parallel with 01-03: 01-03 imports `DraftGasto` from this plan, so 01-03 is Wave 3 and runs strictly after 01-02. The sequential chain is 01-01 (Wave 1) → 01-02 (Wave 2) → 01-03 (Wave 3) → 01-04 (Wave 4). Covers CONV-05 (Argentine number parsing) and GASTO-01 (free-form intent → structured slots).

Output: New DTO module, amounts utility, slot extraction service, and two test files. This is a TDD plan — write the failing tests first, then implement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-data-conversation-core/01-CONTEXT.md
@.planning/phases/01-data-conversation-core/01-RESEARCH.md
@.planning/phases/01-data-conversation-core/01-PATTERNS.md

<interfaces>
<!-- Mirror these existing patterns. Read the analog files in each task's read_first. -->
ExtractedInvoice in backend/app/models/extraction.py uses pydantic BaseModel with `ConfigDict(use_enum_values=True)` and all-Optional fields (null over hallucination). Mirror this for GastoSlots/DraftGasto.
ExtractionService in backend/app/services/extraction.py: constructor injects `AsyncOpenAI` + `Settings`; uses `await self._client.chat.completions.parse(model=..., messages=[system, user], response_format=<PydanticModel>)`; checks `msg.refusal` BEFORE `msg.parsed`; uses module-level SYSTEM_PROMPT constant and a custom exception hierarchy; binds structlog per call.
test_extraction.py mocks AsyncOpenAI with a MagicMock(spec=ParsedChatCompletionMessage) helper setting `.parsed` / `.refusal` — reuse this; never make live OpenAI calls.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parse_ars_amount() utility (TDD)</name>
  <files>backend/app/services/amounts.py, backend/tests/test_amounts.py</files>
  <read_first>
    - backend/app/services/extraction.py lines ~105-126 (compute_confidence/assign_status — module-level pure helper convention to mirror; importable, no class)
    - .planning/phases/01-data-conversation-core/01-RESEARCH.md Pattern 3 and Pitfall A (exact parse_ars_amount spec + the Decimal("1.500") trap)
    - .planning/ROADMAP.md success criterion 5 (the exact assertions required)
  </read_first>
  <behavior>
    - parse_ars_amount("1.500") == Decimal("1500")  (dot = thousands sep)
    - parse_ars_amount("1.234,56") == Decimal("1234.56")  (comma = decimal sep)
    - parse_ars_amount("1500") == Decimal("1500")
    - parse_ars_amount("1500,50") == Decimal("1500.50")
    - parse_ars_amount("abc") is None  (unparseable → None, never raises)
    - parse_ars_amount("") is None
  </behavior>
  <action>
    First write backend/tests/test_amounts.py encoding the behavior cases above (RED). Then implement `parse_ars_amount(text: str) -> Optional[Decimal]` in backend/app/services/amounts.py as a module-level pure function: strip whitespace, remove "." (thousands separator), replace "," with "." (decimal separator), then `Decimal(cleaned)`; catch `InvalidOperation` (and empty/None) and return None. NEVER use Python's `locale` module (global mutable state, unsafe in async — Pitfall). Document the Decimal("1.500") trap in the docstring. Keep it a pure function with no class.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_amounts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All six behavior assertions pass.
    - parse_ars_amount never raises on bad input — returns None.
    - No use of the `locale` module anywhere in amounts.py.
  </acceptance_criteria>
  <done>parse_ars_amount implemented and green against the ROADMAP success-criterion-5 assertions plus edge cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GastoSlots + DraftGasto DTOs and SlotExtractionService (TDD)</name>
  <files>backend/app/models/conversation.py, backend/app/services/slot_extraction.py, backend/tests/test_slot_extraction.py</files>
  <read_first>
    - backend/app/models/extraction.py (ExtractedInvoice — all-Optional + ConfigDict(use_enum_values=True))
    - backend/app/services/extraction.py (ExtractionService constructor DI, .parse() call, refusal-before-parsed order, module SYSTEM_PROMPT, exception hierarchy, structlog binding)
    - backend/tests/test_extraction.py (the ParsedChatCompletionMessage mock helper to reuse)
    - .planning/phases/01-data-conversation-core/01-RESEARCH.md Pattern 1, Pattern 2, Pattern 10 (service mirror, DTO design, system prompt design)
  </read_first>
  <behavior>
    - GastoSlots and DraftGasto: every field Optional with default None. GastoSlots has concepto: Optional[str], monto: Optional[float] (JSON number so GPT emits a number, not a locale string). DraftGasto has concepto: Optional[str], monto: Optional[Decimal], ticket_image_path: Optional[str], failure_count: int = 0.
    - SlotExtractionService.extract("queso en supermercado $1500"), with .parse() mocked to return parsed=GastoSlots(concepto="queso en supermercado", monto=1500.0), returns that GastoSlots.
    - When mocked msg.refusal is set (non-None), extract() returns an empty GastoSlots() (all None) — checked BEFORE parsed.
    - When mocked msg.parsed is None, extract() returns an empty GastoSlots().
    - extract() calls client.chat.completions.parse with model="gpt-4o-mini" and response_format=GastoSlots (assert via the mock's call_args).
  </behavior>
  <action>
    First write backend/tests/test_slot_extraction.py with the behavior cases above, mocking AsyncOpenAI exactly as test_extraction.py does (RED).
    Then create backend/app/models/conversation.py with `GastoSlots` (concepto: Optional[str]=None, monto: Optional[float]=None) and `DraftGasto` (concepto: Optional[str]=None, monto: Optional[Decimal]=None, ticket_image_path: Optional[str]=None, failure_count: int=0), both using `ConfigDict(use_enum_values=True)` per the ExtractedInvoice analog. monto is float in GastoSlots (GPT JSON number) and Decimal in DraftGasto (converted via Decimal(str(slots.monto)) by the orchestrator). Per D-01 do NOT add lugar/proveedor/entrada/category.
    Then create backend/app/services/slot_extraction.py: `SlotExtractionService` with constructor `__init__(self, openai_client: AsyncOpenAI, settings: Settings)` (DI, never construct client at import); an async `extract(self, text: str) -> GastoSlots` that calls `await self._client.chat.completions.parse(model="gpt-4o-mini", messages=[{system}, {user}], response_format=GastoSlots)`, checks `msg.refusal` BEFORE `msg.parsed`, returns `GastoSlots()` (all None) on refusal/None, else returns `msg.parsed`. Add a module-level `SLOT_SYSTEM_PROMPT` (concise Argentine Spanish: restaurant manager recording a cash expense; return monto as a plain JSON number e.g. "$1.500" → 1500; return null for any field not clearly stated — do NOT guess; concepto = item/description as stated). Add a `SlotExtractionError` exception mirroring the extraction.py hierarchy; wrap the .parse() call in try/except and raise it on transport failure. Bind structlog per call with a text preview; never log secrets (T-02-02).
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_slot_extraction.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - GastoSlots.monto is typed Optional[float]; DraftGasto.monto is typed Optional[Decimal]; both DTOs have all fields Optional (DraftGasto.failure_count defaults to 0); neither has lugar/proveedor/entrada/category.
    - extract() returns the parsed GastoSlots when the mock yields one; returns empty GastoSlots() on refusal and on parsed=None.
    - The mocked .parse() was called with model="gpt-4o-mini" and response_format=GastoSlots.
    - No live OpenAI call is made in tests.
  </acceptance_criteria>
  <done>DTOs and SlotExtractionService implemented and green; mirrors the ExtractionService pattern on gpt-4o-mini with all-Optional slots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted message text → GPT prompt | Inbound free-form Spanish drives the slot-extraction prompt (prompt-injection surface) |
| GPT output → monto value | Extracted monto becomes a money value; mis-extraction writes wrong amount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | monto extraction | mitigate | monto typed Optional[float] (GPT emits JSON number, sidesteps Decimal("1.500") trap); orchestrator (Plan 04) bounds-checks and requires explicit confirmation before any write |
| T-02-02 | Information Disclosure | structlog in SlotExtractionService | mitigate | log only a truncated text preview; never log API keys or full secrets |
| T-02-03 | Tampering / injection | inbound text into SLOT_SYSTEM_PROMPT | accept | GPT only extracts into a fixed Optional schema; it cannot trigger a write (write path is deterministic in Plan 04); worst case is a wrong slot value caught at the mandatory confirm step |
| T-02-SC | Tampering | pip installs | accept | No new packages (RESEARCH Package Legitimacy Audit: none); no install task |
</threat_model>

<verification>
- `cd backend && pytest tests/test_amounts.py tests/test_slot_extraction.py -q` green.
- ROADMAP success criterion 5 assertions pass in test_amounts.py.
- No live network calls; OpenAI client fully mocked.
</verification>

<success_criteria>
- parse_ars_amount handles "1.500"→1500 and "1.234,56"→1234.56 and returns None on garbage (CONV-05).
- SlotExtractionService extracts concepto+monto from a free-form intent via mocked gpt-4o-mini (GASTO-01).
- All DTO fields Optional; D-01 minimal field set respected.
</success_criteria>

<output>
Create `.planning/phases/01-data-conversation-core/01-02-SUMMARY.md` when done.
</output>
