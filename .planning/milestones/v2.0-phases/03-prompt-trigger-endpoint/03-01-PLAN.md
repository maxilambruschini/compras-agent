---
phase: 03-prompt-trigger-endpoint
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_prompt_trigger.py
  - backend/tests/test_conversation_cierre.py
  - backend/tests/conftest.py
  - backend/app/services/cierre.py
  - backend/app/config.py
autonomous: true
requirements: [TRIG-01, TRIG-02, CAJA-01, CAJA-02]

must_haves:
  truths:
    - "The full quick test suite for this phase is importable and collects without ImportError (cierre.py skeleton + config token field exist)"
    - "Every Phase 3 requirement has at least one failing (RED) test asserting the target behavior"
    - "conftest sets GASTOS_PROMPT_TOKEN so happy-path endpoint tests can authenticate"
  artifacts:
    - path: "backend/tests/test_prompt_trigger.py"
      provides: "RED tests for TRIG-01 (auth, skip, state, lock) and TRIG-02 (prompt text)"
      contains: "test_missing_token_401"
    - path: "backend/tests/test_conversation_cierre.py"
      provides: "RED tests for CAJA-01/CAJA-02 FSM + hora_cierre + duplicate insert"
      contains: "test_hora_cierre_morning"
    - path: "backend/app/services/cierre.py"
      provides: "CajaCierreService skeleton + _derive_hora_cierre/_today_art so imports resolve"
      contains: "class CajaCierreService"
    - path: "backend/app/config.py"
      provides: "gastos_prompt_token Settings field (empty-string default)"
      contains: "gastos_prompt_token"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "GASTOS_PROMPT_TOKEN env var"
      via: "mp.setenv in env_setup fixture"
      pattern: "GASTOS_PROMPT_TOKEN"
---

<objective>
Wave 0 RED step for Phase 3. Create the two new test files (test_prompt_trigger.py, test_conversation_cierre.py), wire the GASTOS_PROMPT_TOKEN env var into the shared conftest fixture, add the empty-string `gastos_prompt_token` field to Settings, and lay down a CajaCierreService skeleton so test collection does not fail on import.

Purpose: Establish the failing-test contract before any production behavior is written, mirroring the RED→GREEN TDD pattern used in Phases 1 and 2. The tests in this plan reference symbols (routers.prompt.verify_token, ConvState.AWAITING_CIERRE, CajaCierreService.save_cierre) that do not yet fully exist — they are EXPECTED to fail/error until Plans 02 and 03 land. This plan only guarantees the files collect (no ImportError) and that the per-requirement assertions are present.
Output: Two RED test files, an extended conftest, a config field, and a cierre.py skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-prompt-trigger-endpoint/03-CONTEXT.md
@.planning/phases/03-prompt-trigger-endpoint/03-RESEARCH.md
@.planning/phases/03-prompt-trigger-endpoint/03-PATTERNS.md
@.planning/phases/03-prompt-trigger-endpoint/03-VALIDATION.md

<interfaces>
<!-- Verified from codebase 2026-05-30. Use these directly — no exploration needed. -->

ConvState (backend/app/services/conversation.py:85) — current constants:
  IDLE="idle", AWAITING_MONTO="awaiting_monto", AWAITING_TICKET="awaiting_ticket", CONFIRM="confirm"
  Plan 03 ADDS: AWAITING_CIERRE="awaiting_cierre", AWAITING_CIERRE_CONFIRM="awaiting_cierre_confirm"

ConversationOrchestrator.__init__(self, slot_service, gasto_service, provider) — POSITIONAL args (conversation.py:177)
  handle_message(self, session_factory, sender, text, message_id) (conversation.py:183)

DraftGasto, GastoSlots live in app.models.conversation (NOT app.services.conversation)
parse_ars_amount in app.services.amounts (returns Optional[Decimal])

CajaCierre ORM (backend/app/db/models.py:166): fields id (Uuid PK), fecha (Date), hora_cierre (String(5)), efectivo_en_caja (Numeric(14,2)), sender_phone (String(30)). NO unique constraint on (fecha, hora_cierre). Table already exists — NO migration.

Conversation ORM (models.py:116): sender_phone (String(30) PK), state (String(30)), draft_gasto (Text JSON), last_message_id (String(100)).

Test helpers to reuse verbatim (do NOT redefine):
  - make_mock_provider (test_gastos_webhook.py:67) — MagicMock(spec=WhatsAppProvider), send_message=AsyncMock
  - _make_session_factory (test_conversation.py:77) — async-context session factory wrapper
  - db_session, async_engine, env_setup fixtures (conftest.py)
  - app.routers.gastos.get_whatsapp_provider, app.db.session.get_db (for dependency_overrides)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gastos_prompt_token to Settings, wire GASTOS_PROMPT_TOKEN into conftest, create cierre.py skeleton</name>
  <files>backend/app/config.py, backend/tests/conftest.py, backend/app/services/cierre.py</files>
  <read_first>
    - backend/app/config.py (whole file — Settings class, line 6; twilio_from_number at line 45 is the placement analog)
    - backend/tests/conftest.py (env_setup fixture — find the AGENT_MODE setenv line)
    - backend/app/services/gasto.py (GastoService shape: __init__ at line 37, save_gasto at line 40, session.flush() at line 72 — the skeleton mirrors this signature)
    - backend/app/db/models.py lines 166-186 (CajaCierre fields)
    - .planning/phases/03-prompt-trigger-endpoint/03-RESEARCH.md (Pattern 5, Pattern 6, Pitfall 6)
  </read_first>
  <action>
    In backend/app/config.py, add field `gastos_prompt_token: str = ""` to the Settings class following the same empty-string-default pattern as `twilio_from_number` (line 45). Empty default is mandatory so the existing test suite (which does not set this env var) still instantiates Settings — the fail-closed enforcement happens in the verify_token dependency (Plan 02), not here. Add an inline comment noting empty = fail-closed (deny all) at runtime.

    In backend/tests/conftest.py, add `mp.setenv("GASTOS_PROMPT_TOKEN", "test-prompt-token")` to the existing session-scoped `env_setup` fixture, immediately after the existing `AGENT_MODE` setenv line (per Pitfall 6 — without this, happy-path endpoint tests get 401 because the configured token is "").

    Create backend/app/services/cierre.py as a SKELETON ONLY (so Plan 01 test files collect without ImportError; full implementation is Plan 03). Mirror the GastoService module docstring/structure from gasto.py. Include: module-level `_ART = ZoneInfo("America/Argentina/Buenos_Aires")` and `_CUTOFF = time(14, 30)`; module-level functions `_derive_hora_cierre() -> str` and `_today_art()` (these MAY be implemented now since they are pure and tested by test_hora_cierre_morning/afternoon — implement them per Pattern 5: return "12:00" if `datetime.now(_ART).time() < _CUTOFF` else "17:00"; `_today_art` returns `datetime.now(_ART).date()`); and `class CajaCierreService` with `__init__` (binds structlog logger like gasto.py) and an `async def save_cierre(self, session, efectivo_en_caja, sender_phone)` method whose body raises NotImplementedError (Plan 03 fills it). Import datetime, time from datetime; ZoneInfo from zoneinfo; Decimal from decimal; structlog; AsyncSession from sqlalchemy.ext.asyncio; CajaCierre from app.db.models. Do NOT use `datetime.utcnow()` (deprecated). Use module-level `datetime` reference so tests can patch `app.services.cierre.datetime`.
  </action>
  <verify>
    <automated>cd backend && python -c "import app.config; assert hasattr(app.config.Settings(), 'gastos_prompt_token'); import app.services.cierre as c; assert c._derive_hora_cierre.__module__=='app.services.cierre'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "gastos_prompt_token" backend/app/config.py` returns the field with `: str = ""` default.
    - `grep -n "GASTOS_PROMPT_TOKEN" backend/tests/conftest.py` returns the setenv line inside env_setup.
    - `cd backend && python -c "import app.services.cierre"` exits 0 (no ImportError).
    - `cd backend && python -c "from app.services.cierre import _derive_hora_cierre, _today_art, CajaCierreService; print('OK')"` exits 0.
    - `save_cierre` body is NotImplementedError (verified by grep `NotImplementedError` in cierre.py); the two pure time helpers are fully implemented.
  </acceptance_criteria>
  <done>Settings has gastos_prompt_token, conftest sets the token env var, cierre.py imports cleanly with implemented time helpers and a NotImplementedError save_cierre stub.</done>
</task>

<task type="auto">
  <name>Task 2: Write RED test file test_prompt_trigger.py (TRIG-01, TRIG-02)</name>
  <files>backend/tests/test_prompt_trigger.py</files>
  <read_first>
    - backend/tests/test_gastos_webhook.py (ASGI client fixture ~lines 92-120, make_mock_provider ~67-74, dependency_overrides ~107-118 — primary analog)
    - backend/tests/test_conversation.py (row-lock spy pattern ~lines 99-120 / test_conversation_lock_sql.py for FOR NO KEY UPDATE compile assertion)
    - backend/tests/test_conversation_lock_sql.py (whole file — how the codebase asserts FOR NO KEY UPDATE on a compiled Select)
    - .planning/phases/03-prompt-trigger-endpoint/03-PATTERNS.md (test_prompt_trigger.py section — fixtures + auth test bodies)
    - .planning/phases/03-prompt-trigger-endpoint/03-VALIDATION.md (Per-Task Verification Map — exact pytest node IDs)
  </read_first>
  <action>
    Create backend/tests/test_prompt_trigger.py. Build a `prompt_client` pytest_asyncio fixture mirroring the ASGI client fixture in test_gastos_webhook.py: set AGENT_MODE=gastos and GASTOS_PROMPT_TOKEN=test-prompt-token via monkeypatch, `get_settings.cache_clear()`, `create_app()`, override `app.dependency_overrides[get_whatsapp_provider]` with a make_mock_provider() instance and `app.dependency_overrides[get_db]` with a function yielding db_session; clear overrides and cache after. Reuse make_mock_provider (import or copy from the webhook test analog).

    Write these test functions with the EXACT node IDs from 03-VALIDATION.md (they will fail/error until Plans 02-03 land — that is the RED contract):
    - test_valid_token_sends: POST /gastos/prompt with body {"phone_number": "+5491112345678"} and header Authorization "Bearer test-prompt-token" → 200, json status=="sent", mock_provider.send_message awaited once.
    - test_missing_token_401: POST with no Authorization header → 401, send_message NOT awaited.
    - test_wrong_token_401: POST with "Bearer wrong-token" → 401, send_message NOT awaited.
    - test_empty_configured_token_denies: with GASTOS_PROMPT_TOKEN="" configured, even "Bearer " (empty credential) or any token → 401 (fail-closed). Use a separate app/client built with the empty token, or directly call verify_token with a crafted credentials object and assert HTTPException 401.
    - test_active_conversation_skipped: seed a Conversation row with state != IDLE (e.g. AWAITING_MONTO) for the phone, POST with valid token → 200, json status=="skipped", reason=="active_conversation", send_message NOT awaited, conv.state unchanged.
    - test_state_set_to_awaiting_cierre: idle/new recipient, POST valid token → after request, the Conversation row state == ConvState.AWAITING_CIERRE.
    - test_prompt_text_sent: POST valid token → send_message called with text that mentions cash-on-hand / efectivo and the three asks (assert the PROMPT_TEXT substrings: "efectivo" and the "otra compra" nudge — assert on the actual call args text).
    - test_row_lock_issued: spy on db_session.execute (per test_conversation_lock_sql.py pattern), call the endpoint, assert at least one issued Select has `_for_update_arg` set and its compiled Postgres form contains "FOR NO KEY UPDATE".
    Reference ConvState constants via `from app.services.conversation import ConvState` and PromptResponse/verify_token via `from app.routers.prompt import verify_token`. Do NOT implement production code here.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_prompt_trigger.py --collect-only -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_prompt_trigger.py --collect-only` lists all 8 named tests (test_valid_token_sends, test_missing_token_401, test_wrong_token_401, test_empty_configured_token_denies, test_active_conversation_skipped, test_state_set_to_awaiting_cierre, test_prompt_text_sent, test_row_lock_issued) with no collection error other than an expected ImportError on app.routers.prompt IF prompt.py absent — acceptable here ONLY if the file is written to tolerate the not-yet-existing import by importing inside the fixture/test bodies rather than at module top. Prefer in-test imports of app.routers.prompt so collection succeeds and tests RED-fail at runtime, not at collection.
    - Each test node ID exactly matches the IDs in 03-VALIDATION.md Per-Task Verification Map.
    - Running `pytest tests/test_prompt_trigger.py -x -q` exits non-zero (RED) because prompt.py/endpoint behavior does not exist yet — this is the expected RED state for Wave 0.
  </acceptance_criteria>
  <done>test_prompt_trigger.py collects cleanly and asserts every TRIG-01/TRIG-02 behavior with the canonical node IDs; tests are RED pending Plan 02.</done>
</task>

<task type="auto">
  <name>Task 3: Write RED test file test_conversation_cierre.py (CAJA-01, CAJA-02)</name>
  <files>backend/tests/test_conversation_cierre.py</files>
  <read_first>
    - backend/tests/test_conversation.py (whole file — _make_orchestrator helper ~52-74, _make_session_factory ~77-90, FSM state-transition test structure, is_confirmation usage)
    - backend/app/services/conversation.py lines 122-146 (is_confirmation / is_cancel affirmative token set — reuse, do not redefine)
    - .planning/phases/03-prompt-trigger-endpoint/03-PATTERNS.md (test_conversation_cierre.py section — FSM + hora_cierre time-mock bodies)
    - .planning/phases/03-prompt-trigger-endpoint/03-VALIDATION.md (Per-Task Verification Map — exact node IDs)
  </read_first>
  <action>
    Create backend/tests/test_conversation_cierre.py. Reuse `_make_orchestrator` and `_make_session_factory` from the test_conversation.py analog (import or copy). Seed Conversation rows in the target state via db_session before each scenario.

    Write these tests with the EXACT node IDs from 03-VALIDATION.md (RED until Plan 03):
    - test_bare_amount_advances_to_confirm: seed conv in ConvState.AWAITING_CIERRE; handle_message with text "1500" → conv.state becomes AWAITING_CIERRE_CONFIRM; draft_gasto holds the parsed amount.
    - test_gasto_intent_handoff: seed conv in AWAITING_CIERRE; mock slot_service.extract to return GastoSlots with concepto set (and parse_ars_amount on the text must return None so it falls through to GPT branch) → orchestrator routes into the gasto/idle flow (assert conv.state moved into the gasto path, e.g. AWAITING_TICKET or AWAITING_MONTO, and draft is not a cierre draft).
    - test_confirm_saves_cierre: seed conv in AWAITING_CIERRE_CONFIRM with a DraftCierre JSON ({"cierre_monto":"1500.00"}) in draft_gasto; handle_message with "sí" → a CajaCierre row exists in db_session for the sender, conv.state == IDLE, draft_gasto is None.
    - test_confirm_requires_exact_token: seed AWAITING_CIERRE_CONFIRM; handle_message with a non-affirmative reply (e.g. "tal vez") → NO CajaCierre row written, state stays AWAITING_CIERRE_CONFIRM (re-echo). GPT is never invoked at the gate (assert slot_service.extract NOT awaited in confirm).
    - test_hora_cierre_morning: patch app.services.cierre.datetime so .now returns 2026-05-30 11:00 ART → _derive_hora_cierre() == "12:00".
    - test_hora_cierre_afternoon: patch datetime.now → 2026-05-30 14:30 ART (boundary) → _derive_hora_cierre() == "17:00".
    - test_fecha_art_not_utc: patch datetime so ART date differs from naive UTC date at a late-evening UTC instant (e.g. choose a time where UTC and ART fall on different calendar days), assert save_cierre writes fecha == the ART date (use _today_art / the saved row's fecha). Drive through CajaCierreService.save_cierre with a real db_session.
    - test_duplicate_cierres_allowed: call save_cierre twice for the same derived (fecha, hora_cierre) → two distinct CajaCierre rows persist (count == 2), no IntegrityError (confirms no unique constraint).
    Import ConvState, DraftCierre from app.services.conversation; CajaCierreService from app.services.cierre; GastoSlots/DraftGasto from app.models.conversation. Prefer in-test imports for symbols not yet defined (DraftCierre, AWAITING_CIERRE*) so collection succeeds and tests RED-fail at runtime.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_conversation_cierre.py --collect-only -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_conversation_cierre.py --collect-only` lists all 8 tests (test_bare_amount_advances_to_confirm, test_gasto_intent_handoff, test_confirm_saves_cierre, test_confirm_requires_exact_token, test_hora_cierre_morning, test_hora_cierre_afternoon, test_fecha_art_not_utc, test_duplicate_cierres_allowed).
    - Node IDs match 03-VALIDATION.md exactly.
    - `pytest tests/test_conversation_cierre.py -x -q` exits non-zero (RED) pending Plan 03 implementation.
    - No test invokes a real OpenAI call (slot_service is mocked); confirm tests assert slot_service.extract is NOT awaited at the confirm gate.
  </acceptance_criteria>
  <done>test_conversation_cierre.py collects cleanly, asserts every CAJA-01/CAJA-02 behavior with canonical node IDs, GPT never hit at the confirm gate; tests RED pending Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → Settings/env | Tests must not silently disable the auth gate; the empty-token fail-closed behavior is asserted, not assumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-A2 | Elevation of Privilege | empty GASTOS_PROMPT_TOKEN | mitigate | test_empty_configured_token_denies (Task 2) locks in fail-closed before Plan 02 implements verify_token |
| T-03-SC | Tampering | npm/pip installs | accept | No new packages this phase (RESEARCH §Package Legitimacy Audit: satisfied by absence — zoneinfo/secrets are stdlib). No install task, no legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_prompt_trigger.py tests/test_conversation_cierre.py --collect-only -q` → both files collect, 16 tests total, no collection-time ImportError.
- `cd backend && python -c "import app.services.cierre; import app.config; from app.config import Settings; assert Settings().gastos_prompt_token == ''"` → exits 0.
- Existing suite still collects: `cd backend && python -m pytest tests/ --collect-only -q` exits 0.
</verification>

<success_criteria>
- gastos_prompt_token field present in Settings (empty default).
- conftest env_setup sets GASTOS_PROMPT_TOKEN.
- cierre.py imports cleanly; time helpers implemented; save_cierre is a NotImplementedError stub.
- Both new test files collect and define every canonical node ID from 03-VALIDATION.md.
- Both test files are RED (fail at runtime) — the GREEN transition is delivered by Plans 02 and 03.
</success_criteria>

<output>
Create `.planning/phases/03-prompt-trigger-endpoint/03-01-SUMMARY.md` when done.
</output>
